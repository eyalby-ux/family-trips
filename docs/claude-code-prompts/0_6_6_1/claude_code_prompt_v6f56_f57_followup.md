Good work on `V6-F56`/`V6-F57` — splitting DNI (schema-level, provable, real defect) from seat/gate/row/ticket-code (display-level, code-traced but not database-confirmed) is exactly the right level of precision, and being explicit about the Firestore/Firebase-CLI access limitation rather than guessing was the right call. Two follow-ups before this is fully closed — no fix yet, same as before.

## Follow-up 1: does `V6-F57`'s root cause also explain `AE-002` and `AE-004`'s missing seat data?

`V6-F57` found that `ticketHolderSchema` (which includes `seatOrSection`) is correctly captured into `proposed.details.ticketHolders` by the adapter, but `src/v5-app.js` never reads `ticketHolders` anywhere — a pure display gap, as far as the code shows.

Two other tested cases are also missing seat data on their saved items: `AE-002` (Row 4, Seat 26, Section/Gate B — frozen ground truth) and `AE-004` (3 tickets, each with an assigned seat — frozen ground truth). Since `seatOrSection` lives inside the same `ticketHolderSchema` structure `V6-F57` already traced, this is very likely the identical code path — but please confirm explicitly rather than leaving it as my inference: does the `activity-import-adapter.js:44` capture path and the confirmed-absent `v5-app.js` rendering gap apply uniformly regardless of which specific case's ticket holders are involved? If so, say so plainly so `V6-F57`'s scope is understood to cover `AE-001`+`AE-002`+`AE-004` together, not just `AE-001`. If you find anything case-specific that would make `AE-002` or `AE-004` behave differently (e.g. a different code path for single-ticket vs multi-ticket sources), flag that instead.

## Follow-up 2: `AE-005`'s two still-open items — use the existing regression test suite instead of live DB access

Two things from the same investigation prompt weren't addressed in the `V6-F56`/`V6-F57` report: `AE-005`'s missing `amount` (₪120 frozen ground truth, not shown on the saved item) and, more importantly, `AE-005`'s core precision test — whether `ticketNumber` was actually extracted as `2ZTN-G53R-GJ81P` (the order number `2ZTN-G53R-GJ8` with `1P` appended) rather than being confused with or collapsed into the order number itself. This is the one field the whole `AE-005` benchmark case exists to test, so it matters more than the general display-gap question.

You flagged no live Firestore/Firebase-CLI access as the blocker for confirming stored values directly. There should be a way around that specific gap: per `MVP_DELIVERY_PLAN_CURRENT_STATE_V1.md`'s `V53` entry, the `0.6.6` implementation package already includes `family-trips-repository/tests/v6-6-attraction-event-import.mjs`, described as covering all seven `AE-` cases "including the mandatory `AE-001` wild-card, SHA-256-verified against the frozen benchmark." Please:

1. Open that test file and check what it actually asserts for `AE-005` — does it assert the extracted `ticketNumber` equals `2ZTN-G53R-GJ81P` specifically (distinct from the order number), or does it only check `confirmationNumber`/top-level fields and never actually exercise the `ticketHolders`/`ticketNumber` precision distinction at all? Also check what, if anything, it asserts for `amount` on `AE-005`, and for `AE-001`'s DNI/seat data and `AE-002`/`AE-004`'s seat data while you're in there — a regression test that doesn't actually assert these frozen fields would itself be a gap worth naming (a false-green test), separate from `V6-F56`/`V6-F57`.
2. Run that test suite now and report whether it currently passes or fails, and for which specific assertions.
3. If the test suite doesn't cover these fields at all (rather than covering them and passing), that's a distinct, worth-naming finding on its own — a regression suite that claims to be "SHA-256-verified against the frozen benchmark" but doesn't actually assert the fields the benchmark was specifically designed to test is a coverage gap, not evidence either way about whether the extraction itself is correct.

## Follow-up 3: `AE-006` (WaterLand) adds a fifth confirmed instance of the same display gap, plus two new, unrelated issues

Testing `AE-006` (`1713265782549_entryTickets_1.pdf`, WaterLand water park, 4-tickets-in-one-PDF family case) surfaced the same missing-participants/ticket-breakdown symptom as `AE-001`/`AE-002`/`AE-004`/`AE-005`: nothing on the saved item shows the 4 `ticketNumbers` (`623000111`–`623000114`) or any per-ticket/participant breakdown, consistent with `V6-F57`'s root cause (`ticketHolders` captured but never rendered by `v5-app.js`). Please fold `AE-006` into Follow-up 1's confirmation — same question: does the identical code path apply here too? This case is a good one to check closely, since the `V53` implementation report specifically claims `AE-006` was tested to "yield zero fabricated `participants` while still individually preserving all four physical tickets via `details.ticketNumbers`/`ticketHolders`" — if that preservation is real but just unrendered, this is more confirming evidence for `V6-F57`; if the data isn't actually there, that V53 claim was wrong and this needs its own severity assessment separate from a pure display bug.

Two further things on `AE-006`, unrelated to the display-gap pattern — please check both, don't assume either way:

1. **Title accuracy:** the saved item's title is "WaterLand" — the provider/venue name. `AE-006`'s frozen ground truth title is "כניסה לפארק יום" (day park entry / Entry Ticket) — the discovery doc explicitly corrects an earlier draft that had mistakenly used a zoo-themed title, settling on "כניסה לפארק יום" as correct. Displaying the venue name as the title instead of the actual activity name looks like a real title-extraction defect, not a display gap — check the extraction/title-selection logic for this case and report what's actually happening.
2. **Possible fabricated/defaulted time:** the saved item's date/time field shows "24 באפר', 2024, 12:00" — a specific time, 12:00. `AE-006`'s frozen ground truth is explicit that this source is a day-ticket with **no time given at all**, unlike every other case in the `AE-` benchmark set. Check whether "12:00" was actually produced by the extraction model (a real fabrication — inventing a time the source doesn't contain) or whether it's a UI-layer default applied whenever `startAt` has a date but no time component (in which case the underlying stored value may be date-only and this is a rendering/formatting choice, not a fabrication — but worth checking whether that default should be visually distinguished from a real extracted time, since right now it's indistinguishable from one). Report which it is.

Register whichever of these two turn out to be genuine defects as new findings with the next available finding ID(s), same format as before.

Report back on all three follow-ups — whether `V6-F57` extends cleanly to `AE-002`/`AE-004`/`AE-006`, what the existing regression suite actually proves (or doesn't) about `AE-005`'s precision test and `amount`, and the `AE-006` title/time findings — before any correction package is authored for `V6-F56`/`V6-F57`. Still no need to block continuing the rest of `0.6.6` acceptance testing.
