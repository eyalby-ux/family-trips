Eyal approved: begin implementation of the `0.6.6.1` correction package now, per `claude/FAMILY_TRIPS_ALPHA_0_6_6_1_CORRECTION_SCOPE_V1.md` (`docs/versions/V6/`) as the source of truth for scope, fix specifications, and acceptance criteria. Follow the same discipline as every prior correction package this project has produced.

## What to do

1. Implement all 11 scoped findings — `V6-F55` (merged with `V6-F54`), `V6-F56`, `V6-F57`, `V6-F58`, `V6-F59`, `V6-F60`, `V6-F61`, `V6-F62`, `V6-F63`, `V6-F64`, `V6-F65` — per the scope document's per-finding fix specifications. Use the root-cause groupings in that document as your implementation order where they indicate a shared fix (Group A's extraction/prompt changes, Group B's rendering-completeness pass, Group C's standalone attach-capability addition, and the cross-cutting warning-lifecycle fix for `V6-F55`), but every finding still needs its own named regression test regardless of whether the underlying fix is shared with another finding — per this project's standing "mandatory regression test per closed finding" rule.
2. For `V6-F55` specifically: implement Eyal's preferred fix direction as recorded in the scope document — resolve merge conflicts by asking the user which value is correct (reusing the existing `directionAmbiguous`/`directionCandidates` picker UI pattern rather than building a new one) or by proposing a value based on the Trip's own date range with required explicit user confirmation — replacing the current silent-keep-plus-indefinite-warning behavior. If during implementation you find a reason this exact approach doesn't fit cleanly (e.g. a structural mismatch with the picker mechanism), stop and report back with the specific obstacle rather than substituting a different approach unilaterally.
3. Run the full quality gate: syntax checks, the complete automated test suite (including the new regression tests you add and the `V6-F58` assertions folded into Group B's work), and the production build. Confirm all of it passes.
4. Update the finding register (`MVP_DELIVERY_PLAN_CURRENT_STATE_V1.md`) to reflect implementation for each of the 11 findings — per this project's standard, do not mark any of them `Fixed` yet; that status is reserved for after Eyal's own QA acceptance retest confirms it, same pattern as every finding this session.
5. Commit the work to the appropriate branch. Do NOT deploy to QA or Production — deployment is a separate, explicitly-authorized step, same as every prior package; wait for that go-ahead after implementation is reported complete.

Report back once implementation, testing, and the build are complete and committed: what was changed and where (files/functions), confirmation the full quality gate passed, and the regression test names added per finding, so Eyal can review before authorizing a QA deploy.
