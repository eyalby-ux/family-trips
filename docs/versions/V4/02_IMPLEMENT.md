# 02 — IMPLEMENT V4

Implementation is performed by the Development Agent, not by the Product Owner.

Delivered scope:

- PDF, image, photo, pasted-link, and manual intake;
- suggestion-first workflow;
- explicit approve, edit, reject, and defer actions;
- type, provider, readable-title, date, and confirmation-number heuristics;
- EL AL flight recognition that is not overridden by generic `booking` text;
- original PDF/image storage in IndexedDB;
- source preservation after item deletion;
- duplicate warning with merge, keep both, and cancel;
- unsupported-file, invalid-link, oversized-file, partial-extraction, and missing-field recovery;
- local persistence and offline source access on the same device.
