# 06 — ROLLBACK V4

Rollback on silent incorrect item creation, source loss, duplicate data loss, auth/local-data regression, blank production, broken PWA update, or deployment mismatch.

Run `scripts/rollback_V4.ps1` locally to restore the pre-install files from the generated backup directory.
