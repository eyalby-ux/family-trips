# V4 Known Issues

- Extraction is heuristic and based on source name/URL metadata; it does not perform OCR or PDF text parsing in V4.
- Original uploaded files are available offline on the same browser/device through IndexedDB and are not synchronized to other devices.
- Firestore and Storage remain deny-all by design.
