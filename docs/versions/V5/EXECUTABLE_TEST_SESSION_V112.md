# V5 Executable Test Session — Alpha 0.5.6

**Document revision:** `V112`
**Status:** Alpha 0.5.5 is final NO-GO. Alpha 0.5.6 implements only the focused `V5-F27` range-boundary correction and is ready for installation and fixed-QA Product Owner acceptance. All passed 0.5.5 evidence remains accepted. `V5-F27` stays open until the corrected candidate is installed, the untouched tested `dist` is deployed with `--no-build`, the affected interval cases pass and the Product Owner issues explicit GO. Alpha 0.4.3 remains Production; Alpha 0.6 Smart Import implementation remains blocked.

## Document change history

| Document revision | Date | Product phase | Change |
|---|---|---|---|
| V1 | 2026-08-03 | Alpha 0.5.0 implementation | Created the complete V5 acceptance and short-regression sequence. |
| V2 | 2026-08-03 | Alpha 0.5.0 installer correction | Updated the installation prerequisite to support the local 0.4.4 NO-GO handoff while retaining 0.4.3 as the accepted Production baseline. |
| V3 | 2026-08-03 | Alpha 0.5.0 QA checkpoint | Recorded successful installation/build and Google sign-in, final classification of the Draft authorization issue, desktop-and-phone availability, fixed QA alias/process, open V5-F05/V5-W01 findings, and the exact resume point. |
| V4 | 2026-08-04 | Alpha 0.5.0 phone exploratory checkpoint | Recorded five phone findings and their exact retest criteria: V5-F06 through V5-F10. Preserved the numbered acceptance resume point and recorded that deletion is only a temporary continuation workaround for V5-F07. |
| V5 | 2026-08-04 | Alpha 0.5.0 F08 interaction clarification | Expanded V5-F08 from standalone Calendar placement into the full Timeline contract: Calendar-based Entire Trip, Week and Day views, a list below filtered to the active period, and synchronized week selection/list updates when navigating by arrows or swipe/scroll. |
| V6 | 2026-08-04 | Alpha 0.5.0 structured acceptance resumption | Recorded test 1 FAIL because the login screen still displays V4; added V5-F11 for the missing Settings delete/reset controls and V5-F12 for the blank screen after sign-out/sign-in. Both new findings are Alpha 0.5 release blockers. |
| V7 | 2026-08-04 | Alpha 0.5.0 blocker characterization | Recorded that `V5-F12` persists after `Ctrl+Shift+R`, with the account shell visible but the main application area blank; reconfirmed `V5-F07` after Insurance creation; preserved test 2 as not started and set console-error capture as the next guided action. |
| V8 | 2026-08-04 | Alpha 0.5.0 Console evidence | Captured the first complete application error for `V5-F12`: `Uncaught (in promise) TypeError: Cannot read properties of undefined (reading 'id')` in `v5-app-DoxSbB8E.js`, with `Array.findIndex` in the stack. Separated Chrome-extension messages from application evidence and made exact source-location inspection the next guided action. |
| V9 | 2026-08-04 | Alpha 0.5.0 root-cause confirmation | Source inspection identified the exact Today quick-access expression: the candidate array contains absent hotel/transport entries, while its duplicate-removal `findIndex` reads `.id` before those entries are removed. A Flight or Insurance makes a later valid candidate execute that unsafe lookup. Linked `V5-F07` and the observed `V5-F12` re-entry failure to this shared render defect, defined the null-safe correction, and stopped further acceptance until a corrected QA build is available. |
| V10 | 2026-08-04 | Alpha 0.5.1 correction candidate | Recorded implementation of V5-F05, F06, F07/F12, F08, F10 and F11, preserved V5-F09 in Alpha 0.6, and defined the exact resume gate on the fixed QA alias without closing any finding before Product Owner retest. |
| V11 | 2026-08-04 | Alpha 0.5.1 QA retest checkpoint | Recorded successful installation/build, fixed-QA deployment, Alpha 0.5.1 identity, Google sign-in and V5-F11 retest. Recorded that a saved Flight no longer makes Today blank, but appears both in Today and Quick Access, while the Product Owner expects preparation-stage Quick Access to prioritize packing and tasks. Added V5-F13, preserved all incomplete focused regressions, and recorded the exact resume point for the next session. |
| V12 | 2026-08-05 | Alpha 0.5.1 QR acceptance coverage | Added the missing executable QR-generation tests ahead of scanning: run and identify the approved generator, validate single and multipart artifacts plus `.txt` sidecars, independently decode/reconstruct and compare payloads, verify multi-file relationships and special characters, and prove clean failure without misleading QR output. Preserved the existing application scanning sequence and one-action-at-a-time rule. |
| V13 | 2026-08-05 | Task workflow findings and future coverage | Recorded `V5-F13` future-task omission and the fixed maximum of three Quick Access tasks; added release-blocking `V5-F14` because saved tasks cannot be edited. Added deferred Alpha 0.6 acceptance coverage for visible due date/priority, priority and due-date filters, combined filters, reset and deterministic ordering. |
| V14 | 2026-08-06 | Alpha 0.5.1 consolidated QA checkpoint | Reconciled the executable document with the later Product Owner chat evidence. Marked the complete QR generation/import matrix as executed, added the cold-start offline blocker and import/link/UI findings, recorded the passed item lifecycle/offline-warm scenarios, separated tests still required before planning 0.5.2 from post-fix 0.5.2 regressions, and assigned correction findings to 0.5.2 versus new capabilities to 0.6. |
| V15 | 2026-08-06 | Alpha 0.5.2 planning handoff | Recorded completion of the pre-planning gate: V5-F06, V5-F07, V5-F10 and V5-F12 passed; V5-F08 remains a verified Timeline contract gap; V5-F13 future-task handling and V5-F14 task editing remain failed; URL editing and cold-start offline remain verified gaps. Removed the incorrect `Not defined` Trip-date finding and froze the 0.5.2 correction and acceptance scope. |
| V16 | 2026-08-06 | Alpha 0.5.2 correction implementation | Reconciled the QA chat against the frozen scope, recorded implementation and automated PASS for all eight approved repairs, preserved the previously passed evidence, retained Alpha 0.6 deferrals, and identified `V5-W01` as the only earlier warning requiring an explicit target decision before package closure. |
| V17 | 2026-08-07 | Alpha 0.5.2 COOP correction decision | Recorded Product Owner approval to include `V5-W01` in Alpha 0.5.2, added the `same-origin-allow-popups` Netlify header and automated configuration evidence, expanded the correction scope to nine, and added deployed Google sign-in/Console acceptance. |
| V18 | 2026-08-08 | Alpha 0.5.2 paused QA checkpoint | Recorded `V5-F19`: the user-visible standalone Links area added to Trip Center is redundant and must be removed. Links remain properties of their owning items; an internal background index is permitted if needed. Preserved the exact acceptance resume point at `V5-F15`. |
| V19 | 2026-08-09 | Alpha 0.5.2 resumed QA checkpoint | Recorded PASS for existing URL replacement/removal, usable phone import layout, safe generic-link handling, and destructive-confirmation wording. Kept `V5-F17` open because the real shared link produced only the generic `Shared link` title instead of relevant content such as `BIG גדרה`; kept `V5-F19` open because the item still appears in a standalone Links collection. Set executable step 9, deployed COOP/Google popup verification, as the exact resume point. |
| V20 | 2026-08-09 | Alpha 0.5.2 paused before desktop verification | Recorded the Product Owner pause because the desktop computer is unavailable. Preserved every result through step 8, kept step 9 as the exact resume point, and added the desktop continuation commands and observation sequence. No finding was closed or reclassified. |
| V21 | 2026-08-09 | Alpha 0.5.2 desktop verification complete | Recorded that the fixed QA deployment returns `Cross-Origin-Opener-Policy: same-origin-allow-popups` and Google popup sign-in succeeds, but Chrome Console still reports the popup-close COOP warning twice. Reclassified the step-9 outcome as functional sign-in PASS with `V5-W01` still open; set compact regression step 10 as the exact resume point. |
| V22 | 2026-08-09 | Alpha 0.5.2 QR-guided regression | Added the Product Owner requirement that every QR-dependent test must display the exact QR image or ordered image set in the conversation together with the expected result. Generated and independently validated the single `FTI1` smoke-test image for compact regression step 10; the scan result remains pending. |
| V23 | 2026-08-10 | Alpha 0.5.2 single-QR regression PASS | Recorded that the single `FTI1` image produced exactly one approval-stage proposal with the expected Bangkok location, 2026-01-25 date, `בדיקת QR יחיד` title and note, without reported corruption, immediate save or duplication. Set the validated multipart `FTI1C` smoke test as the next action and preserved the rule to display every required QR image with its expected result. |
| V24 | 2026-08-10 | Alpha 0.5.2 multipart QR checkpoint | Recorded that scanning multipart `FTI1C` part 2 of 2 first produced no visible response. Added open `V5-F20` for missing progress/missing-part feedback, without yet concluding whether the part was retained internally. Set part 1 of 2 as the exact next scan: a completed proposal will prove silent retention; otherwise the application must at least display 1/2 progress with part 2 missing. |
| V25 | 2026-08-10 | Alpha 0.5.2 multipart QR PASS | Recorded successful multipart reconstruction in both scan orders. Part 1 first displayed 1/2 progress and completed after part 2; part 2 first explicitly displayed part 2 of 2 and identified part 1 as missing, then completed after part 1. With the original no-response observation not reproduced, withdrew provisional `V5-F20` as a missed capture rather than a product defect. Set approval of the current imported proposal as the next compact-regression action. |
| V26 | 2026-08-10 | Alpha 0.5.2 imported-proposal approval PASS | Recorded that approving the multipart Flight proposal without changing its fields created exactly one saved item, with no duplication reported. Advanced compact regression to the edit/persist/delete lifecycle of that same imported item. |
| V27 | 2026-08-10 | Alpha 0.5.2 imported-item edit PASS | Recorded that the saved imported Flight was edited to Activity, renamed to `בדיקת פריט מיובא ערוך`, and assigned `https://www.elal.com`. The same record was updated and remained singular, with the new type, title and link displayed. Advanced the lifecycle regression to refresh persistence before deletion. |
| V28 | 2026-08-10 | Alpha 0.5.2 imported-item refresh persistence PASS | Recorded that after application refresh/reload the edited imported Activity remained exactly one record, with title `בדיקת פריט מיובא ערוך` and URL `https://www.elal.com` still present and opening correctly. Advanced the lifecycle regression to deletion of that same test item. |
| V29 | 2026-08-10 | Alpha 0.5.2 imported-item deletion PASS | Recorded that deleting `בדיקת פריט מיובא ערוך` removed the imported test item permanently after refresh and did not delete or change any unrelated item. Completed the QR/imported-item lifecycle regression and advanced compact regression to the warm-offline edit/reconnect cycle. The already-failed cold-start offline acceptance check is not repeated. |
| V30 | 2026-08-10 | Alpha 0.5.2 warm-offline regression PASS | Recorded that editing one existing test item while the already-open application was offline succeeded, displayed exactly one edited record, and persisted after connectivity was restored and the application was refreshed. No unrelated item changed. Advanced compact regression to the Flight-plus-Insurance Today/re-entry cycle without repeating the already-passed functional scenarios beyond the required smoke path. |
| V31 | 2026-08-10 | Alpha 0.5.2 Flight-plus-Insurance re-entry regression PASS | Recorded that Today rendered normally with valid Flight and Insurance records, each relevant item appeared once and opened correctly, and the same state remained intact after Google sign-out/sign-in. No blank screen, loss or duplication occurred. Advanced compact regression to the preparation-task lifecycle and Quick Access ordering check. |
| V32 | 2026-08-10 | Alpha 0.5.2 preparation-task create/edit regression PASS | Recorded that a future open preparation task appeared once in the Tasks list and in Quick Access when eligible. Editing its title updated the same single record in both locations without duplication. Advanced the lifecycle regression to completion and uncompletion of that same task before restart persistence. |
| V33 | 2026-08-10 | Alpha 0.5.2 preparation-task completion/uncompletion regression PASS | Recorded that completing the edited preparation task removed it from Quick Access and allowed the next eligible task to fill the slot when present. Reopening the same task restored the same single record to the Tasks list and Quick Access when eligible, without duplication. Advanced the lifecycle regression to refresh/restart persistence of that reopened task. |
| V34 | 2026-08-10 | Alpha 0.5.2 preparation-task restart persistence PASS | Recorded that after fully closing and reopening the connected application, `בדיקת משימת הכנה ערוכה` remained open as exactly one record and appeared in Quick Access when eligible. Completed the preparation-task lifecycle regression and advanced compact regression to Timeline Entire Trip / Week / Day navigation and date selection. |
| V35 | 2026-08-10 | Alpha 0.5.2 Timeline compact regression PASS | Recorded that Entire Trip remained chronological, Week arrows and swipe synchronized the visible active week with the list, Day showed only the selected date, the correct item opened, and Timeline navigation did not change the item date. Advanced compact regression to the Trip-date/out-of-range smoke check. |
| V36 | 2026-08-10 | Alpha 0.5.2 out-of-range warning regression FAIL | Recorded `V5-F21`: an item could be changed to one day after the Trip end date and saved without any warning or explicit proceed confirmation. The saved item was clearly marked as outside the Trip range, so post-save identification passed. Set restoration to the original in-range date as the exact next action before the final Settings delete-cancel smoke check. |
| V37 | 2026-08-10 | Alpha 0.5.2 out-of-range restoration PASS | Recorded that returning the same test item to its original in-range date removed the out-of-range marker, preserved exactly one record and did not change unrelated items or Trip dates. `V5-F21` remains open solely for the missing pre-save warning and explicit confirmation. Advanced compact regression to the final Settings delete-cancel smoke check. |
| V38 | 2026-08-10 | Alpha 0.5.2 current Trip-data delete-cancel PASS | Recorded that the current Trip-data deletion confirmation clearly identified its target and scope, contained no unsupported claim about other Trips, and Cancel preserved all Trip data unchanged. Advanced compact regression to the remaining Full Trip Reset confirmation-and-cancel smoke check. |
| V39 | 2026-08-10 | Alpha 0.5.2 final QA decision | Recorded PASS for the Full Trip Reset confirmation-and-cancel smoke check: the warning identified the full-reset scope without unsupported claims, and Cancel preserved the Trip, dates, tasks and items. Closed executable acceptance and compact regression as complete, issued final `NO-GO`, retained Alpha 0.4.3 in Production and set the next activity to correction planning rather than additional testing of this candidate. |
| V40 | 2026-08-10 | Alpha 0.5.2 cross-chat reconciliation | Reconciled the final QA document with Product Owner decisions recorded in the planning chat. Confirmed that `V5-F19` was already fully merged, retained `V5-F21` for the out-of-range pre-save warning, and added `V5-F22` for removal of the standalone user-visible Locations category while preserving item-owned location data, aggregate map projection, navigation and related background capabilities. The Shopping category decision remains Alpha 0.6 planning scope and is not represented as a 0.5.2 QA finding. |
| V41 | 2026-08-10 | Alpha 0.5.3 correction planning and implementation | Preserved the complete 0.5.2 evidence and final NO-GO. Froze the 0.5.3 scope to cold-start offline, `V5-F17`, `V5-F19`, `V5-F21` and `V5-F22`; retained `V5-W01` for technical verification. Added the replacement focused acceptance sequence and kept all Alpha 0.6 scope excluded. |
| V42 | 2026-08-10 | Alpha 0.5.3 correction installation checkpoint | Recorded successful R2 installation over the detected Alpha 0.5.2 NO-GO baseline, full automated and production-build PASS, installed version `0.5.3`, branch `version/V5` and the created backup. Preserved all findings as open and set fixed-QA deployment of the same tested `dist` as the exact next action. |
| V43 | 2026-08-10 | Alpha 0.5.3 fixed-QA deployment checkpoint | Recorded successful Draft deployment of the already-tested `dist` to the fixed `qa` alias, including deploy ID `6a7a1a215ae74b40bafc1f35`. Confirmed that no Production deployment was performed and set displayed `Alpha 0.5.3` identity verification as the exact next action before focused acceptance step 1. |
| V44 | 2026-08-10 | Alpha 0.5.3 displayed-version verification PASS | Recorded Product Owner confirmation that the fixed QA deployment displays `Alpha 0.5.3`. Closed the identity gate and advanced the exact resume point to focused acceptance step 1: installed-phone PWA cold-start offline and reconnect recovery, without clearing site data or uninstalling the PWA. |
| V45 | 2026-08-10 | Alpha 0.5.3 cold-start offline FAIL | Recorded focused acceptance step 1 as release-blocking FAIL: after the installed PWA was fully closed and both network paths were disabled, reopening from its icon showed the browser connection-error page instead of the cached application shell. In the subsequent refresh/recovery path, the first refresh reported no connection and the second loaded successfully; the precise network state during those refreshes remains to be isolated. Preserved the Product Owner's Google-auth suspicion as an unproven hypothesis; because the browser error appears before the application shell loads, the immediate evidence points first to service-worker navigation control/cache or reconnect transition behavior. Set a controlled two-refresh test while connectivity remains disabled as the exact next diagnostic action. |
| V46 | 2026-08-10 | Alpha 0.5.3 deterministic offline alternation and sign-out FAIL | Recorded the controlled offline diagnostic result: while both network paths remained disabled, refreshes alternated consistently between browser connection error on every odd refresh and successful cached application/Trip load on every even refresh. This removes the earlier uncertainty about the refresh network state and confirms a deterministic service-worker/navigation-cache control defect rather than random connectivity. Added release-blocking `V5-F23`: selecting Sign out while the cached application is offline immediately reaches the connection-error page and offers no recovery until the network returns. Stopped further offline refresh diagnosis on this build and advanced the exact resume point to focused online acceptance step 2. |
| V47 | 2026-08-10 | Alpha 0.5.3 shared-link intake and orphan-Link FAIL | Recorded focused acceptance step 2. The proposal displayed `Shared link`, exposed the original URL and allowed the title and item type to be changed before approval. `V5-F17` remains open because the default title was still not meaningful. Added release-blocking `V5-F24`: approval was permitted while the proposal remained a standalone Link, but after save the record was not visible because the standalone Links collection had intentionally been removed and no owning item was associated. Defined the correction contract: Link is intake/source data, not a final orphan item; approval must require association with an existing/new meaningful owning item or conversion to a supported owning type. Advanced the exact resume point to focused acceptance step 3 without asking the Product Owner to recover or delete the inaccessible test record. |
| V48 | 2026-08-10 | Shared-URL import product decision | Recorded the Product Owner decision that `Shared link` is not an acceptable import fallback. When the application cannot read enough meaningful data from a shared URL to create a supported item, the share-import flow must stop without creating a proposal or record. A URL remains addable from the editor of an existing item; contextual attachment is permitted only when the application can identify and present a clear owning item or meaningful item type for explicit approval. Updated `V5-F17` and `V5-F24` acceptance criteria accordingly; this decision supersedes the earlier current-release fallback wording without rewriting historical 0.5.2 evidence. |
| V49 | 2026-08-10 | Alpha 0.5.3 standalone Links removal PASS | Recorded focused acceptance step 3 as PASS. Trip Center exposes no standalone Links category or destination, while an existing item-owned URL remains visible inside its meaningful owning item and opens the correct address. Closed `V5-F19` for Alpha 0.5.3 without closing `V5-F17` or `V5-F24`, because failed URL extraction can still produce a `Shared link` proposal and an inaccessible orphan Link. Advanced the exact resume point to focused acceptance step 4, `V5-F22`. |
| V50 | 2026-08-10 | Alpha 0.5.3 standalone Locations removal PASS | Recorded focused acceptance step 4 as PASS. Trip Center exposes no standalone Locations category or destination, while a pre-existing meaningful item retains its address and its navigation/map action opens the correct destination. Closed `V5-F22` for Alpha 0.5.3 and advanced the exact resume point to focused acceptance step 5, `V5-F21`, beginning with the warning-and-Cancel path. |
| V51 | 2026-08-10 | Alpha 0.5.3 out-of-range warning Cancel branch PASS | Recorded the first half of focused acceptance step 5 / `V5-F21` as PASS. The pre-save warning clearly identified the configured Trip range; choosing Cancel preserved the same item's original in-range date and created no duplicate. Kept `V5-F21` open pending the Proceed/save branch, preservation of the existing out-of-range marker, single-record check and restoration to the original date. |
| V52 | 2026-08-10 | Alpha 0.5.3 out-of-range warning Proceed/restoration PASS | Recorded the second half of focused acceptance step 5 as PASS and closed `V5-F21`. Proceed saved the same item exactly once outside the Trip range and preserved its outside-range marker. Restoring the original date removed the marker, kept one record and caused no unrelated item or Trip-date changes. Advanced the exact resume point to focused acceptance step 6, deployed COOP header and Google popup Console verification. |
| V53 | 2026-08-10 | Alpha 0.5.3 Google popup functional PASS / COOP warning FAIL | Recorded focused acceptance step 6 as partial PASS. Google popup sign-in succeeded, but the Product Owner's Console capture showed `Cross-Origin-Opener-Policy policy would block the window.close call` twice. Kept `V5-W01` open because the no-warning criterion failed. Did not infer the 0.5.3 response header from functional sign-in or the Console capture; set verification of the current QA response header as the only remaining observation for this step. |
| V54 | 2026-08-10 | Alpha 0.5.3 COOP response-header verification | Recorded the Product Owner's Network evidence that the current QA document response contains exactly `Cross-Origin-Opener-Policy: same-origin-allow-popups`. Completed the evidence set for focused acceptance step 6: the required header is served and Google sign-in succeeds, but the popup-close warning still occurs twice. Kept `V5-W01` open and advanced to the smallest focused regression step 7. |
| V55 | 2026-08-10 | Alpha 0.5.3 URL regression PASS and end-of-day pause | Recorded the first check of focused regression step 7 as PASS: an existing item's URL was temporarily replaced, saved, reopened and opened correctly; the original URL was then restored and persisted on the same single record without duplication. Recorded the Product Owner's end-of-day pause. Preserved `NO-GO / acceptance pending` and set the next-session resume point to the item-owned location/map smoke check without repeating completed URL, COOP, Offline or shared-link tests. |
| V56 | 2026-08-11 | Alpha 0.5.3 item-owned location/map regression PASS | Recorded focused regression step 7B as PASS: an existing item's displayed address/location was correct, its map/navigation action opened the correct destination, and returning to the application left the same single item unchanged and without duplication. Advanced the exact resume point to step 7C, the warm-offline edit/reconnect smoke check, without repeating cold-start Offline diagnosis. |
| V57 | 2026-08-11 | Alpha 0.5.3 warm-offline regression PASS and QR smoke insertion | Recorded focused regression step 7C as PASS: an item edited while the already-open application was Offline remained singular and persisted after connectivity and refresh, then its original title was restored without collateral changes. At the Product Owner's request, inserted one additional non-blocking QR creation-to-ingestion smoke check before the remaining Timeline smoke navigation. The complete QR-generation and ingestion matrix remains previously passed and is not reopened. |
| V58 | 2026-08-11 | QR fixture reuse and official-source enrichment rule | Recorded the Product Owner decision that `Confirmation_for_Booking_ Ban Ta Khun.pdf` is the default source document for every future repeated QR check unless another document is supplied. Corrected the current fixture by adding the independently verified official Panvaree Resort website, `https://www.panvaree.com/`; the official-site identity was confirmed by matching the property name, Ratchaprapa Dam / Khao Sok location and the `+66 90-991-5995` contact number from the booking document. External enrichment must be traceable and must not be represented as content extracted from the PDF. The regenerated rich fixture remains a multipart `FTI1C` series and replaces the pre-scan version that omitted the website. |
| V59 | 2026-08-11 | QR pre-rescan findings and external-enrichment instruction correction | Recorded screenshot evidence for new `V5-F25`: rejected proposals are displayed in the active Suggestions section before a new pending proposal and must instead move to an archive. Determined that the first Panvaree proposal's general park location matched the general address encoded in the QR fixture, so this is a generator/instruction defect rather than a QR decoder failure. Advanced the ingestion instructions to V4 with mandatory official-site, exact-place and specific-detail research. Regenerated and independently verified a five-part `FTI1C` fixture whose `location` begins with the exact named property, retains the PDF address separately, and records website, place/access details and external provenance. |
| V60 | 2026-08-11 | Corrected Panvaree QR smoke PASS | Recorded Product Owner confirmation that the corrected five-part `FTI1C` series ingested correctly as one Panvaree Resort hotel proposal with the official website, exact named-property location and navigation target at the resort. Closed only the additional QR smoke; retained `V5-F25` because rejected proposals still require a separate archive, and advanced the exact resume point to Timeline smoke step 7D. |
| V61 | 2026-08-11 | Alpha 0.5.3 final QA decision | Recorded focused regression step 7D as PASS: Entire Trip, Week and Day remained synchronized with their lists, an existing item opened correctly, and returning preserved its date and singular record. Completed all focused acceptance and regression observations, issued final `NO-GO / acceptance complete`, retained Alpha 0.4.3 in Production and moved the next activity to correction planning. |
| V62 | 2026-08-11 | Alpha 0.5.4 correction planning | Reconciled the final 0.5.3 NO-GO evidence, approved a focused 0.5.4 before Alpha 0.6, froze the correction scope and one-action-at-a-time acceptance sequence, selected blocked Offline Sign out with local-session preservation, prohibited `Shared link` fallback/orphan Link persistence, required a separate rejected-proposal archive, assigned `V5-W01` to non-blocking Alpha 0.6 hardening and preserved all accepted 0.5.3 evidence. |
| V63 | 2026-08-11 | Alpha 0.5.4 implementation | Recorded completed implementation, zero-vulnerability dependency disposition, full automated and Production-build PASS, package preparation and the pending Product Owner installation/fixed-QA acceptance gate. |
| V64 | 2026-08-11 | Alpha 0.5.4 installation checkpoint | Recorded successful Product Owner correction installation over the Alpha 0.5.3 handoff, full automated and Production-build PASS, installed version `0.5.4`, branch `version/V5` and rollback backup. Preserved every Product Owner finding as open and set fixed-QA deployment of the exact already-tested `dist`, without rebuild or Production deployment, as the exact next action. |
| V65 | 2026-08-11 | Alpha 0.5.4 fixed-QA deployment checkpoint | Recorded successful Draft deployment of the exact already-tested `dist` to the fixed `qa` alias, including deploy ID `6a7ac35b55da1eee6726154e`. Confirmed that no rebuild and no Production deployment were performed. Preserved all Product Owner findings as open and advanced the exact resume point to focused acceptance step 1: displayed `Alpha 0.5.4`, functional Google sign-in and existing-Trip load. |
| V66 | 2026-08-11 | Alpha 0.5.4 online-baseline PASS | Recorded focused acceptance step 1 as PASS: fixed QA displayed `Alpha 0.5.4`, Google sign-in completed and the existing Trip/data loaded correctly. Preserved all correction findings as open and advanced the exact resume point to step 2, a single first-attempt cold-start Offline launch of the installed phone PWA after fully closing it and disabling Wi-Fi and mobile data. |
| V67 | 2026-08-11 | Alpha 0.5.4 first-attempt cold-start Offline PASS | Recorded focused acceptance step 2 as PASS: after the installed phone PWA was fully closed and both Wi-Fi and mobile data were disabled, the application shell and last saved Trip appeared on the first launch from the home-screen icon without a browser connection-error page. This accepts the first-launch criterion but does not yet close the broader Offline correction. Advanced the exact resume point to step 3: two controlled Offline refresh/reopen cycles followed by reconnect and single-record/data-integrity verification. |
| V68 | 2026-08-11 | Alpha 0.5.4 Offline stability/reconnect PASS | Recorded focused acceptance step 3 as PASS: with Wi-Fi and mobile data still disabled, one in-app refresh and one full close/reopen both loaded the same cached application and Trip without the former odd/even browser-error alternation. Restoring connectivity returned the application online automatically without lost information or duplicated records. Accepted the cold-start plus stability/reconnect correction path and advanced the exact resume point to step 4, the `V5-F23` Offline Sign out retest. |
| V69 | 2026-08-11 | Alpha 0.5.4 Offline Sign out PASS | Recorded focused acceptance step 4 / `V5-F23` as PASS: while Offline, selecting Sign out did not terminate the local session or navigate to a browser connection-error page, and a clear connectivity-required message was displayed. Closed `V5-F23` for Alpha 0.5.4 and advanced the exact resume point to step 5, the insufficient shared-URL retest for `V5-F17`/`V5-F24`. |
| V70 | 2026-08-11 | Alpha 0.5.4 insufficient shared-URL PASS | Recorded focused acceptance step 5 / `V5-F17` and `V5-F24` as PASS: re-sharing the same real `BIG גדרה` URL that previously created a generic `Shared link` now displayed a clear import failure and created no proposal, source, standalone Link record or inaccessible item. Closed both findings for Alpha 0.5.4 and advanced the exact resume point to step 6A, supported shared-URL intake. |
| V71 | 2026-08-11 | Alpha 0.5.4 supported shared-URL save blocked | Recorded focused acceptance step 6A as `FAIL — characterization pending`: the controlled Panvaree URL reached an item/proposal flow, but the Product Owner reported that the item could not be saved. No workaround or field mutation is permitted before observing whether Save is disabled, unresponsive or produces a visible validation/error message. Step 6B and all later steps remain paused. |
| V72 | 2026-08-11 | Alpha 0.5.4 supported shared-URL rejection characterized | Screenshot evidence showed that Save was not reached: the application displayed `לא ניתן לזהות מהקישור פריט נסיעה נתמך. לא נוצרה הצעה, מקור או פריט. אפשר להוסיף את הקישור מתוך עריכת פריט קיים.` The safe no-orphan behavior passed, but the official Panvaree hotel URL was not recognized and produced no supported Hotel proposal. Reclassified step 6A as a characterized blocking FAIL in the supported branch of `V5-F17`, retained `V5-F24` as closed, and unpaused step 6B for the separate existing-item URL regression. |
| V73 | 2026-08-11 | Alpha 0.5.4 existing-item URL regression PASS | Recorded focused step 6B as PASS: a stable existing non-critical item's original URL was recorded, replaced with the Panvaree URL, saved, reopened and opened at the correct destination; no additional item was created. Restoring the original URL persisted on the same single record. Preserved step 6A as a blocking FAIL under `V5-F17` and advanced the exact resume point to step 7 / `V5-F25`, active/archive separation. |
| V74 | 2026-08-11 | Alpha 0.5.4 controlled archive-test proposal PASS | Recorded focused step 7A as PASS: scanning the ordered five-part controlled Panvaree QR series produced exactly one `Panvaree Resort` proposal in `Pending approval`, with no approval or rejection yet performed. Preserved `V5-F25` as open and advanced the exact resume point to step 7B: reject that proposal and verify it immediately leaves active Suggestions and appears exactly once in the rejected-proposal archive before any refresh. |
| V75 | 2026-08-11 | Alpha 0.5.4 immediate active/archive separation PASS | Recorded focused step 7B as PASS: rejecting the single controlled `Panvaree Resort` proposal removed it from active Suggestions and displayed it exactly once in the rejected-proposal archive. Preserved `V5-F25` as partially accepted pending persistence and later-pending coexistence checks. Added the Product Owner's non-blocking UX preference to place the archive at the bottom of the screen after active content and Sources. Advanced the exact resume point to step 7C: refresh/reopen and verify that the rejected proposal remains exactly once only in the archive. |
| V76 | 2026-08-11 | Alpha 0.5.4 rejected-archive persistence PASS | Recorded focused step 7C as PASS: after refresh, `Panvaree Resort` remained exactly once in the rejected-proposal archive and did not return to active Suggestions. Accepted the persistence branch of `V5-F25` and left only later-pending coexistence open. Added a distinct independently decoded single-part controlled QR for step 7D and advanced the exact resume point to verifying that the new pending proposal remains once in active Suggestions while the rejected Panvaree entry remains once only in the archive. |
| V77 | 2026-08-11 | Alpha 0.5.4 step 7D fixture correction | Recorded that the first distinct QR was rejected before proposal creation with `השדה provider חייב להיות מחרוזת`. Classified the observation as a QA-fixture/schema failure, not a product failure, because no active/archive coexistence behavior was exercised. Regenerated the single-part fixture with all required string fields (`provider`, `confirmationNumber`, `location`, `website`, `phone`, `startAt`, `endAt`, `notes`) and required objects (`details`, `dateMeta`, `fieldConfidence`), independently decoded it, verified `provider` as string `Agoda`, and retained step 7D as the exact resume point. |
| V78 | 2026-08-11 | Alpha 0.5.4 corrected step 7D proposal creation PASS | Recorded Product Owner confirmation that scanning the corrected distinct QR created a new proposal. This passes proposal creation but does not yet close `V5-F25`; the remaining observation is to verify that the new proposal exists exactly once only in active Suggestions as `Pending approval` while rejected `Panvaree Resort` remains exactly once only in the archive. |
| V79 | 2026-08-11 | Alpha 0.5.4 active/archive coexistence PASS | Recorded Product Owner confirmation that `בדיקת הצעה חדשה לאחר ארכיון` appears exactly once only in active Suggestions as `Pending approval`, while rejected `Panvaree Resort` remains exactly once only in the archive, with no duplication or state mixing. Completed the full rejection, archive-persistence and later-pending coexistence contract and closed `V5-F25` for Alpha 0.5.4. Preserved the archive-bottom preference as non-blocking UX refinement and advanced the exact resume point to step 8A: verify that Trip Center still exposes no standalone Links or Locations destination. |
| V80 | 2026-08-11 | Alpha 0.5.4 Trip Center structure smoke PASS | Recorded Product Owner confirmation that Trip Center exposes neither a standalone Links destination nor a standalone Locations destination. Accepted step 8A without reopening the already-closed `V5-F19` or `V5-F22`; advanced the exact resume point to step 8B, opening one URL from its existing meaningful owning item and verifying correct navigation, return state and singular ownership. |
| V81 | 2026-08-11 | Alpha 0.5.4 item-owned URL smoke PASS | Recorded Product Owner confirmation that an existing meaningful item's URL opened the correct website, returning to the application restored the same item, and no duplicate item or standalone Link record was created. Accepted step 8B without reopening `V5-F19` or `V5-F24`; advanced the exact resume point to step 8C, opening one item-owned map/navigation action and verifying the correct destination, return state and singular ownership. |
| V82 | 2026-08-11 | Alpha 0.5.4 item-owned location smoke PASS | Recorded Product Owner confirmation that an existing meaningful item's map/navigation action opened the correct destination, returning to the application restored the same item, and no duplicate item or standalone Location record was created. Accepted step 8C without reopening `V5-F22`; advanced the exact resume point to step 8D, exercising only Cancel on the out-of-range pre-save warning and verifying that the item remains unchanged. |
| V83 | 2026-08-11 | Alpha 0.5.4 out-of-range Cancel regression detected | Recorded step 8D as FAIL against its Cancel criterion. The warning appeared, but Cancel left the edited out-of-range date visible instead of restoring the original in-range date. Classified this as a regression of the previously closed `V5-F21` contract, pending one close/reopen observation to distinguish unsaved editor state from an incorrectly persisted date. No Proceed or additional save is permitted during characterization. |
| V84 | 2026-08-11 | Alpha 0.5.4 out-of-range Cancel persistence characterized | Recorded diagnostic step 8D.1: after closing the editor without another Save or Proceed action and reopening the same item, the original in-range date was present. Confirmed that Cancel prevents persistence and preserves data integrity, while the open editor retains a stale unsaved out-of-range value until closed. Reclassified the remaining `V5-F21` regression as a non-blocking UX/editor-state defect for the next correction package and advanced the exact resume point to compact regression step 9A. |
| V85 | 2026-08-12 | Alpha 0.5.4 warm-Offline compact regression PASS | Recorded compact-regression step 9A as PASS. A non-date text field was edited and saved on one stable existing item while Offline; after connectivity was restored and the application refreshed, the same single item retained the change with no duplication, data loss or collateral modification. Advanced the exact resume point to Timeline synchronization smoke step 9B. |
| V86 | 2026-08-12 | Alpha 0.5.4 final QA decision | Recorded Timeline synchronization smoke step 9B as PASS: Entire Trip, Week and Day remained synchronized with their active period, selected date and filtered list; no item date changed and no record was duplicated or lost. Completed every planned focused acceptance and compact-regression observation, issued final `NO-GO / acceptance complete` because the supported shared-URL branch of `V5-F17` remains blocking, retained Alpha 0.4.3 in Production and moved the next activity to focused correction planning. |
| V87 | 2026-08-12 | Alpha 0.5.5 implementation | Reconciled final 0.5.4 NO-GO evidence, implemented the approved intake-scope reset, hid new-item URL/image/photo paths, marked PDF experimental with source preservation and no Trip-date default, corrected out-of-range Cancel editor reset and archive placement, passed the complete automated/audit/Production-build gate and prepared focused Product Owner acceptance without reopening accepted Offline, QR or Timeline matrices. |
| V88 | 2026-08-12 | Alpha 0.5.5 installation PASS / online baseline pending | Recorded the Product Owner correction installation as PASS over the recognized 0.5.4 NO-GO handoff: installed version `0.5.5`, source handoff `0.5.4`, branch `version/V5`, all ten automated suites, dependency audit and Vite Production build passed, and local recovery backup `C:\Projects\FamilyTrips\family-trips-backups\v5-0.5.5-correction-20260812-130305` was created. Focused acceptance step 1 remains open only for fixed-QA displayed `Alpha 0.5.5`, successful Google sign-in and existing Trip/data load. |
| V89 | 2026-08-12 | Alpha 0.5.5 Draft deployment PASS / exact-build gate pending | Recorded successful Draft deployment to the fixed QA alias with deploy ID `6a7c463d69ebf06b430da88a` and confirmed that Production was not changed. Netlify executed `npm run build` because the supplied deploy command omitted `--no-build`; the deployed main asset `index-C1PUUAZ0.js` differs from installer-tested `index-C-x8FSBM.js`. Classified this as a deployment-process correction rather than an application failure, paused user-visible acceptance, and set rerunning the complete local check as the exact next action before a no-build redeploy. |
| V90 | 2026-08-12 | Alpha 0.5.5 invalid deployment-time bundle characterized | Recorded screenshots showing that the rebuilt QA bundle displays `Alpha 0.5.5` but Google sign-in fails. Console evidence is explicit: Firebase reports `auth/api-key-not-valid`, Identity Toolkit returns HTTP 400, and Google APIs reports `API_KEY_INVALID`. Confirmed the failing bundle is `index-C1PUUAZ0.js`, the deployment-time asset already known to differ from the installer-tested asset. Classified the result as invalid deployment artifact/environment evidence rather than a product defect, ignored the separate Tracking Prevention notice as non-causal browser noise, and retained `npm run check` as the only next action. |
| V91 | 2026-08-12 | Alpha 0.5.5 exact-build recovery PASS | Recorded the Product Owner's complete `npm run check` result: syntax checks, all ten ordered automated suites and Vite 7.3.6 Production build passed. The regenerated local `dist` contains the expected `index-C-x8FSBM.js` asset at 413.49 kB. Re-established the exact-build gate and advanced the sole next action to a fixed-QA Draft redeploy of this untouched artifact using `netlify deploy --dir dist --alias qa --no-build`; no Production deployment is authorized. |
| V92 | 2026-08-12 | Alpha 0.5.5 desktop-and-phone online baseline PASS | Recorded Product Owner confirmation that fixed QA displays `Alpha 0.5.5`, Google sign-in succeeds and the existing Trip/data loads on both desktop and phone. Closed the functional online baseline without opening a product authentication finding. Retained the missing `--no-build` deploy output as an exact-artifact evidence gap for the final gate and advanced focused acceptance to step 2, the Add/source-menu boundary. |
| V93 | 2026-08-12 | Alpha 0.5.5 Add/source-menu boundary PASS | Recorded Product Owner phone screenshot evidence that Add shows exactly Manual, Experimental PDF, JSON and QR, with no URL-intake, image-upload or camera-photo source choice. Confirmed that the optional URL field remains available inside the Manual owning-item form, preserving item-owned links while removing URL as an independent intake source. Advanced focused acceptance to step 3, Manual item-owned URL. |
| V94 | 2026-08-12 | Alpha 0.5.5 Manual item-owned URL save/display partial PASS | Recorded Product Owner phone screenshot evidence that one manually created Activity titled `בדיקת קישור ידני 0.5.5` saved successfully and displays a `קישור` action inside the owning item. No separate Link item is shown in the supplied evidence. Kept focused acceptance step 3 open pending correct destination opening, return-state verification and cleanup of the disposable test item. |
| V95 | 2026-08-12 | Alpha 0.5.5 Manual item-owned URL destination-opening PASS | Recorded Product Owner confirmation that the item-owned `קישור` action opened the intended EL AL website. Kept step 3 open only for return-state singularity and deletion of the disposable Activity. |
| V96 | 2026-08-12 | Alpha 0.5.5 Manual item-owned URL cleanup PASS | Recorded Product Owner confirmation that the same single Activity remained available after returning to FamilyTrips and that deleting it removed it successfully. Closed focused acceptance step 3 with no standalone Link or residual test item. Generated and independently QR-decoded a new single-part `FTI1` fixture titled `בדיקת קישור מתוך QR 0.5.5`, containing item-owned website `https://www.elal.com`, and advanced the exact next action to step 4 proposal observation without approval. |
| V97 | 2026-08-12 | Alpha 0.5.5 validated-QR proposal partial PASS | Recorded Product Owner confirmation that scanning the controlled QR created a result dated 19 January with an EL AL link. The date matches the fixture's 19 January 2027 start and the destination matches `https://www.elal.com`. Kept step 4 open pending confirmation of the exact title, exactly one review-stage proposal and no saved final item before approval. |
| V98 | 2026-08-12 | Alpha 0.5.5 validated-QR identity discrepancy checkpoint | Recorded that exactly one visible result was created, satisfying the singularity check. The reported title `בדיקות קישור מתוך QR 0.5.5` differs from the independently decoded fixture title `בדיקת קישור מתוך QR 0.5.5`. Kept the observation unclassified pending screenshot evidence of the exact displayed title and whether the result is still a review-stage proposal or was auto-saved as a final item. |
| V99 | 2026-08-12 | Alpha 0.5.5 validated-QR review-stage PASS | Recorded three Product Owner phone screenshots proving that the controlled QR produced exactly one review-stage proposal rather than an auto-saved final item. The visible title prefix is singular `בדיקת קישור מתוך`, matching the fixture; the earlier plural report was a reporting typo. The date, item-owned EL AL URL, source identity and review actions are present. Closed the observation branch of step 4 as PASS and advanced the sole next action to rejecting the disposable proposal; the rejected record may be reused for archive-placement step 7. |
| V100 | 2026-08-12 | Alpha 0.5.5 QR rejection and archive-order PASS | Recorded Product Owner confirmation that rejecting the controlled QR proposal moved the proposal to the rejected-proposal archive. Classified continued display of `qa-v5-0-5-5-item-owned-url.json` under Sources as expected provenance retention, not a defect: the source is a separate record and rejection changes only proposal lifecycle state. Screenshot evidence also confirms that Sources and Add are rendered before the archive, satisfying the immediate archive-placement criterion. Advanced the sole next action to refresh/reopen persistence verification. |
| V101 | 2026-08-12 | Alpha 0.5.5 QR rejection persistence PASS | Recorded Product Owner confirmation that after one refresh/reopen the rejected QR proposal remained only in the archive, did not return to active review, and its separate provenance source remained exactly once under Sources. Closed focused acceptance step 4 and advanced to Experimental PDF safety. |
| V102 | 2026-08-12 | Alpha 0.5.5 Experimental PDF initial safety partial PASS | Recorded two Product Owner phone screenshots of `Confirmation_for_Booking_ Ban Ta Khun.pdf`. The application shows both the explicit experimental-review warning and the source filename, keeps the result on the `בדיקת הצעה` review page, and leaves the unidentified date empty with `לא זוהה תאריך` instead of silently defaulting it to the Trip. Recorded materially inaccurate extraction—`for Ban Ta Khun`, `CONFIRMATION`, and a corrupted location string—as Alpha 0.6 redesign evidence rather than a new 0.5.5 finding. Kept step 5 open only for proving that the preserved original PDF can be opened before safely rejecting the disposable proposal. |
| V103 | 2026-08-12 | Alpha 0.5.5 Experimental PDF safety PASS / acceptance correction | Recorded the Product Owner clarification that the experimental path is accepted when it displays the warning, preserves and identifies the source, creates only a review-stage proposal, keeps missing values empty and allows partial extraction. Corrected the executable acceptance wording to match the frozen implementation scope: in-app opening of the original PDF was not required for 0.5.5 and its absence is not a defect or release blocker. No `V5-F26` is opened. Extraction inaccuracies remain Alpha 0.6 redesign evidence. Advanced the sole next action to safe rejection of the disposable PDF proposal. |
| V104 | 2026-08-12 | Alpha 0.5.5 Experimental PDF safe rejection PASS | Recorded Product Owner confirmation that rejecting the disposable PDF proposal removed it from the active area, placed it exactly once in the rejected-proposal archive and retained its source exactly once under Sources for provenance. Closed focused acceptance step 5 in full and advanced the sole next action to focused acceptance step 6, the immediate editor-state reset after cancelling an out-of-range save warning. |
| V105 | 2026-08-12 | Alpha 0.5.5 out-of-range Cancel immediate reset PASS | Recorded Product Owner confirmation that after entering an out-of-range date, tapping Save and cancelling the warning, the still-open editor immediately displayed the original persisted date. Confirmed the 0.5.5 correction of the stale editor-state regression observed in 0.5.4. Kept step 6 open only for closing/reopening the same item and confirming one unchanged record with the original date. |
| V106 | 2026-08-12 | Alpha 0.5.5 out-of-range Cancel persistence PASS | Recorded Product Owner confirmation that after closing the editor without saving and reopening the same item, the original persisted date remained and the item appeared exactly once. Closed the complete Cancel branch with immediate reset, persistence and singularity all PASS. Advanced the sole next action to the controlled Proceed/save branch on the same item; restoration to the original date remains required afterward. |
| V107 | 2026-08-12 | Alpha 0.5.5 out-of-range Proceed immediate PASS | Recorded Product Owner confirmation that changing the same stable item to one day after the Trip end, saving and confirming the warning produced exactly one saved item with the out-of-range date and marker. Kept the Proceed branch open for its required close/reopen persistence and singularity check. The item must not be restored, edited or deleted before that observation. |
| V108 | 2026-08-12 | Alpha 0.5.5 end-boundary marker defect detected | Recorded Product Owner observation that an item whose start date is inside the Trip range but whose end date is after the Trip end is not marked `מחוץ לטווח`. Opened `V5-F27` as a separate range-evaluation defect rather than reopening `V5-F21`. Source inspection is consistent with the observation because the current out-of-range predicate evaluates `startAt` but not the effective `endAt`. Paused acceptance for one close/reopen persistence check before cleanup or final blocker disposition. |
| V109 | 2026-08-12 | Alpha 0.5.5 `V5-F27` persistence confirmation | Recorded Product Owner confirmation that after closing and reopening FamilyTrips the same range item still had no `מחוץ לטווח` marker. Classified `V5-F27` as a confirmed release-blocking FAIL rather than a transient rendering issue. Kept it open until correction and focused Product Owner retest, and advanced the sole next action to restoration of the test item to its original in-range dates. |
| V110 | 2026-08-12 | Alpha 0.5.5 range-item restoration PASS | Recorded Product Owner confirmation that the controlled range item was restored to its original in-range dates, remains exactly one item and no longer shows `מחוץ לטווח`, as expected. Completed test-data cleanup without closing `V5-F27`. Advanced the sole next evidence action to capture the output of the corrective exact-artifact QA deployment using `netlify deploy --dir dist --alias qa --no-build`. |
| V111 | 2026-08-12 | Alpha 0.5.5 final NO-GO and redundant-deploy correction | Clarified that functional testing had already been completed on fixed QA and that the unsupplied `--no-build` command output was only an exact-artifact evidence gap. With `V5-F27` already confirmed as release-blocking, withdrew the request to redeploy the known-failing 0.5.5 artifact solely to close that evidence gap. Stopped 0.5.5 testing, retained Alpha 0.4.3 in Production and moved the exact-artifact deployment requirement to the corrected 0.5.6 candidate. |
| V112 | 2026-08-12 | Alpha 0.5.6 focused correction implementation | Implemented one shared start/end Trip-boundary predicate for pre-save confirmation and persisted out-of-range marking, added a focused boundary suite, advanced application/package/cache identity to 0.5.6, preserved every passed 0.5.5 result and defined the minimal Product Owner retest plus exact `--no-build` fixed-QA deployment gate. |

## Session checkpoint — 2026-08-03

| Check | Result | Evidence / disposition |
|---|---|---|
| Corrected R2 installation and local build | PASS | Installation/build completed successfully from the local 0.4.4 NO-GO handoff; 0.4.3 remains the accepted Production baseline. |
| Draft load | PASS | The deployed application loaded successfully. |
| Google sign-in | PASS after environment authorization | The initial `auth/api-key-not-valid` symptom was traced to Draft-domain authorization, not to a missing local variable. After the domain was authorized in Firebase Authentication and the API-key website restrictions, sign-in succeeded. Close as `V5-F04` setup issue. |
| Supported access modes | CONFIRMED | The application can be run on both a computer and a phone/PWA. Device-specific actions and offline behavior remain subject to their numbered tests. |
| Version identity | FAIL — `V5-F05` | On 2026-08-04, structured desktop test 1 confirmed that the login screen still displays `V4`. Correct the hard-coded label to the V5/Alpha 0.5 identity and repeat test 1 before release. |
| Post-login console | NON-BLOCKING — `V5-W01` | Google popup sign-in succeeds, but Chrome reports COOP warnings around popup close checks. Add the `same-origin-allow-popups` Netlify header and verify before release. |
| Repeatable QA deployment | ADOPTED | Use the fixed `qa--familytripz.netlify.app` alias; build locally with the approved Firebase environment, deploy `dist` with `--no-build`, run smoke checks, and require explicit approval before Production. |

The Product Owner stopped testing for the day after this checkpoint. No numbered application-function test beyond the setup observations above was closed by that record, and no GO/NO-GO decision was issued. Structured testing resumed on 2026-08-04; test 1 is now recorded as FAIL because of `V5-F05`.

## Phone exploratory checkpoint — 2026-08-04

| Finding | Observed result | Required focused retest / disposition |
|---|---|---|
| `V5-F06` — mandatory-field marking | Required fields were not visibly marked before save. On save attempt, a note appeared beside the missing field and save was blocked. | On the phone, open each affected creation form and verify that every mandatory field is marked before save; leave one empty and verify save remains blocked with a field-level explanation. Alpha 0.5 correction required before GO. |
| `V5-F07` — Today unreachable after Flight/Insurance | Creating a Flight or Insurance item prevented navigation to Today. Deleting the item released the condition. Desktop evidence on 2026-08-04 reconfirmed the Insurance branch: Timeline remained visible after Insurance creation, but Today could not be reached. Source inspection confirmed the exact cause in Today quick-access construction: `[currentHotel, nextTransport, ...critical]` may contain `undefined`, while duplicate removal calls `findIndex(u => u.id === r.id)` before removing absent entries. A valid Flight (`nextTransport`) or Insurance (`critical`) therefore causes the lookup to visit an earlier undefined candidate and throw. | Correct the quick-access pipeline by removing null/undefined candidates before any `.id` comparison, then deduplicate the remaining records safely. Create and save one valid Flight and one valid Insurance item separately and together; return to Today after each save, refresh/restart, and verify Today renders without deleting either item. Functional release blocker for Alpha 0.5. |
| `V5-F08` — incomplete Timeline Calendar/list interaction | Calendar appeared as an independent quick-access button, and the complete agreed period behavior is not represented. | Verify Timeline is the single destination and Entire Trip, Week and Day each render a Calendar above the event list. Entire Trip lists all Trip events; Week lists only the active week's events; Day lists only the selected day's events. In Week, previous/next arrows and swipe/scroll must move and visibly mark the active week and update the list to the same week. Any additional duration-enabled period must follow the same contract. Alpha 0.5 correction is required before GO. |
| `V5-F09` — category-prefilled Add | Add from a category did not preset the new item's type from the current category. | Assigned to Alpha 0.6. From every category, verify Add opens the shared creation flow with that category type preselected and still allows a valid type change. This does not close any current V5 numbered test. |
| `V5-F10` — start/end-date invariant | The expected automatic end-date behavior was clarified during phone testing. | Case A: set end before a newly selected start and verify end becomes equal to start. Case B: set end equal to/after the new start and verify end is unchanged. Alpha 0.5 correction/verification required before GO. |

Deleting a Flight or Insurance item is permitted only as a temporary way to continue unrelated tests. It is not a fix, PASS result, accepted limitation, or severity reduction for `V5-F07`.

## Structured desktop resumption — 2026-08-04

| Finding / test | Observed result | Required focused retest / disposition |
|---|---|---|
| Test 1 / `V5-F05` — version identity | The login screen still displays `V4`, not Alpha 0.5. | Test 1 is FAIL. After correction and QA redeployment, perform a hard refresh and verify the login screen shows the exact approved Alpha 0.5 identity with no V4 label. Release-blocking identity defect. |
| `V5-F11` — Settings delete/reset controls missing | The previously available Settings option to delete current Trip data or Trips is no longer present. | Restore clearly separated deletion scopes in Settings, with explicit target wording and destructive-action confirmation. Verify the user can delete the intended current Trip data or selected Trip without affecting unrelated Trips. Functional regression and Alpha 0.5 release blocker. |
| `V5-F12` — blank screen after sign-out/sign-in | After signing out and then signing in again, the authenticated account bar renders while the main `#app` area remains blank; `Ctrl+Shift+R` does not recover it. Source inspection confirms that authentication itself has completed and the returning Today render enters the same unsafe quick-access duplicate-removal expression as `V5-F07`. With a saved Flight/Insurance candidate and an absent hotel or transport candidate, `findIndex` reads `.id` from `undefined` and aborts main-content rendering. | Keep this as a distinct re-entry regression scenario but implement one shared code correction with `V5-F07`: remove null/undefined quick-access candidates before `.id` comparison and deduplicate only valid records. Then sign in, open the saved Trip, sign out, and sign in again on desktop and installed phone PWA; verify Today renders without refresh/data deletion/cache clearing and no uncaught error occurs. Release blocker for Alpha 0.5. |

A refresh was used only to characterize `V5-F12` and the screen remained blank. Source inspection now establishes the failing collection and expression: Today quick-access candidates are deduplicated before absent candidates are removed. The required implementation order is equivalent to `candidates.filter(Boolean)` followed by null-safe ID deduplication; the correction must not alter valid item ordering or remove distinct records. Do not clear site data or delete Trips merely to continue this check.

The `chrome-extension://` load failures, host-validation messages and asynchronous-listener response shown in the same Console capture are treated as browser-extension noise unless later evidence connects them to the application. They are not assigned as FamilyTrips defects. Focused diagnosis is complete; no further Product Owner diagnostic action is required on the broken build. Test 2 remains not started until the shared `V5-F07`/`V5-F12` correction is deployed and the application is usable. The earlier phone findings remain in the register and do not need to be rediscovered.

## Alpha 0.5.1 correction resume gate

- Use only the fixed QA address `https://qa--familytripz.netlify.app` after deployment evidence confirms Alpha 0.5.1.
- Keep `V5-F05`, `V5-F06`, `V5-F07`, `V5-F08`, `V5-F10`, `V5-F11` and `V5-F12` open until their focused Product Owner retests pass.
- Keep `V5-F09` assigned to Alpha 0.6; do not test or close it as part of 0.5.1.
- Resume with test 1. After test 1, run the blocking focused regressions below before continuing to test 2.
- Present one test action at a time and record evidence immediately in the application-testing chat.

## Alpha 0.5.1 QA retest checkpoint — 2026-08-04

| Check / finding | Result | Evidence / disposition |
|---|---|---|
| Installation, automated correction tests and production build | PASS | Installer reported all V5 extraction/merge/date-default, external import/QR, operational data, Alpha 0.5.1 correction and offline-shell tests passed. Vite production build completed successfully. Final evidence: `INSTALLATION PASSED`, installed version `0.5.1`, branch `version/V5`; backup created at `C:\Projects\FamilyTrips\family-trips-backups\v5-0.5.1-correction-20260804-224849`. |
| Fixed QA deployment | PASS | The already-tested local `dist` was deployed with `--alias=qa --no-build` to `https://qa--familytripz.netlify.app`; deploy ID `6a724497f1c081ae894f7d53`. No Production deployment was performed. |
| Test 1 / `V5-F05` — version identity | PASS in focused retest | A hard-refreshed private window displayed `Alpha 0.5.1` on the login screen with no V4 label. Keep final closure synchronized with the end-of-cycle finding review. |
| Google sign-in and main-shell load | PASS | Google sign-in completed and the main application screen opened normally. This does not yet constitute the dedicated V5-F12 sign-out/sign-in re-entry regression. |
| `V5-F11` — Settings deletion scopes | PASS in focused retest | Settings displayed separate `Delete Trip Data` and `Full Trip Reset` actions. Both confirmation dialogs identified the current Trip and destructive scope; Cancel preserved state. Confirmed deletion of Trip data preserved the Trip and its dates. Confirmed full reset removed the Trip and returned the application to the empty/create-Trip state. |
| `V5-F11` wording caveat | OPEN wording follow-up | The full-reset dialog says that other Trips will not be affected, although Alpha 0.5.1 still supports only one Trip. The deletion behavior passed, but the wording anticipates a capability not yet available. Reword now or track with multi-Trip support; do not treat this caveat as a V5-F11 functional failure. |
| Create a new Trip after full reset | PASS | A new Trip was created successfully and opened on the main screen. |
| `V5-F07` — Flight branch / Today availability | PARTIAL PASS | A valid Flight was saved and Today remained reachable and rendered; the original blank/unreachable Today symptom did not reproduce on this branch. Insurance-only, Flight+Insurance, restart and ordering/deduplication branches remain untested, so V5-F07 is not finally closed. |
| `V5-F13` — preparation-stage Today Quick Access relevance and duplicate presentation | OPEN — new Product Owner finding | The single saved Flight appears once in the Today section and again in `Quick Access` on the same home screen. Quick Access contains only that Flight. During the organization/preparation phase, the Product Owner expects Quick Access to prioritize actionable packing and preparation tasks rather than repeat the same Flight card. Define phase-aware Quick Access selection and prevent confusing same-screen repetition, while preserving valid Today information and the null-safe correction. |
| `V5-F13` — future task visibility / capacity refinement | FAIL — Product Owner retest | A future open task was absent from Quick Access and appeared only after its due date was changed to today. Correct Today to show at most three open preparation tasks, including future tasks, ordered overdue, today, nearest due date, then undated. Exclude completed tasks and show `All tasks` when more than three exist. |
| `V5-F14` — saved task cannot be edited | FAIL — new functional finding | Open an existing task, edit its title and due date, save, reopen and verify the changes persisted on the same record without duplication. Repeat completion/uncompletion and ensure list/Quick Access update. This is an Alpha 0.5.1 blocker. |
| Task list due date, priority and filters | DEFERRED — Alpha 0.6 | Add `High / Normal / Low` priority with `Normal` default. Verify every task row shows due date and priority. Verify priority filters, due-date scopes/range, combined filtering, clear/reset, empty results and deterministic ordering. |

The Product Owner ended testing for the day at this point. No GO/NO-GO decision was issued, no Production deployment is authorized, and Production remains on version 0.4.3.

### Exact resume point for the next session

1. Continue from the saved new Trip and Flight; do not count the same Flight's appearance in Today and Quick Access as two stored records unless data inspection proves duplication.
2. Correct and retest `V5-F13`: Quick Access shows at most three open tasks, including future tasks, and does not confusingly duplicate the Flight already shown above.
3. Correct and retest `V5-F14`: edit and save an existing task, reopen it and verify persistence without duplication.
4. Complete `V5-F07` with Insurance alone, Flight and Insurance together, refresh/restart, ordering and deduplication checks.
5. Complete the dedicated `V5-F12` sign-out/sign-in cycle on desktop and phone/PWA.
6. Continue the remaining focused regressions before acceptance test 2 onward.

## Consolidated Alpha 0.5.1 QA checkpoint — 2026-08-06

This checkpoint supersedes the earlier `Exact resume point` wherever later Product Owner evidence exists. A scenario is marked complete only when an observed result was recorded in the testing chat; its presence in the original plan alone is not evidence of execution.

### Completed and accepted in the current candidate

| Area | Result / evidence |
|---|---|
| Installation and identity | PASS — corrected installation/build completed; login screen shows `Alpha 0.5.1`; Google sign-in and application load succeed. |
| Settings deletion | PASS functionally — `Delete Trip Data` and `Full Trip Reset` were exercised, confirmation was shown, cancellation preserved data, Trip-data deletion preserved the Trip/dates, and full reset returned to the create-Trip state. The full-reset wording remains a separate correction. |
| QR generation and validation | PASS — approved single `FTI1` and multipart `FTI1C` artifacts were generated and validated, including sidecars, independent decode/reconstruction, Hebrew/English/special characters, multi-file relationships and controlled failure behavior. |
| QR ingestion | PASS — single QR, gallery import after the camera path, multipart parts out of order, repeated part, cancellation of an incomplete series and checksum rejection were exercised. No partial item was created on cancellation or checksum failure. |
| Imported proposal lifecycle | PASS — approval/save, persistence after refresh/reopen, type change, ordinary field edits and confirmed deletion were exercised. |
| Duplicate/merge behavior | PASS in the executed import sequence — duplicate import/merge did not create an additional stored item; retain one smoke check in 0.5.2 rather than repeating the full matrix. |
| Item lifecycle | PASS — create, edit title/address/notes, move between categories by type change, delete after confirmation and persistence of deletion after refresh. |
| Dated/undated items | PASS — undated category item persists; adding a date places it in the correct day without duplication; removing the date removes it from the daily route but keeps it in its category. |
| Ordering and out-of-range date | PASS/usable — chronological order updates after time editing; an out-of-Trip date produces a warning and is saved only after the user proceeds. |
| External link navigation | PASS for destination and return state — the link opens the correct target and returning restores the same item/scroll position. |
| Warm offline use and recovery | PASS — while the application is already open, saved data remains available, edits persist, reconnection synchronizes automatically and no duplicate is created. |

### Verified open findings and target release

| Finding | Current evidence | Target |
|---|---|---|
| Cold-start offline failure | After a successful online load, fully closing and reopening the PWA without connectivity shows a connection error instead of opening the last saved Trip. Repeatable and release-blocking. | `0.5.2` |
| `V5-F08` — Timeline/Calendar contract | Items appear on the correct dates, but the view opens on the current month rather than the Trip period, Calendar remains a separate Quick Access destination, and the agreed unified Entire Trip / Week / Day Calendar-plus-filtered-list behavior and synchronized selection/navigation are not fully implemented. | `0.5.2` |
| `V5-F13` — Quick Access tasks | Controlled retest showed overdue, today and undated tasks, while the future open task was omitted. After one displayed task was completed, it disappeared correctly but the future task still did not fill the vacant slot. Quick Access must show up to three open tasks including future tasks, ordered overdue, today, nearest future date, then undated; exclude completed tasks and offer `All tasks` when more exist. Avoid confusing repetition of the same Flight already presented elsewhere on Today. | `0.5.2` |
| `V5-F14` — task editing | An existing saved task cannot be edited. Editing title/due date and completion state must persist on the same record without duplication. | `0.5.2` |
| Import proposal overflows phone width | The proposed-item card exceeds the viewport, especially with long addresses/URLs. | `0.5.2` |
| URL displayed as a document | A `https://share.google/...` value is stored and opens correctly, but is rendered with the document/file presentation instead of Link. | `0.5.2` |
| Meaningless title extracted from URL | The importer creates a non-meaningful title from the URL instead of a reliable page title or safe `Shared link` fallback. | `0.5.2` |
| Generic URL classified as Flight | A generic shared link was automatically classified as Flight without reliable evidence. Use an unidentified state or require type selection when confidence is insufficient. | `0.5.2` |
| Existing URL cannot be edited | The stored URL cannot be replaced or removed from the item editor, although the destination itself works. | `0.5.2` |
| `V5-F11` wording caveat | Full-reset wording promises that other Trips are unaffected although this version has no multi-Trip capability. Correct the text without changing the already-passed deletion behavior. | `0.5.2` |
| `V5-F09` — category-prefilled Add | Previously assigned to Alpha 0.6; no change to disposition. | `0.6` |
| Task priority, visible due date and filters | New task-management capability, not a repair of current 0.5 behavior. | `0.6` |
| Multi-Trip and multi-user/two-device collaboration | New data, permissions and synchronization capability. | `0.6` |
| Trip participants in Settings | Product/model expansion. | `0.6` planning |
| Multiple destination countries per Trip | Product/data-model expansion; country must be a collection, not a single Trip field. | `0.6` planning |

### Focused findings closed during the pre-planning gate

| Finding | Final result / evidence |
|---|---|
| `V5-F06` — mandatory fields | PASS / usable on phone. Required-field validation blocks an invalid save and identifies the missing field. No further pre-planning retest is required. |
| `V5-F07` — Flight/Insurance Today rendering | PASS. Flight alone, Insurance alone, and Flight plus Insurance together render correctly; each relevant card appears once and opens the correct record. The original blank/unreachable Today defect is closed. |
| `V5-F10` — Trip-date behavior | PASS. Date changes persist, items outside the Trip range are correctly marked as out of range, and no screen was found displaying configured Trip dates as `Not defined`. The earlier undefined-date finding was incorrect and is withdrawn. |
| `V5-F12` — sign-out/sign-in re-entry | PASS on desktop and installed phone PWA. Sign-out hides Trip data and returns to sign-in; signing in again restores the Trip and items without loss or duplication. |

### Pre-planning gate result

The gate is complete. No further Product Owner testing is required before the planning chat scopes and prepares Alpha 0.5.2.

- `V5-F06`, `V5-F07`, `V5-F10` and `V5-F12` passed and are not part of the correction scope.
- `V5-F08`, `V5-F13`, `V5-F14`, URL editing and cold-start offline are sufficiently characterized for implementation planning.
- The complete QR matrix, ordinary item CRUD, warm-offline synchronization, chronological ordering, out-of-range warning and correct link destination are already complete and must not be repeated during planning.

### Approved Alpha 0.5.2 correction scope and acceptance criteria

1. **Cold-start offline:** after one completed online load, fully close the installed phone PWA, disable connectivity and reopen it. The application shell, last Trip and saved local data must open without a connection-error dead end. Restore connectivity and verify automatic recovery/synchronization without duplication.
2. **`V5-F08` Timeline/Calendar:** Timeline is the single destination; it initially focuses the Trip period rather than the current month. Entire Trip, Week and Day show Calendar above the list, filter the list to the active period, and synchronize arrows/swipe/date selection, visible selection and list contents without modifying item dates. Remove the redundant standalone Calendar Quick Access entry.
3. **`V5-F13` Quick Access:** show up to three open tasks, including future tasks, ordered overdue → today → nearest future date → undated. Exclude completed tasks; when one is completed, the next eligible future task fills the slot. Show `All tasks` when more eligible tasks exist. Avoid confusing same-screen repetition of a Flight already presented in Today.
4. **`V5-F14` task editing:** an existing task can be opened and edited. Title and due date changes persist on the same record after reopen/restart, and completion/uncompletion updates both the task list and Quick Access without creating a duplicate.
5. **`V5-F15` — existing URL editing:** the item editor allows replacing and removing a stored URL; save updates the same item, the new link opens correctly, removal leaves no stale link action, and neither action creates a duplicate.
6. **`V5-F16` — import proposal phone layout:** long addresses and URLs wrap or truncate safely inside the viewport; no horizontal overflow or inaccessible action is permitted.
7. **`V5-F17` — imported shared-link presentation/title and `V5-F18` — safe classification:** a generic `https://share.google/...` URL is presented as a Link, not a document/file; use a reliable page title when available and otherwise a safe `Shared link` fallback. Do not classify a generic link as Flight without reliable evidence; use unidentified/type-selection behavior when confidence is insufficient.
8. **`V5-F11` wording:** remove the unsupported promise that other Trips are unaffected while multi-Trip is unavailable. Preserve the already-passed deletion scopes, confirmations and behavior.
9. **`V5-W01` — Google popup COOP warning:** serve `Cross-Origin-Opener-Policy: same-origin-allow-popups` from Netlify application routes. On the fixed QA deployment, verify the response header, complete Google popup sign-in, and confirm that the previously recorded popup-close COOP warning no longer appears in the Console.

### Alpha 0.5.2 implementation evidence

| Repair | Automated evidence | Installed acceptance state |
|---|---|---|
| Cold-start offline | 0.5.2 cache identity, scope/index fallback, ignore-search navigation matching and offline local-session path pass. | Open pending installed phone/PWA cold start and reconnect. |
| `V5-F08` | Timeline entry focuses Entire Trip/Trip start; embedded Calendar, shared bounds, Day selection, Week arrows/swipe and absence of standalone Calendar remain asserted. | Open pending full interaction retest. |
| `V5-F13` | Task selector includes future tasks, excludes completed tasks, orders overdue/today/future/undated, limits to three and exposes `All tasks`; duplicate Today records are filtered out. | Open pending Product Owner retest. |
| `V5-F14` | Edit action and stable-ID update path are asserted; new-task creation remains separate. | Open pending create/edit/complete/uncomplete/restart retest. |
| Existing URL editing | Item editor always exposes URL, saves replacement/empty value on the same item and renders a URL action without requiring location. | Open pending replace/remove/open/restart retest. |
| Proposal phone layout | Modal horizontal overflow is suppressed and long proposal/source/warning/address/URL content wraps within the viewport. | Open pending target-phone layout retest. |
| Shared link | `share.google` fixture remains Link, retains URL, uses `Shared link` fallback or supplied page title and is not classified as Flight from an opaque token. | Open pending real-link retest. |
| `V5-F11` wording | Unsupported other-Trips promise is absent; destructive handlers and reset helper remain present. | Installed PASS: both current Trip-data and Full Trip Reset confirmations identified their scopes without unsupported claims, and Cancel preserved all data unchanged. |
| `V5-W01` | `netlify.toml` contains `Cross-Origin-Opener-Policy = "same-origin-allow-popups"` for `/*`, and the correction suite asserts the exact value. | Open pending fixed-QA response-header, Google sign-in and Console verification. |

Complete automated gate: syntax checks, V4 regressions, V5 ingestion/QR/operational/correction/offline tests and Vite Production build all passed on 2026-08-06. Automated PASS does not close an installed-device finding.

QA-chat reconciliation found no missing functional defect beyond the original eight repairs. It preserved the Product Owner correction that out-of-range marking after Trip-date changes is correct and kept the future-task, task-edit and URL-edit gaps separate. The Product Owner then explicitly assigned the earlier non-blocking `V5-W01` Google-popup COOP warning to Alpha 0.5.2, bringing the approved correction scope to nine.

Any implementation proposal outside these nine corrections requires an explicit version decision; the Alpha 0.6 items below must not be silently added to 0.5.2.

### Alpha 0.5.2 post-fix regression scope

After the corrected package is installed, retest every `0.5.2` finding against its acceptance criterion, then run this compact regression:

- one valid single QR and one multipart QR as smoke tests only;
- approve one imported proposal, edit its type/fields/URL, refresh and delete it;
- one warm-offline edit/reconnect cycle and one cold-start offline cycle;
- one Flight plus Insurance Today/re-entry cycle;
- one preparation-task create/edit/complete/uncomplete cycle with Quick Access ordering;
- one complete Timeline Entire Trip / Week / Day navigation and date-selection pass;
- one Trip-date/out-of-range smoke check to protect the already-passed `V5-F10` behavior;
- Settings delete-cancel smoke check, preserving the already-validated deletion scopes;
- one fixed-QA Google popup sign-in with response-header and Console verification for `V5-W01`.

Do not run the deferred Alpha 0.6 priority/filter, category-prefilled Add, multi-Trip, multi-user, participant or multiple-country tests against 0.5.2.

## Alpha 0.5.2 executable acceptance sequence

Prerequisite: install `FAMILY_TRIPS_V5_0_5_2_CORRECTION.zip`, confirm `INSTALLATION PASSED`, branch `version/V5` and displayed version `Alpha 0.5.2`. Use the fixed QA URL only after deployment evidence for the same tested build.

1. Complete one online load, close the installed phone PWA, disable connectivity and cold-start it. Verify shell, last Trip and saved data; reconnect and verify automatic recovery without duplication.
2. Enter Timeline and verify initial Entire Trip/Trip-period focus. Exercise Entire Trip, Week and Day, including arrows, Week swipe, date selection, visible active period and matching list contents without changing item dates.
3. Create open tasks for overdue, today, two future dates and undated. Verify Quick Access shows exactly the first three by the required order and offers `All tasks`; complete one and verify the next future task fills the vacancy. Confirm no Flight already shown in Today repeats there.
4. Open an existing task, change title and due date, save, reopen and restart. Complete and uncomplete the same task and verify list/Quick Access update with one stable record.
5. Open an existing item with a URL, replace it and verify the new destination. Remove it, save/reopen and verify no stale link action or duplicate item remains.
5A. Verify `V5-F19`: Trip Center does not expose a standalone Links section, list, card, tab or Quick Access destination. A URL is visible and actionable only in the context of its owning item. The application may maintain an internal link index for import, lookup or data integrity, but that index is not rendered as a separate user-facing collection.
5B. Verify `V5-F22`: Trip Center does not expose a standalone Locations section, list, card, tab or Quick Access destination. Address, coordinates, source, country/destination association and map/navigation actions remain available only through the owning hotel, attraction, restaurant, shopping, transport or other item. An aggregate Trip map or background geospatial index may project those item-owned records, but must not create a separate user-facing Locations collection.
6. Import a proposal containing a long address and URL on the target phone. Verify every field and action remains inside the viewport with no horizontal scrolling.
7. Paste the controlled generic `https://share.google/...` link. Verify Link presentation, meaningful page title when available or `Shared link` fallback, and no unsupported Flight classification; select a type explicitly if confidence is insufficient.
8. Open both Settings destructive confirmations and cancel. Verify wording names the current target/scope and makes no unsupported claim about other Trips; existing data remains intact.
9. On the fixed QA deployment, verify an application response contains `Cross-Origin-Opener-Policy: same-origin-allow-popups`; clear the Console, complete Google popup sign-in, and verify the earlier popup-close COOP warning does not recur.
10. Run the compact regression exactly as listed above. Record each result immediately and issue GO / NO-GO only after every finding and regression has an explicit result.

## Alpha 0.5.2 final QA execution result — acceptance completed on 2026-08-10

Testing is complete through acceptance step 10, including every compact-regression cycle. Do not repeat the completed checks below against this candidate.

| Check / finding | Result | Evidence / disposition |
|---|---|---|
| Installer, automated tests and production build | PASS | Installer reported `INSTALLATION PASSED`, installed version `0.5.2`, branch `version/V5`; all automated suites and the Vite production build passed. |
| Fixed QA deployment and version identity | PASS | The tested `dist` was deployed as a draft to `https://qa--familytripz.netlify.app`; the login screen displayed `Alpha 0.5.2`. No Production deployment was performed. |
| Google sign-in and existing Trip load | PASS | Sign-in completed and the existing Trip loaded correctly. |
| Acceptance step 1 — cold-start offline | FAIL — BLOCKING | After a completed online load, fully closing the installed PWA, disabling Wi-Fi and mobile data, and reopening from the home-screen icon did not start the application. The application recovered automatically when connectivity was restored. The cold-start offline correction is therefore not accepted. |
| Acceptance step 2 / `V5-F08` — Timeline/Calendar | PASS | Timeline opened at the Trip date/period; Entire Trip, Week and Day behavior, list filtering and Calendar synchronization were reported correct. |
| Acceptance step 3 / `V5-F13` — Quick Access | PASS | With overdue, today, future and undated open tasks, Quick Access displayed overdue → today → future. After completing the overdue task, it disappeared and the undated task filled the vacant slot. |
| Acceptance step 4 / `V5-F14` — task editing | PASS | Existing task title and due date were edited successfully; the original record was updated without duplication and the change appeared in Quick Access. |
| Acceptance step 5 / `V5-F15` — existing URL editing | PASS | An existing item's URL was replaced successfully and the new link opened. The URL was then removed successfully. Both changes updated the same item without duplication. |
| `V5-F19` — standalone Links area in Trip Center | FAIL — OPEN PRODUCT DEFECT | Alpha 0.5.2 added a separate user-visible Links area to Trip Center. The Product Owner confirmed that this is redundant: every link must belong to and be displayed under a specific item, not as a separate user-facing collection. Remove the standalone Links UI. A background-only index may be retained if technically useful. Retest on the corrected build by confirming that item links still open from their owning items and that no separate Links destination is visible. |
| `V5-F21` — missing out-of-range pre-save warning | FAIL — OPEN PRODUCT DEFECT | In compact regression, an item was changed from an in-range date to one day after the Trip end date and saved immediately. No warning appeared and no explicit proceed confirmation was required. After saving, the same item was clearly marked as outside the Trip range. Returning it to its original in-range date removed the marker, preserved exactly one record and did not change unrelated items or Trip dates. Preserve the working post-save marking/restoration behavior, but restore a clear pre-save warning and explicit confirmation before accepting an out-of-range date. |
| `V5-F22` — standalone Locations area in Trip Center | FAIL — OPEN PRODUCT DEFECT | Alpha 0.5.2 exposes Locations as a separate user-visible Trip Center category. The Product Owner confirmed that location is an attribute of its owning item, not an independent user-facing collection. Remove the standalone Locations UI without deleting or detaching existing addresses, coordinates, sources, country/destination associations or map/navigation actions. An aggregate Trip map and a background geospatial index may remain as projections of item-owned records. Retest on the corrected build by confirming that no separate Locations destination is visible, each item retains its location behavior, and aggregate map/navigation behavior remains correct. |
| Acceptance step 6 / `V5-F16` — import proposal phone layout | PASS | The real shared-link import proposal was usable on the target phone. Its content and actions remained accessible, with no blocking viewport overflow reported. |
| Acceptance step 7 / `V5-F18` — safe shared-link classification | PASS | A real shared URL was handled as a link, remained accessible after save, and could be edited. It was not misclassified as a Flight or document/file. |
| Acceptance step 7 / `V5-F17` — meaningful shared-link title | FAIL — OPEN | The imported proposal used the generic fallback title `Shared link` rather than identifying relevant content such as `BIG גדרה`. Manual title correction allowed the item to be saved and used, but the Product Owner did not accept the automatic title as relevant. Improve reliable title extraction when the destination exposes a usable title; retain the safe fallback only when relevant metadata truly cannot be obtained. |
| Acceptance step 8 / `V5-F11` — destructive-confirmation wording | PASS | The current Trip-data and Full Trip Reset warnings clearly identified their targets and scopes, contained no unsupported promise about other Trips, and Cancel preserved the Trip, dates, tasks and all items unchanged. Neither destructive action was confirmed during the final smoke checks. |
| Acceptance step 9 / `V5-W01` — deployed COOP and Google popup | PARTIAL PASS / WARNING STILL OPEN | `curl.exe -I` returned HTTP 200 and the exact required header `Cross-Origin-Opener-Policy: same-origin-allow-popups`. Google popup sign-in completed successfully and the application returned to the Trip. However, Chrome Console still displayed `Cross-Origin-Opener-Policy policy would block the window.close call` twice. Functional sign-in passed, but the explicit no-warning acceptance criterion failed; keep `V5-W01` open for technical correction or an explicit documented acceptance decision. The unrelated `Host is not in insights whitelist` message is not treated as the primary FamilyTrips finding. |
| Acceptance step 10 — compact regression | COMPLETE — REGRESSION PASS EXCEPT OPEN `V5-F21` | The validated single `FTI1` smoke-test image and multipart `FTI1C` in both orders passed; provisional `V5-F20` is withdrawn. The imported-item approval/edit/refresh/delete lifecycle, warm-offline edit/reconnect, Flight-plus-Insurance Today/re-entry, complete preparation-task lifecycle and Timeline regression passed without duplication, data loss or collateral changes. In the Trip-date/out-of-range smoke check, the item saved one day after the Trip end date and was clearly marked outside the range, but no warning or explicit proceed confirmation appeared before save; `V5-F21` remains open. Returning the item to its original date removed the marker, preserved one record and caused no collateral changes. Both Settings confirmation-and-cancel smoke checks passed and preserved all data. |

Final release status: **NO-GO / acceptance complete**. Cold-start offline remains blocking. `V5-F17` title relevance, `V5-F19` standalone Links UI, `V5-F21` missing out-of-range pre-save warning and `V5-F22` standalone Locations UI require correction and focused retest. `V5-W01` remains open because the Console warning persists despite successful sign-in and the correct response header; it requires technical correction or an explicit documented acceptance decision. All planned executable and compact-regression checks now have explicit results. Do not deploy Alpha 0.5.2 to Production; Alpha 0.4.3 remains the accepted Production version.

### Next project action

Executable QA for Alpha 0.5.2 is complete. Do not run another test against this candidate. Return to the planning/implementation workflow, prepare a corrected build for cold-start offline, `V5-F17`, `V5-F19`, `V5-F21` and `V5-F22`, and resolve `V5-W01` technically or by explicit Product Owner acceptance. The next QA session begins only after a new corrected package and fixed-QA deployment are available, and it must focus on those open items plus the smallest required regression around their changes.

## Rules

ChatGPT presents one test only, waits for the observed result, records it immediately, then continues. For every QR-dependent step, ChatGPT must display the exact QR image (or the ordered image set for multipart QR) in the conversation and state the expected result; the Product Owner is not required to generate the fixture. Findings remain `TBD after acceptance completion` until the final test and short regression are complete.

## Acceptance sequence — baseline

Execution status is governed by the consolidated 2026-08-06 checkpoint above. In particular, tests 7A–14 are complete and must not be treated as remaining work merely because their executable steps are retained here for traceability.

1. Install `FAMILY_TRIPS_V5_0_5_1_CORRECTION.zip` over local 0.5.0, 0.4.3 or the local 0.4.4 NO-GO test handoff; confirm `INSTALLATION PASSED`, branch `version/V5`, and app version Alpha 0.5.1.
2. Set Trip dates after creating one undated eligible item; verify backfill and preservation of a second explicit date.
3. Import the EL AL case containing `YITZHAK` / `LY087`; verify type, route/flight details and empty booking number when no label exists.
4. Import the `SCREENSHOT` flight case and confirm it is not a booking number.
5. Import the Agoda case and confirm booking ID `1736468481` is not a phone number; verify visible structured fields.
6. Import a multi-segment itinerary and confirm separate editable proposals.
7. Import a valid `familytrips.import` JSON batch; confirm proposal-only behavior, sources, confidence and warnings.
7A. Execute the approved QR-generation script/command on the controlled valid JSON fixture. Record the exact script path/name and invocation; verify exit success and that it emits a real scannable QR image plus a same-basename `.txt` payload sidecar. Independently decode the QR and compare the decoded payload byte-for-byte with the sidecar and semantically with the source JSON.
7B. Execute the same generator on a controlled oversize multi-file fixture containing Hebrew, English and special characters. Verify deterministic `part-01-of-N` naming, complete part numbering, one matching `.txt` sidecar per image, no dropped item/source relationship, and independent reconstruction whose Base64URL and SHA-256 checks reproduce the complete source data exactly.
7C. Run the generator with malformed/unsupported input and with an unavailable precise QR dependency. Verify a non-zero/clear failure, no simulated QR, no partial series presented as complete, and no overwrite of the last known-good artifacts.
8. Scan a valid single `FTI1` QR with the camera.
9. Deny camera permission and import the same QR from a gallery image; compare the proposals.
10. Scan a complete `FTI1C` series out of order with one repeated part; verify progress and one proposal only.
11. Cancel an incomplete series and verify that no source, suggestion or item was written.
12. Scan a checksum-failing payload and verify rejection with no partial write.
13. Approve JSON/QR proposal, restart, reopen and compare every supported value.
14. Exercise duplicate merge; verify empty imported fields do not replace non-empty values and all source references remain.

## Deferred Alpha 0.6 task-list acceptance coverage

1. Create tasks covering all priorities, overdue, today, future and no due date; verify `Normal` is the default priority.
2. Verify each task row displays its due date (or explicit `No due date`) and priority without opening the task.
3. Filter separately by each priority and by overdue, today, upcoming, undated and an explicit date range.
4. Combine priority and due-date filters and verify only the intersection is shown; completed-task visibility follows the selected completion filter.
5. Clear all filters and verify the full list returns.
6. Verify default ordering: overdue, today, priority, nearest due date, undated, with deterministic order for ties.
7. Edit a task's priority and due date and verify its visible fields, filter membership and sort position update immediately and persist after restart/sign-in.

## Acceptance sequence continued — Alpha 0.5.1

15. Verify Games favorites and offline instructions; keep games separate from booked activities.
16. Verify packing category collapse, duplicate warning, owner, Packed section and reversible completion.
17. Create/complete a preparation task and shopping item; verify pre-trip Today visibility.
18. Verify Today current hotel/next transport/insurance/contact access and absence of irrelevant old events.
19. Open Timeline and verify the complete `V5-F08` contract: no standalone Calendar quick access; Entire Trip, Week and Day each render a Calendar above the list; the list is filtered to the active period; Week navigation by both previous/next arrows and swipe/scroll changes and visibly marks the active week and updates the list to that same week. If another duration-enabled period is offered, verify the same Calendar/list synchronization.
20. Select a Calendar date, verify Day and the list update to that date, and confirm no source item date changed; also verify both branches of the start/end-date invariant in `V5-F10`.
21. Create, correct and approve a location; open map/pin and confirm failure is never silent.
22. Open a locally stored critical source while offline; verify unavailable external metadata is clearly marked.
23. Install the PWA, close it, disable connectivity, cold-start it and open saved Trip data.
24. Restore connectivity and verify normal recovery.

## Short regression

25. Edit a pending suggestion title, approve and reopen it.
26. Open the correct original source from the item.
27. Delete the item and confirm the original local source remains in Documents.
28. Verify website, navigation and telephone actions on the target phone.

## Blocking focused regressions before GO

- Repeat test 1 and close `V5-F05` only when the login identity is Alpha 0.5.1 with no V4 label.
- Verify the restored Settings deletion scopes and confirmation behavior for `V5-F11` without affecting unrelated Trips.
- Smoke-regress the passed `V5-F12` sign-in → sign-out → sign-in cycle on desktop and phone/PWA, without refresh, data deletion or cache clearing.
- Smoke-regress the passed `V5-F07` case with Flight and Insurance together; confirm Today remains reachable and each card appears once.
- Resolve `V5-F13`: verify preparation-stage Quick Access prioritizes packing/preparation tasks and does not confusingly repeat the same Flight already presented in Today.

## Final gate

After test 28 and all blocking focused regressions, classify every finding, assign an explicit target/disposition, update synchronized documents and issue the Product Owner GO / NO-GO decision.

## Alpha 0.5.3 focused acceptance sequence

Prerequisite: install `FAMILY_TRIPS_V5_0_5_3_CORRECTION.zip`, confirm `INSTALLATION PASSED`, branch `version/V5` and displayed version `Alpha 0.5.3`. Deploy that same `dist` only to the fixed QA URL. Do not repeat the completed 0.5.2 acceptance session.

1. Complete one online load, fully close the installed phone PWA, disable Wi-Fi and mobile data, and reopen from the home-screen icon. The saved Trip and local data must load in Offline mode without a connection-error dead end. Restore connectivity and verify automatic recovery without duplication.
1A. While the cached application is open Offline, attempt Sign out. The corrected behavior must never leave the user on the browser connection-error page. Before correction implementation, choose and document one supported behavior: block Sign out with a clear Offline explanation while preserving the local session, or complete Sign out into an Offline-capable signed-out application shell that recovers when connectivity returns.
2. Share the real URL to the application. If the application can read enough meaningful data to identify a supported item, verify that it proposes that meaningful type and extracted fields, keeps the source URL visible and requires explicit approval. If it cannot read enough meaningful data, verify that it stops with a clear explanation and creates neither a proposal nor a record; `Shared link` is not an allowed fallback. Separately verify that a URL can still be added to an existing item through that item's editor. Contextual attachment is allowed only when the application presents a clear existing/new owning item for explicit approval. A standalone Link is never a final savable item type.
3. Open Trip Center. Verify there is no standalone Links tile, list, card, tab or Quick Access destination. An existing item-owned website/source action must still open from its item.
4. Open Trip Center. Verify there is no standalone Locations tile, list, card, tab or Quick Access destination. An existing item retains its address, navigation action and map handoff.
5. Edit one item to a date outside the Trip range. Verify the pre-save warning identifies the Trip range; choose Cancel and confirm that the item is unchanged. Repeat, choose Proceed, and verify the same single item saves and retains the existing out-of-range marker. Restore the original date.
6. Verify a QA application response has `Cross-Origin-Opener-Policy: same-origin-allow-popups`, complete Google popup sign-in and record whether the popup-close COOP warning recurs. This observation remains `V5-W01` evidence; it is not implicitly accepted.
7. Run the smallest regression only: one existing URL open/edit check, one item detail location/map action, one warm-offline edit/reconnect, and one Timeline smoke navigation. Record each result immediately and issue GO/NO-GO only after every focused finding has an explicit disposition.

### Alpha 0.5.3 implementation evidence

| Finding | Implemented change | Retest status |
|---|---|---|
| Cold-start offline | The saved local session is rendered before Firebase initialization when the installed PWA starts offline; connectivity restoration reloads into normal authentication. Service-worker cache identity advanced to `0.5.3`. | **FAIL — BLOCKING.** Fully closed offline launch reaches the browser connection-error page. With connectivity continuously disabled, refreshes alternate deterministically: every odd refresh fails at the browser connection-error page and every even refresh loads the cached application and saved Trip. This confirms unstable service-worker navigation/cache control on the same Offline state; it is not explained by a network-state change or by Google authentication alone. No further refresh diagnosis is required on this build. |
| `V5-F23` | Not part of the original 0.5.3 correction scope. Newly discovered during the installed-device Offline diagnostic. | **FAIL — BLOCKING / OPEN.** Selecting Sign out while the cached application is open Offline immediately reaches the browser connection-error page and cannot recover until connectivity is restored. Correction planning must choose either a clearly disabled Offline Sign out that preserves the local session, or an Offline-capable signed-out application shell; a browser error dead end is not acceptable. |
| `V5-F17` | Link intake now accepts an optional meaningful link/place name while retaining `Shared link` as the safe fallback. | **FAIL — OPEN; ACCEPTANCE CONTRACT REVISED.** The real-link proposal defaulted to `Shared link`. The title was editable before approval and the original URL remained visible, but the application did not extract meaningful item data. The Product Owner rejects `Shared link` as an import fallback. The corrected flow may create a proposal only when it extracts enough meaningful data to identify a supported item, or when it can present a clear contextual owning-item association for explicit approval. Otherwise it must stop with an explanation and create neither a proposal nor a record. Manual URL entry remains available from an existing item's editor. |
| `V5-F24` | Not part of the original 0.5.3 correction scope. Newly exposed by the interaction between shared-link intake and removal of the standalone Links collection. | **FAIL — BLOCKING / OPEN.** The proposal could be approved while its final type remained Link. The saved Link then had no owning item and no user-visible collection or route, so the Product Owner could not see or reopen it after save. A standalone Link is not a final domain item and must never be savable. Link data may be stored only as a property/source of a meaningful owning item selected in the item editor or proposed from reliable context and explicitly approved. Never persist an inaccessible orphan Link. |
| `V5-F25` | Not part of the original 0.5.3 correction scope. Newly exposed by the Product Owner's QR pre-rescan screenshot. | **FAIL — OPEN PRODUCT DEFECT.** Two proposals already marked `Rejected` remain inside the active Suggestions section and are rendered before a new `Pending approval` Panvaree Resort proposal. Rejection must immediately remove a proposal from the active queue and place it once in a separate archive. Archived proposals must not precede, obscure or count as active/new suggestions. Retest requires: reject one pending proposal; confirm it disappears from active Suggestions, appears once in the archive with its rejected state, survives refresh there, and a later pending proposal remains in the active section without duplication. |
| `V5-F19` | The standalone Links category is excluded from Trip Center; item-owned URL and source actions are retained. | **PASS / CLOSED.** Product Owner confirmed that Trip Center has no standalone Links category or destination. A URL already owned by an existing meaningful item remains visible inside that item and opens the correct address. This closes only the presentation/category defect; it does not permit standalone Link creation. |
| `V5-F21` | Out-of-range start/end dates now require an explicit pre-save confirmation; cancellation leaves the existing item untouched. | **PASS / CLOSED.** Product Owner confirmed both branches. The warning clearly identifies the Trip range; Cancel preserves the original in-range date; Proceed saves the same single item outside the Trip range with the existing marker; restoring the original date removes the marker without duplication or changes to unrelated items or Trip dates. |
| `V5-F22` | The standalone Locations operational tile is removed; stored location records and item-level map/navigation behavior remain preserved. | **PASS / CLOSED.** Product Owner confirmed that Trip Center has no standalone Locations category or destination. A pre-existing meaningful item retains its address, and its navigation/map action opens the correct destination. |
| `V5-W01` | Existing Netlify COOP configuration remains asserted. | **PARTIAL PASS / WARNING STILL OPEN; EVIDENCE COMPLETE.** The current QA document response contains exactly `Cross-Origin-Opener-Policy: same-origin-allow-popups`, and Google popup sign-in succeeds. Chrome Console nevertheless displays `Cross-Origin-Opener-Policy policy would block the window.close call` twice. The unrelated `Host is not in insights whitelist` message is not treated as the FamilyTrips finding. The header configuration is verified; the remaining defect is the recurring popup-close warning. |

Production remains unchanged. Alpha 0.6 capabilities—including Shopping places/visits, multi-user synchronization, participant management, multiple destination countries, task filters and category-prefilled Add—are excluded from 0.5.3.

## Alpha 0.5.3 QA execution checkpoint — installation

| Check | Result | Evidence / disposition |
|---|---|---|
| R2 correction installation | PASS | Installer detected the Alpha 0.5.2 NO-GO correction baseline and completed successfully. Installed version: `0.5.3`; source handoff: `0.5.2`; branch: `version/V5`. |
| Automated syntax and regression gate | PASS | Syntax checks, smoke tests, V4 ingestion/correction, V5 ingestion/external-import/operational/correction and offline-shell suites all passed. |
| Production build | PASS | Vite 7.3.6 transformed 32 modules and completed the `dist` build successfully. |
| Dependency audit observation | OPEN NON-ACCEPTANCE OBSERVATION | `npm audit` reported one high-severity vulnerability. Do not run an unreviewed `npm audit fix` during acceptance because it could change the tested dependency graph. Track separately for technical review. |
| Local recovery point | RECORDED | `C:\Projects\FamilyTrips\family-trips-backups\v5-0.5.3-correction-20260810-212922`. |
| Fixed QA deployment of this exact `dist` | PASS | The already-tested `dist` was deployed with the fixed `qa` alias and `--no-build` to `https://qa--familytripz.netlify.app`. Netlify reported successful Draft deploy ID `6a7a1a215ae74b40bafc1f35`. No `--prod` deployment was performed. |
| Displayed version identity | PASS | Product Owner confirmed that the fixed QA deployment displays `Alpha 0.5.3`. No Production deployment was performed. |
| Focused installed-device acceptance step 1 — cold-start offline and reconnect | **FAIL — BLOCKING** | After a completed online load, the installed PWA was fully closed, Wi-Fi and mobile data were disabled, and the app was opened from its home-screen icon. It did not load the cached shell or saved Trip and instead displayed the browser connection-error page. During a controlled sequence in which both network paths stayed disabled throughout, every odd refresh showed the connection-error page and every even refresh loaded the cached application and saved Trip. The pattern repeated consistently through additional refreshes. This deterministic alternation confirms a service-worker/navigation-cache control failure on the same Offline state; Google authentication may still affect session behavior, but it does not explain the browser-level odd/even navigation alternation. No data loss or duplication was reported. |
| `V5-F23` — Sign out while Offline | **FAIL — BLOCKING / OPEN** | While the cached application was loaded and the device remained Offline, selecting Sign out immediately displayed the browser connection-error page. There was no in-app route back and recovery was possible only after restoring Internet connectivity. Record as a separate finding because the user action triggers a new dead-end path even when the Offline application shell is already available. |
| Focused acceptance step 2 / `V5-F17` — shared-URL intake | **FAIL — OPEN** | The application could not read meaningful item data from the real shared URL but still produced a `Shared link` proposal. Before approval, the title and proposed type could be changed and the original URL was visible. The Product Owner explicitly rejects this fallback. Correct behavior is: create a proposal only from sufficient meaningful extraction or a clear contextual owning-item association; otherwise explain that the link could not be imported and create nothing. The URL can still be added manually to an existing item. |
| `V5-F24` — approved standalone Link becomes inaccessible | **FAIL — BLOCKING / OPEN** | The proposal was approved without changing its Link type. A Link record was added, but after save it was not visible anywhere because the user-visible standalone Links collection had been removed and the record was not attached to another item. No deletion or recovery action is requested from the Product Owner on this build. |
| `V5-F25` — rejected proposals shown ahead of pending proposals | **FAIL — OPEN** | The supplied Trip Center screenshot shows two `Shared link` proposals marked `Rejected` still displayed in the active Suggestions section above the newly scanned `Panvaree Resort` proposal marked `Pending approval`. Product decision: rejected proposals belong in a separate archive and must not be rendered before or among active/new suggestions. No additional sorting assumption is made beyond separating active and archived states. |
| Focused acceptance step 3 / `V5-F19` — standalone Links UI and item-owned URL | **PASS / CLOSED** | Product Owner confirmed that Trip Center has no standalone Links category or destination. A pre-existing meaningful item that owns a URL still displays that URL inside the item, and the action opens the correct address. The supported item-owned link path is preserved. This result does not close `V5-F17` or `V5-F24`, because share intake can still create a `Shared link` proposal and orphan Link record. |
| Focused acceptance step 4 / `V5-F22` — standalone Locations UI and item-owned location | **PASS / CLOSED** | Product Owner confirmed that Trip Center has no standalone Locations category or destination. A pre-existing meaningful item retains its address, and its navigation/map action opens the correct destination. The supported item-owned location path is preserved. |
| Focused acceptance step 5 / `V5-F21` — warning, Cancel, Proceed and restoration | **PASS / CLOSED** | Product Owner confirmed that changing an existing in-range item to one day after the Trip end date triggered a pre-save warning identifying the configured Trip range. Cancel preserved the original date and created no duplicate. Repeating the change and choosing Proceed saved exactly one item with the outside-Trip-range marker. Restoring the original in-range date removed the marker, preserved one record and changed no unrelated item or Trip date. |
| Focused acceptance step 6 / `V5-W01` — deployed COOP and Google popup | **PARTIAL PASS / WARNING STILL OPEN; EVIDENCE COMPLETE** | The supplied Network capture shows that the main QA document response contains exactly `Cross-Origin-Opener-Policy: same-origin-allow-popups`. Google popup sign-in completed successfully and the application returned to the signed-in state. The supplied Chrome Console capture nevertheless shows `Cross-Origin-Opener-Policy policy would block the window.close call` twice, so the explicit no-warning criterion failed and `V5-W01` remains open. `Host is not in insights whitelist` is recorded as separate browser/extension noise unless later evidence links it to the application. No further header or sign-in observation is required on this build. |
| Focused regression step 7A — existing-item URL edit/open/restore | **PASS** | Product Owner confirmed that a non-critical existing item's URL could be replaced and saved, the same item reopened once with the replacement URL, and the replacement destination opened correctly. Restoring the original URL persisted on the same single record without duplication. This smoke check is complete and must not be repeated on this build. |
| Focused regression step 7B — item-owned location/map navigation | **PASS** | Product Owner confirmed that a non-critical existing item displayed the correct address/location, its map/navigation action opened the correct destination, and returning to the application left the same single item unchanged and without duplication. This smoke check is complete and must not be repeated on this build. |
| Focused regression step 7C — warm-offline edit/reconnect | **PASS** | Product Owner confirmed that while the application remained open, connectivity was disabled, one non-critical item's title was edited and saved, and the edited item appeared exactly once. After connectivity was restored and the application refreshed, the edit persisted once without duplication or changes to unrelated items. The original title was restored and saved. This smoke check is complete and must not be repeated on this build. |
| Focused regression step 7D — Timeline Entire Trip / Week / Day navigation | **PASS** | Product Owner confirmed that Entire Trip, Week and Day each kept the active period synchronized with the list below it. Opening one existing item from the list and returning preserved the same date and the same single record without change or duplication. Focused regression step 7 is complete and must not be repeated on this build. |
| Product Owner requested QR creation-to-ingestion smoke | **PASS — NON-BLOCKING ADDITIONAL REGRESSION COMPLETE** | The first scan created one editable Panvaree Resort proposal, but its location was only the general Khao Sok/Ratchaprapha address. Payload inspection proved that this exact general address was encoded in the fixture, so no QR decoder/importer failure was assigned. `FAMILYTRIPS_EXTERNAL_INGESTION_MODEL_INSTRUCTIONS_V4.md` now requires official-site and exact-place research before QR creation. The replacement five-part `FTI1C` series was independently decoded, checksum-verified and accepted by the application collector as exactly one hotel proposal. The Product Owner then confirmed correct installed-app ingestion, including the official website `https://www.panvaree.com/`, exact named-property location `Panvaree Resort/พันวารีย์ รีสอร์ท · Cheow Lan Lake, Khao Sok National Park, Ban Ta Khun, Surat Thani 84230, Thailand`, and map/navigation target at the resort rather than only Khao Sok National Park or Ratchaprapha Dam. The corrected proposal must be rejected/cancelled rather than approved because this is a smoke fixture. The complete historical QR matrix remains closed. |

Final release state is **NO-GO / acceptance complete**. Cold-start Offline, `V5-F23` and `V5-F24` are confirmed release-blocking FAILs in Alpha 0.5.3. `V5-F17` and new `V5-F25` also remain open. `V5-F19`, `V5-F21` and `V5-F22` are closed by focused Product Owner retest. `V5-W01` remains open: the required response header is verified and functional sign-in passed, but the popup-close warning criterion failed. Focused acceptance step 6 has no remaining observation on this build. Focused regression steps 7A, 7B, 7C and 7D all passed. The corrected QR smoke passed. The observed general location is assigned to the earlier fixture/instruction process, not to the QR decoder; `V5-F25` is a separate proposal-lifecycle/UI defect. No additional testing is required on Alpha 0.5.3 before correction planning.

### Next activity

Testing of Alpha 0.5.3 is complete. Do not repeat the focused acceptance or regression sequence on this candidate. Move to correction planning for cold-start Offline, `V5-F17`, `V5-F23`, `V5-F24` and `V5-F25`, and decide the technical disposition of `V5-W01`. Preserve the already-passed `V5-F19`, `V5-F21`, `V5-F22` and regression evidence.

The corrected QR creation-to-ingestion smoke is complete and must not be repeated on this build. `Confirmation_for_Booking_ Ban Ta Khun.pdf` remains the standing default source for every later repeated QR check unless the Product Owner supplies another document. Reject/cancel the current corrected Panvaree Resort smoke proposal; do not approve it. Its rejected-state presentation may continue to demonstrate already-open `V5-F25`, but no additional archive diagnosis is required on this build.

Do not deploy Alpha 0.5.3 to Production. Alpha 0.4.3 remains the accepted Production version until a later corrected candidate completes its own focused acceptance.

## Alpha 0.5.4 approved correction plan

Alpha 0.5.4 is a stabilization release, not an Alpha 0.6 feature release. It must be implemented and packaged as one controlled correction over the local Alpha 0.5.3 NO-GO handoff while Alpha 0.4.3 remains the Production rollback baseline.

### Frozen implementation scope

1. **Cold-start Offline/navigation control**
   - Cache the application shell and required local runtime assets under an unambiguous 0.5.4 cache identity.
   - On the first fully closed Offline launch, render the cached shell and last saved Trip before any network-dependent Firebase/authentication initialization.
   - Repeated launches and refreshes while connectivity remains disabled must stay consistently inside the application; the 0.5.3 odd/even browser-error alternation must not recur.
   - Reconnect must restore normal online/authentication state without duplicate items, lost edits or manual cache clearing.

2. **`V5-F23` — Offline Sign out**
   - When Offline, disable Sign out and show a clear explanation that connectivity is required.
   - Preserve the current local session, application shell and visible Trip.
   - Do not clear local usability, navigate to an authentication route or expose a browser connection-error page.

3. **`V5-F17` and `V5-F24` — shared-URL intake and Link ownership**
   - Do not create a `Shared link` fallback proposal.
   - Create a proposal only when the application extracts enough meaningful information to identify a supported item/type and shows that owning item for explicit approval.
   - If extraction is insufficient, show a clear failure explanation and create no proposal, source or record.
   - A standalone Link is never an approvable/savable final type. Manual URL entry remains available inside an existing item's editor.

4. **`V5-F25` — rejected-proposal archive**
   - Rejecting a pending proposal removes it immediately from active Suggestions.
   - Store and show it exactly once in a separate archive with its rejected state.
   - The archived record persists after refresh/restart, does not count as active and never appears ahead of or among later pending proposals.

5. **Dependency advisory disposition**
   - Review the high-severity `npm audit` observation recorded during 0.5.3 installation evidence.
   - Record the exact package/advisory, runtime reachability and chosen correction or deferral.
   - Do not run an unreviewed bulk `npm audit fix`; any dependency change must pass the same automated and Production-build gate as application changes.

### Explicit exclusions

- No Multi-trip, Owner/Editor sharing, cloud/offline synchronization expansion or conflict model.
- No Documents/Contacts category relocation, Intake Inbox, participant-document model or direct file/ticket attachment.
- No first-class Transport, Shopping expansion, Expenses, task priority/filter/detail expansion, participant management move or multiple Destination Countries.
- No broad visual redesign and no repetition of the complete QR generation/decoding matrix.

### `V5-W01` technical disposition

The required `Cross-Origin-Opener-Policy: same-origin-allow-popups` response header and functional Google popup sign-in passed on 0.5.3. The recurring popup-close Console warning remains open but is non-blocking for 0.5.4 and assigned to Alpha 0.6 technical hardening. The 0.5.4 package must preserve the header and pass one functional Google sign-in smoke; eliminating the Console warning is not a 0.5.4 GO criterion.

### Package and deployment gate

Before Product Owner testing begins:

1. Install the correction over the recognized Alpha 0.5.3 NO-GO handoff with rollback backup and displayed version `Alpha 0.5.4`.
2. Pass all automated correction/regression suites and the Production build.
3. Record dependency-advisory disposition.
4. Deploy the exact already-tested `dist` only to the fixed QA alias; do not rebuild during deployment and do not deploy to Production.
5. Record install, build, deploy and displayed-version evidence in this document. Automated PASS does not close any Product Owner finding.

### Alpha 0.5.4 installation checkpoint — 2026-08-11

| Check | Result | Evidence / disposition |
|---|---|---|
| Correction installation | PASS | Installer reported `INSTALLATION PASSED`; installed version `0.5.4`; source handoff version `0.5.3`; branch `version/V5`. |
| Automated correction/regression suites | PASS | Operational-data, Alpha 0.5.4 correction, Offline-shell and Alpha 0.5.4 stabilization suites all passed. |
| Production build | PASS | Vite 7.3.6 transformed 33 modules and completed the tested `dist` build successfully in 3.65 seconds. |
| Dependency disposition | PASS | The implemented dependency update and automated gate retained the documented zero-vulnerability disposition. |
| Local recovery point | RECORDED | `C:\Projects\FamilyTrips\family-trips-backups\v5-0.5.4-correction-20260811-092940`. |
| Fixed QA deployment of this exact `dist` | PASS | Netlify reported a successful Draft deployment of the already-tested `dist` to `https://qa--familytripz.netlify.app` with deploy ID `6a7ac35b55da1eee6726154e`. The deployment used the fixed `qa` alias and no Production deployment was performed. |
| Product Owner findings | COMPLETE / NO-GO | Cold-start Offline, `V5-F23`, `V5-F24`, `V5-F25`, the existing-item URL path and both compact-regression steps 9A–9B passed Product Owner retest. The insufficient-link failure branch of `V5-F17` passed, but its supported-link branch failed because the official Panvaree hotel URL was not recognized. All planned observations are complete; the remaining `V5-F17` blocker prevents GO. |

## Alpha 0.5.4 focused executable acceptance sequence

ChatGPT presents exactly one numbered action at a time in the application-testing chat, waits for the Product Owner result, records it immediately in the next document revision and then continues. Do not repeat completed 0.5.3 tests outside the focused sequence below.

1. **Version and online baseline:** open fixed QA, verify `Alpha 0.5.4`, complete Google sign-in and open the existing Trip. This is a functional smoke only; do not use Console-warning absence as a GO gate.
2. **Cold-start Offline:** after one completed online load, fully close the installed PWA, disable Wi-Fi and mobile data, and open it once from the home-screen icon. The shell and last saved Trip must appear on the first attempt without a browser error.
3. **Offline stability and reconnect:** while connectivity remains disabled, perform two controlled in-app refresh/reopen cycles. Both must remain successful with no odd/even alternation. Restore connectivity and verify automatic recovery with the same single records and no lost edits.
4. **`V5-F23` Offline Sign out:** return Offline while the cached application is open and select Sign out. Verify the action is blocked with a clear connectivity explanation, the current local session/Trip remains usable and no browser connection-error page appears. Restore connectivity afterward; do not sign out during this step.
5. **`V5-F17`/`V5-F24` insufficient shared URL:** share the same real URL that produced `Shared link` in 0.5.3. Verify a clear import failure and that no proposal, source, Link record or inaccessible item is created.
6. **Supported shared URL and existing-item URL path:** share one controlled URL whose content identifies a supported meaningful item and verify a correctly owned proposal with explicit approval. Cancel/reject it rather than creating test data. Separately open an existing item's editor and verify its URL can still be added/edited and opened from that item.
7. **`V5-F25` active/archive separation:** create one controlled pending proposal, reject it and verify it disappears from active Suggestions and appears exactly once in the archive. Refresh/restart and verify it remains only in the archive. Create or expose a later pending proposal and verify it remains in active Suggestions without the rejected entry ahead of it or any duplication. The standing repeated QR source is `Confirmation_for_Booking_ Ban Ta Khun.pdf` unless another document is supplied; only this proposal-lifecycle path is in scope, not the full QR matrix.
8. **Preserved ownership and date behavior smoke:** confirm Trip Center still has no standalone Links or Locations destination; open one item-owned URL and one item-owned map/navigation action. Exercise only the Cancel branch of the already-accepted out-of-range warning to ensure the item remains unchanged.
9. **Compact regression:** perform one warm-Offline edit/reconnect check and one Timeline Entire Trip/Week/Day synchronization smoke on stable existing records. Confirm no duplication, data loss or date change.
10. **Final gate:** classify cold-start Offline, `V5-F17`, `V5-F23`, `V5-F24`, `V5-F25`, dependency disposition and every focused regression. Issue GO only if all blocking functional checks pass. On GO, the exact tested build may be promoted to Production and Alpha 0.6 planning/implementation may begin. On any blocking FAIL, keep Alpha 0.4.3 in Production and return to another focused correction plan.

### Alpha 0.5.4 focused acceptance results

| Step / finding | Result | Product Owner evidence / disposition |
|---|---|---|
| 1 — version and online baseline | PASS | Fixed QA displayed `Alpha 0.5.4`; Google sign-in and existing-Trip/data load succeeded. |
| 2 — first-attempt cold-start Offline | PASS | After full close and disabling both network paths, the installed PWA opened the cached shell and last Trip on the first launch without a browser connection-error page. |
| 3 — Offline stability and reconnect | PASS | Offline refresh and full close/reopen both succeeded without odd/even alternation; reconnect recovered automatically without loss or duplication. |
| 4 / `V5-F23` — Offline Sign out | PASS / CLOSED FOR 0.5.4 | Sign out was unavailable while Offline and a clear connectivity-required message was displayed. The local session and Trip remained in the application, with no browser connection-error page. |
| 5 / `V5-F17` insufficient branch and `V5-F24` Link ownership | PASS / `V5-F24` CLOSED; `V5-F17` PARTIAL | Re-sharing the same real `BIG גדרה` URL produced a clear import-failure message. No proposal, source, standalone Link record or inaccessible item was created. This accepts the safe insufficient-link branch and closes `V5-F24`, but does not by itself accept supported-link recognition under `V5-F17`. |
| 6A / `V5-F17` — supported shared URL | FAIL — BLOCKING / CHARACTERIZED | Sharing `https://www.panvaree.com/` did not reach a savable proposal. The application displayed: `לא ניתן לזהות מהקישור פריט נסיעה נתמך. לא נוצרה הצעה, מקור או פריט. אפשר להוסיף את הקישור מתוך עריכת פריט קיים.` No field was marked and no proposal existed to save. The no-orphan safeguard worked, but the controlled official hotel URL was rejected instead of producing one explicitly approvable `Hotel` proposal for Panvaree Resort. The supported branch of `V5-F17` remains open and Alpha 0.5.4 cannot receive GO on this evidence. |
| 6B — existing-item URL edit/open/restore | PASS | Product Owner confirmed the complete existing-item path: a stable non-critical item's original URL was recorded, replaced with `https://www.panvaree.com/`, saved, reopened and opened at the correct destination. No additional item was created. The original URL was then restored and saved on the same single record. This preserves the item-owned URL capability but does not cure the separate step 6A supported shared-URL recognition failure. |
| 7A / `V5-F25` — controlled pending proposal | PASS | Scanning the five controlled Panvaree QR parts in order produced exactly one `Panvaree Resort` proposal in `Pending approval`. The proposal has not yet been approved or rejected. This establishes the controlled precondition only; `V5-F25` remains open until rejection, active/archive separation, refresh persistence and coexistence with a later pending proposal all pass. |
| 7B / `V5-F25` — immediate active/archive separation | PASS / PARTIAL ACCEPTANCE | Rejecting the controlled `Panvaree Resort` proposal removed it immediately from active Suggestions and displayed it exactly once in the rejected-proposal archive. Screenshot evidence confirms the archive is a separate section. `V5-F25` remains open only for persistence after refresh/restart and coexistence with a later pending proposal. |
| 7C / `V5-F25` — archive persistence after refresh | PASS / PARTIAL ACCEPTANCE | After one refresh, `Panvaree Resort` remained exactly once in the rejected-proposal archive and did not reappear in active Suggestions. The archive-persistence criterion is accepted. `V5-F25` remains open only for coexistence with a distinct later pending proposal. |
| 7D / `V5-F25` — later-pending coexistence | PASS / CLOSED FOR 0.5.4 | The corrected distinct QR created one proposal named `בדיקת הצעה חדשה לאחר ארכיון`, and the Product Owner confirmed it appears exactly once only in active Suggestions as `Pending approval`. Rejected `Panvaree Resort` remains exactly once only in the archive, with no duplication or mixing between active and archived states. Together with steps 7A–7C, this completes the full `V5-F25` acceptance contract. Do not approve or reject the later test proposal unless cleanup is explicitly requested after the focused sequence. |
| Non-blocking archive placement UX | RECORDED — NOT A 0.5.4 BLOCKER | The archive currently appears above Sources. Product Owner preference: render the archive at the bottom of the screen, after active content, Sources and ordinary actions. This visual-order refinement does not invalidate step 7B and does not independently block Alpha 0.5.4. |
| 8A — Trip Center standalone Links/Locations absence | PASS | Product Owner confirmed that Trip Center exposes neither a standalone Links destination nor a standalone Locations destination. This preserves the accepted item-ownership structure and does not reopen `V5-F19` or `V5-F22`. Item-owned URL, map/navigation and out-of-range Cancel branches remain to be smoked separately. |
| 8B — item-owned URL navigation and ownership | PASS | Product Owner confirmed that an existing meaningful item's URL opened the correct website. Returning to the application restored the same item, and no duplicate item or standalone Link record was created. This preserves the item-owned URL path without reopening `V5-F19` or `V5-F24`. The item-owned map/navigation and out-of-range Cancel branches remain to be smoked separately. |
| 8C — item-owned map/navigation and ownership | PASS | Product Owner confirmed that an existing meaningful item's map/navigation action opened the correct destination. Returning to the application restored the same item, and no duplicate item or standalone Location record was created. This preserves the item-owned location path without reopening `V5-F22`. Only the out-of-range warning Cancel branch remains in step 8. |
| 8D / `V5-F21` — out-of-range warning Cancel branch | FAIL — NON-BLOCKING UX REGRESSION; DATA INTEGRITY PASS | The warning appeared and identified the out-of-range condition. Cancel prevented persistence: after closing the editor without another Save or Proceed action and reopening the same item, the original in-range date remained. However, before the editor was closed it continued to display the newly entered out-of-range date instead of immediately restoring the original value. The saved item is unchanged and no duplicate or collateral change was reported; correct the stale editor state in the next correction package. |
| 9A — warm-Offline edit/reconnect compact regression | PASS | With the application already open, the Product Owner disabled Wi-Fi and mobile data, edited and saved one non-date text field on a stable non-critical existing item, restored connectivity and refreshed once. The same single item retained the edit, with no duplicate, data loss or collateral change to other records. |
| 9B — Timeline Entire Trip / Week / Day synchronization smoke | PASS | The Product Owner confirmed that navigation among Entire Trip, Week and Day remained synchronized. In Week, period navigation updated the visible period, date selection and filtered list together; Day displayed only the selected day's items; returning to Entire Trip showed no duplicates or missing records. No item date changed and no record was duplicated or lost. |

### Final gate decision

Implementation, package creation, Product Owner installation and fixed-QA Draft deployment for Alpha 0.5.4 are complete. Every planned focused acceptance and compact-regression observation has an explicit result. Cold-start Offline, `V5-F23`, `V5-F24`, `V5-F25`, existing-item URL ownership, preserved Trip Center structure, item-owned navigation, warm-Offline edit/reconnect and Timeline synchronization all passed. `V5-F23`, `V5-F24` and `V5-F25` are closed for Alpha 0.5.4.

Final release state: **NO-GO / acceptance complete**. `V5-F17` remains release-blocking because the controlled official Panvaree hotel URL was rejected instead of creating one supported, meaningful Hotel proposal for explicit approval. Step 8D leaves a separate non-blocking `V5-F21` UX regression: Cancel protects persisted data but does not immediately reset the still-open editor's displayed date. The archive-placement preference is also retained as a non-blocking UX refinement. `V5-W01` remains assigned to Alpha 0.6 technical hardening under the approved disposition.

Do not run more tests against this Alpha 0.5.4 candidate and do not deploy it to Production. Alpha 0.4.3 remains the accepted Production version. Return to planning/implementation for a focused correction of supported shared-URL recognition under `V5-F17`, include the small `V5-F21` stale-editor-state correction and archive-placement refinement when the next correction scope is approved, and preserve the later pending test proposal until cleanup is explicitly requested.

## Alpha 0.5.5 focused intake-scope reset

Alpha 0.5.5 closes the failed URL recognizer by removing that unsupported product path, not by adding another site-specific heuristic. It also records the Product Owner decision that image/camera-photo and PDF Smart Import are not reliable. Image/photo creation paths are hidden until the dedicated Alpha 0.6 redesign; PDF alone remains visible as an explicitly experimental path for quick evidence collection.

### Frozen implementation scope

1. **Reliable source menu**
   - Show Manual, Experimental PDF, validated JSON and QR.
   - Do not show Paste/Share URL, image upload or camera-photo as supported new-item creation choices.
   - A defensive attempt to invoke a removed URL/image/photo state creates no source, proposal, Link or item and explains the supported alternatives.

2. **Manual and QR/JSON URL ownership**
   - Manual create/edit retains the optional item-owned URL field.
   - Validated `familytrips.import` / `FTI1` / `FTI1C` may populate the same field inside a meaningful owning-item proposal.
   - Neither path creates a standalone Link or user-visible Links collection.

3. **Experimental PDF safety**
   - Accept PDF only in this experimental path; ordinary images and camera capture are not accepted there.
   - Store the original before showing the proposal.
   - Label the source and proposal as experimental and require explicit review/approval.
   - Do not default a missing extracted date from the Trip start date.
   - Missing or uncertain values remain empty and are shown through warnings; partial or failed extraction is allowed.
   - Silent save, fabricated values, lost original, duplicate creation or inability to cancel/reject safely are blocking.

4. **`V5-F21` editor-state reset**
   - When the user cancels the out-of-range save warning while editing an existing item, re-render the editor from the persisted item immediately.
   - The original date is visible without closing/reopening, and no change or duplicate is saved.

5. **Archive placement**
   - Active suggestions, ordinary items, Sources and the Add action appear before the rejected-proposal archive.
   - The archive remains persistent, deduplicated and excluded from active counts.

### Explicit exclusions

- No Smart Import implementation for URL, image, camera or PDF beyond the experimental PDF safety boundary.
- No new OCR heuristics, provider-specific recognizers, URL-domain rules or filename-based type expansion.
- No Alpha 0.6/0.7 Multi-trip, synchronization, document/contact relocation, Transport, Expenses, Shopping or task-model expansion.
- No repetition of the complete accepted QR, Offline or Timeline matrices.

### Automated implementation gate

The implementation uses displayed application version `Alpha 0.5.5`, package version `0.5.5` and service-worker cache identity `family-trips-alpha-0.5.5-shell-v1`. The dedicated intake-reset tests verify menu scope, defensive removed-path behavior, PDF-only validation, missing-date preservation, experimental warnings, archive ordering and immediate editor reset. Existing V4/V5 ingestion, external-import/QR, operational, Offline, correction and stabilization suites remain in the quality gate.

Technical result on 2026-08-12: all syntax checks and ten ordered automated suites passed; Vite 7.3.6 transformed 33 modules and completed the Production build in 1.97 seconds; `npm audit` reported zero vulnerabilities.

### Focused Product Owner acceptance sequence

ChatGPT presents exactly one action at a time in the application-testing chat, records the result immediately in the next controlled-document revision and does not repeat already accepted matrices.

1. **Install and online baseline:** install the 0.5.5 correction over the recognized 0.5.4 NO-GO handoff, confirm `INSTALLATION PASSED`, displayed `Alpha 0.5.5`, branch `version/V5`, successful Google sign-in and the existing Trip/data.
2. **Source-menu boundary:** open Add and verify exactly Manual, Experimental PDF, JSON and QR. Verify there is no new-item URL, image upload or camera-photo choice.
3. **Manual item-owned URL:** create or edit one stable non-critical item, add/replace its URL, save/reopen/open it, then restore the original value. Confirm one owning item and no standalone Link.
4. **Validated QR/JSON URL:** use one already validated compact fixture containing an item-owned website. Confirm one reviewable proposal, correct URL and no final item before approval. Reject/cancel the test proposal unless it is desired Trip data.
5. **Experimental PDF:** import one controlled real PDF. Confirm the experimental warning, preserved and identified original source, review-only proposal and empty presentation for any missing date/time/location/website/reference. Reject/cancel safely after observation; extraction accuracy is recorded as redesign evidence but does not block unless the safety contract fails. In-app opening of the original PDF is not required in Alpha 0.5.5.
6. **Out-of-range Cancel:** edit one existing dated item to an out-of-range date and choose Cancel. Without closing the editor, verify the original persisted date is immediately visible. Close/reopen and confirm the same single unchanged item.
7. **Archive placement:** open the Documents/source area with active content and a rejected entry. Confirm active suggestions, items, Sources and Add appear before the archive; confirm the rejected record remains once only and does not count as active.
8. **Compact regression:** perform one first-attempt cold Offline launch plus reconnect/data-integrity smoke and one Timeline Entire Trip/Week/Day synchronization smoke. Do not repeat the full matrices.
9. **Final gate:** issue GO only if the supported-source boundary, both item-owned URL paths, PDF safety, Cancel reset, archive placement, exact-build evidence and compact regressions pass. On GO the exact tested build may replace 0.4.3 in Production. On blocking FAIL, retain 0.4.3 and plan only a focused 0.5.6 correction; do not begin Alpha 0.6 implementation.

### Focused acceptance status

Focused acceptance steps 1 through 4 are complete. Fixed QA displays `Alpha 0.5.5`; Google sign-in and the existing Trip/data load succeed on both desktop and phone. The phone Add screenshot shows exactly Manual, Experimental PDF, JSON and QR and no URL-intake, image-upload or camera-photo source choice. In step 3, the Product Owner created one Activity titled `בדיקת קישור ידני 0.5.5` with `https://www.elal.com`; the saved item detail showed the `קישור` action inside that owning Activity, tapping it opened the intended EL AL destination, returning preserved the same single record, and deletion removed the disposable item successfully. This completes manual URL ownership without a standalone Link or residual test data. Step 4 used a newly generated and independently round-trip-verified single-part `FTI1` fixture titled `בדיקת קישור מתוך QR 0.5.5`, dated 19 January 2027 and owning website `https://www.elal.com`. Three Product Owner phone screenshots confirmed exactly one review-stage proposal: the singular title prefix `בדיקת קישור מתוך` matched the fixture, the displayed date was 19 January 2027, the EL AL URL was present, and the page exposed proposal-only actions including `אישור ויצירת פריט`, `אחר כך` and `דחייה`. Rejecting it moved the proposal into the rejected-proposal archive. After one refresh/reopen, the Product Owner confirmed that the proposal remained only in the archive and did not return to active review, while its separate source remained exactly once under Sources. Retaining the source is expected provenance behavior and not a residual item, duplicate proposal or defect. Sources and Add appear before the archive, so rejection, persistence and archive placement are PASS.

Focused acceptance step 5 is PASS and complete. Two Product Owner phone screenshots show `Confirmation_for_Booking_ Ban Ta Khun.pdf` on the `בדיקת הצעה` page with the explicit `PDF ניסיוני` badge, the warning that every field must be reviewed, and the preserved source filename. The proposal was not silently saved as a final item. The missing date remained empty and was explicitly flagged `לא זוהה תאריך`, proving that the importer did not apply the Trip-date default. Extraction accuracy remains poor: the proposed title is `for Ban Ta Khun`, provider is `Booking.com`, booking reference is incorrectly `CONFIRMATION`, and the location text is corrupted. Those values are retained as evidence for the dedicated Alpha 0.6 Smart Intake redesign; under the approved 0.5.5 scope they do not create a new defect. The Product Owner clarified that opening the original PDF from the source-name display is not part of the frozen experimental boundary, so the prior proposed blocker is withdrawn and no `V5-F26` is opened. The Product Owner then confirmed that rejecting the disposable PDF proposal removed it from the active area, placed it exactly once in the rejected-proposal archive and retained the source exactly once under Sources. This closes the PDF cancellation/rejection safety criterion. The missing corrective `--no-build` deployment output remains an exact-artifact evidence gap that must be resolved before the final GO, but it is not classified as an Alpha 0.5.5 product failure.

Focused acceptance step 6 is complete. Its Cancel branch passed immediate editor reset, persistence and singularity. Its controlled Proceed/save branch produced exactly one out-of-range item with the expected marker, and the Product Owner subsequently restored the test item to its original in-range dates. Exactly one item remains with the original values and no `מחוץ לטווח` marker, as expected. During the interval characterization, the Product Owner identified an additional case: when an item's start date remains inside the configured Trip range but its end date extends after the Trip end, the item is not marked `מחוץ לטווח`. The expected contract is interval-based: a dated range is outside whenever either boundary lies outside the Trip, so a valid in-range start cannot mask an out-of-range end. The current implementation evidence is consistent with the visible failure because the marker predicate evaluates `startAt` against the Trip boundaries but omits `endAt`. Closing and reopening FamilyTrips did not restore the marker. This confirms `V5-F27` as a persisted, release-blocking correctness defect rather than a transient rendering state. It remains distinct from and does not invalidate the passed Cancel-state correction in `V5-F21`, and remains open until correction and focused Product Owner retest.

| Check | Result | Evidence / disposition |
|---|---|---|
| Correction installation | PASS | Installer recognized the Alpha 0.5.4 NO-GO handoff and reported `INSTALLATION PASSED`. Installed version is `0.5.5`; source handoff is `0.5.4`; branch is `version/V5`. |
| Automated suites and Production build | PASS | Syntax checks, all ten ordered V4/V5 suites and Vite 7.3.6 Production build passed. The installer reported zero dependency vulnerabilities. |
| Local recovery point | RECORDED | `C:\Projects\FamilyTrips\family-trips-backups\v5-0.5.5-correction-20260812-130305`. |
| Fixed-QA Draft deployment | PASS / PROCESS CORRECTION REQUIRED | `https://qa--familytripz.netlify.app` was deployed successfully as a Draft with deploy ID `6a7c463d69ebf06b430da88a`; no `--prod` operation occurred. Netlify nevertheless ran `npm run build` because `--no-build` was omitted. The installer had produced `dist/assets/index-C-x8FSBM.js` (413.49 kB), while the deployment-time build produced `dist/assets/index-C1PUUAZ0.js` (413.47 kB). The deployment is not treated as the exact installer-tested artifact. |
| Displayed version on invalid artifact | PASS — IDENTITY ONLY | Screenshot evidence shows `Alpha 0.5.5` on the fixed-QA login screen. This does not complete the online baseline because authentication and Trip/data load did not pass on this artifact. |
| Google sign-in on invalid artifact | BLOCKED — DEPLOYMENT ARTIFACT | The login screen reports failure. Console evidence from `index-C1PUUAZ0.js` shows Firebase `auth/api-key-not-valid`; Identity Toolkit and Google APIs return HTTP 400 with `API_KEY_INVALID`. This is not classified as a Google flow regression or a new Alpha 0.5.5 product finding. The separate Tracking Prevention notice is browser noise for this diagnosis. |
| Exact-build recovery | PASS | The Product Owner reran `npm run check`. Syntax checks, all ten ordered automated suites and Vite 7.3.6 Production build passed. Vite transformed 33 modules in 2.28 seconds and recreated `dist/assets/index-C-x8FSBM.js` at 413.49 kB, matching the expected locally tested artifact identity. |
| Corrective fixed-QA redeployment | EVIDENCE GAP — DEFERRED TO 0.5.6 | The output from `netlify deploy --dir dist --alias qa --no-build` was not supplied, although functional QA was completed on fixed QA. Because `V5-F27` independently blocks 0.5.5, do not redeploy this known-failing build merely to close the evidence gap. Require `--no-build` output for the corrected 0.5.6 QA artifact before its GO decision. |
| Fixed-QA online baseline | PASS — DESKTOP AND PHONE | Product Owner confirmed that fixed QA displays `Alpha 0.5.5`, Google sign-in succeeds and the existing Trip/data loads on desktop and phone. No authentication product finding is opened. |
| Focused acceptance step 2 — Source-menu boundary | PASS | Product Owner phone screenshot shows exactly `ידני` (Manual), `PDF ניסיוני` (Experimental PDF), `JSON` and `QR`. No independent URL/link intake, image-upload or camera-photo option is visible. The `קישור` field remains inside the Manual item form, correctly preserving an optional URL owned by the meaningful item rather than exposing URL as an intake source. |
| Focused acceptance step 3 — Manual item-owned URL save/open/cleanup | PASS / COMPLETE | Product Owner phone screenshot shows one saved Activity titled `בדיקת קישור ידני 0.5.5` with its `קישור` action rendered inside the item. Tapping the action opened the intended EL AL website. Returning to FamilyTrips preserved the same single record, and deleting it removed the disposable test Activity successfully. The complete evidence confirms that the URL belongs to the meaningful Activity, no standalone Link was created and no test item remains. |
| Step 4 controlled QR fixture — observation | PASS / COMPLETE | Three Product Owner phone screenshots show exactly one proposal on the `בדיקת הצעה` page. Its title visibly begins with singular `בדיקת קישור מתוך`, matching the controlled fixture; its date is 19 January 2027; its item-owned website is the intended EL AL URL; and its source is `qa-v5-0-5-5-item-owned-url.json`. The `אישור ויצירת פריט`, `אחר כך` and `דחייה` actions prove that it remains review-stage and that no final item was created automatically. The earlier `בדיקות` report is classified as a reporting typo, so no title-mutation finding is opened. |
| Step 4 controlled QR fixture — rejection, archive placement and persistence | PASS / COMPLETE | Product Owner confirmed that the controlled proposal moved to the rejected-proposal archive. Screenshot evidence shows Sources and Add before the archive. After one refresh/reopen, the proposal remained only in the archive and did not return to active review; its source `qa-v5-0-5-5-item-owned-url.json` remained exactly once under Sources by design as separate provenance. No residual final item, duplicate proposal or active-count contamination was reported. Keep both records until cleanup is explicitly requested. |
| Step 5 Experimental PDF — warning/source/review/date safety | PASS / COMPLETE | Product Owner screenshots show the explicit experimental badge and warnings, preserved filename `Confirmation_for_Booking_ Ban Ta Khun.pdf`, and a review-stage proposal rather than a silently saved final item. The unidentified date remains empty and is explicitly flagged instead of receiving a Trip-date default. The inaccurate title/reference/location are redesign evidence, not a new 0.5.5 finding under this safety-only boundary. In-app original-PDF opening is not an Alpha 0.5.5 requirement. |
| Step 5 Experimental PDF — safe rejection | PASS / COMPLETE | Product Owner confirmed that the disposable PDF proposal left the active area after one rejection, appeared exactly once in the rejected-proposal archive and retained its separate source exactly once under Sources. No final item, duplicate proposal or active proposal remains. Keep the source and archived proposal until cleanup is explicitly requested. |
| Step 6 out-of-range Cancel — immediate reset, persistence and singularity | PASS / COMPLETE | Product Owner confirmed that immediately after cancelling the warning, the still-open editor displayed the original persisted date rather than the attempted out-of-range value. Closing without saving and reopening preserved the original date on exactly one unchanged item. No hidden save, duplicate or collateral change was reported. |
| Step 6 out-of-range Proceed — save, marker and restoration | PASS / COMPLETE | Product Owner confirmed that after changing the same item to one day after the Trip end, saving and confirming the warning, exactly one item was displayed with the out-of-range date and marker. The controlled item was then restored to its original in-range date state; exactly one item remains with the original values and no marker, with no unrelated change reported. |
| `V5-F27` — end date outside while start remains inside | CONFIRMED FAIL / OPEN | Product Owner observed that a range item with an in-range start and an end after the Trip end has no `מחוץ לטווח` marker. Closing and reopening FamilyTrips without editing the item did not restore the marker, proving that the failure persists after re-render. This violates the established start/end range contract and is release-blocking. Code evidence indicates the marker predicate currently checks `startAt` only. Keep `V5-F27` open until a corrected build passes focused Product Owner retest; do not conflate it with the passed `V5-F21` Cancel/editor-reset branch. |
| Exact next activity | 0.5.6 CORRECTION PLANNING | Stop testing and deployment of Alpha 0.5.5. Implement only the focused `V5-F27` correction in Alpha 0.5.6, preserve all accepted 0.5.5 behavior, build and validate it once, deploy that untouched `dist` to fixed QA with `netlify deploy --dir dist --alias qa --no-build`, and retest only the affected range-boundary behavior plus the smallest relevant regression. |

Final Alpha 0.5.5 decision: **NO-GO / acceptance stopped**. Do not repeat accepted tests or deploy 0.5.5 again. Alpha 0.4.3 remains Production until a corrected 0.5.6 candidate completes focused acceptance and receives explicit GO.

## Alpha 0.5.6 focused `V5-F27` correction

### Frozen implementation

- `isItemOutsideTrip(item, trip)` evaluates every present dated boundary against the inclusive Trip range.
- Both the visible persisted marker and pre-save confirmation call that same predicate.
- Range items with an in-range start and an end after the Trip end are marked `מחוץ לטווח`.
- Range items with a start before the Trip and an in-range end remain marked; items with both boundaries inside remain unmarked.
- `entire` and `none` schedules remain excluded, and missing Trip boundaries do not create a conflict.
- All accepted 0.5.5 intake, PDF, Cancel, archive, Offline, QR and Timeline behavior is unchanged.

Technical result on 2026-08-12: clean dependency installation passed; all syntax checks and eleven ordered automated suites passed; Vite 7.3.6 transformed 33 modules and completed the Production build in 1.67 seconds; `npm audit --audit-level=low` reported zero vulnerabilities. These results do not close `V5-F27`.

### Focused Product Owner acceptance

1. Install Alpha 0.5.6 over the installed 0.5.5 NO-GO handoff and confirm `INSTALLATION PASSED`, installed version `0.5.6`, source handoff `0.5.5` and branch `version/V5`.
2. Deploy the exact locally tested `dist` with `netlify deploy --dir dist --alias qa --no-build`; retain the output and confirm displayed `Alpha 0.5.6`, Google sign-in and existing Trip/data.
3. On one stable range item, keep the start inside the Trip and set the end after the Trip end. Save/confirm and verify exactly one item with `מחוץ לטווח` in the list and Detail.
4. Close and reopen FamilyTrips. Verify the same single item still has the marker.
5. Restore both boundaries inside the Trip. Verify the marker disappears and the same single item persists.
6. Perform only the smallest complementary boundary smoke: start before Trip/end inside is marked; both inside is not marked. Restore the test data.
7. Issue GO only after all steps pass. Do not repeat the accepted 0.5.5 matrices. Do not promote to Production or begin Alpha 0.6 before explicit Product Owner GO.
