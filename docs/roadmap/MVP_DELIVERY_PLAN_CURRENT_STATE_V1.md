# Family Trips — Thailand MVP Delivery Plan — Current State

**Document revision:** `V66`
**Status:** **`0.6.6` implementation complete, awaiting Product Owner install and acceptance testing.** `V6-F52` (stale hardcoded sign-in version), `V6-F53` (FL-003 flight-direction non-determinism) and `V6-F54` (a warning on a saved item can never be cleared, by any means, once the item exists — the same root cause behind both the persisting date warnings and the persisting merge-conflict warnings Eyal observed on the `H-001`/Panvaree Resort item) are all implemented and unit-tested on `version/V6`. None is recorded `Fixed` until Eyal retests against a QA deploy carrying the fix, per the standard's responsibility split. See the "`0.6.6` QA acceptance findings" section below for full detail. Production remains on `0.6.5` (build `0.6.5-a915a440`, deploy `6a9464b101a1690008db8823`) — **`0.6.6` has not been deployed anywhere, per its candidate definition Production stays blocked until explicit Product Owner GO after acceptance.** The Development Agent implemented both halves of the combined scope on `version/V6`: the Attraction/Event Smart Import extraction adapter/schema/prompt/URL-acquisition path (`ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` `V4`), and the three folded-in `0.6.5` Production findings (`V6-F49`–`V6-F51`). All seven `AE-001`–`AE-007` benchmark cases (including the mandatory wild-card `AE-001` and the `AE-003`/`AE-004` companion-pair decision) and all three findings now have named, passing automated regression tests (`tests/v6-6-attraction-event-import.mjs`, `tests/v6-49-51-correction.mjs`) — per the standard's V23 rule, `V6-F49`–`V6-F51` are recorded `Fixed` below. The full quality gate (`npm run check`: syntax, every regression test, production build) passes. **One explicitly accepted, non-blocking gap carried forward exactly as the discovery document named it:** no official venue/museum/OTA URL has been tested end-to-end against the live model — this remains a named step in the executable acceptance session (`EXECUTABLE_TEST_SESSION_V184.md`) for the Product Owner to run, not something the Development Agent can close from here (no live API access, and the standard's own responsibility split reserves Product Owner acceptance testing to the Product Owner).

**Product intent:** FamilyTrips is built for the Product Owner's own family trips, but it must support sharing with friends who create and use their own independently-owned Trip in parallel — not a demo, not a stripped-down mode, a first-class Trip with the same sharing and sync contract. This is confirmed, non-negotiable MVP scope, scheduled in Alpha 0.7 (see "Shared Trips, roles and access" below); Alpha 0.6 (Smart Import) completes first.
**Scope:** Thailand trip only
**Purpose:** Define the remaining Alpha versions required to complete the MVP, followed by Beta and Release Candidate stages. This document holds the current, forward-looking plan and the live status of in-progress work only. Alpha 0.1 through Alpha 0.5.6 are fully delivered; their specifications, every embedded QA checkpoint, acceptance register and handoff narrative are preserved verbatim in `MVP_DELIVERY_HISTORY_V1.md` and are not repeated here.

## Change history (this document)

| Document revision | Date | Product phase | Change |
|---|---|---|---|
| V66 | 2026-09-04 | `0.6.6.2` QA retest — 5/5 confirmed Fixed, 2 new minor findings registered | Eyal's QA retest of `0.6.6.2` (build `0.6.6.2-2e27ced6`, commit `2e27ced` — supersedes `74628d8`, the originally-implemented commit, which the deploying session caught was still labeled `0.6.6.1` in `package.json` and corrected before deploying so the build identifier wouldn't be confused with the already-tested `0.6.6.1`). **All five scoped items confirmed Fixed, verified:** `V6-F55`'s extension (Panvaree Resort's four previously-permanently-stuck warnings now each show a dismiss control that clears them), `V6-F59` (a fresh `AE-006` reimport now shows the combined "WaterLand - Entry Ticket" title), `V6-F61` (a fresh `AE-005` reimport shows no more literal doubled labels, for the scope this fix targeted), `V6-F65`'s extension (a fresh `AE-007` reimport returns a confident provider name Eyal confirmed is genuinely correct against the source, for the scope this fix targeted), `V6-F66` (repeating the original wild-card two-document attach test, both people's ticket-holder data now survives the merge). **Two new minor findings registered, both discovered incidentally during this retest, neither investigated yet:** `V6-F67` (Low) — a related but distinct residual case under `V6-F61`'s general problem area: a translated canonical label followed by a differently-worded, same-meaning source-embedded prefix ("מזמין: הוזמן על ידי: אייל בן יצחק") reads as redundant, which the exact-literal-string duplicate detection `V6-F61` shipped was never designed to catch. `V6-F68` (Low, pending investigation) — a one-character name misread ("אילי" instead of "אייל") in `AE-007`'s ticket-holder table, root cause not yet determined (could be a live-model transcription inconsistency on this one source, or a genuine adapter/normalization gap). Neither new finding has a target version yet — awaiting Product Owner decision on packaging, per the standard's own target-version-assignment discipline. |
| V65 | 2026-09-04 | `0.6.6.2` implemented on `version/V6` | Implements the four items Eyal decided to package together after the `0.6.6.1` QA retest (`V64`): `V6-F55`'s open sub-issue (extended, not a new finding), `V6-F59`, `V6-F61` (shared-mechanism fix), `V6-F65` (extended), plus registers and implements the new `V6-F66` (High). **`V6-F55` extension:** root-caused (by code reading, not live-repro against the exact stored item — stated honestly rather than overclaimed) `isDismissibleWarning`'s old text-substring heuristic (`needsReviewLabels.some(label=>text.includes(label))`) as the actual mechanism behind Panvaree Resort's four never-clearing warnings — a free-text warning merely mentioning a common label word (e.g. "טלפון") collided with an unrelated live field entry and was misclassified as field-backed, so it could clear by neither means. Replaced with an explicit `needsReviewRef:{key,value}` a warning carries only when genuinely generated from a `needsReviewFields` entry (`src/smart-import-adapter.js`, `src/activity-import-adapter.js`); `isDismissibleWarning` moved to `src/ingestion.js` (pure logic, directly testable — v5-app.js's CSS import makes it unimportable from plain Node) and now checks that reference by exact key+value match; `reconcileStaleNeedsReview` (`src/ingestion.js`) clears a `needsReviewRef`-tagged warning by exact key match, keeping the old label-substring fallback for any warning shape not yet migrated. **`V6-F59`:** `activitySystemPrompt`'s `activityName` paragraph (`netlify/functions/_shared/smart-import-schema.mjs`) rewritten from a strict either/or preference to a combination rule — report venue name AND ticket-type label together ("WaterLand - Day Entry"), falling back to whichever one the source actually shows when only one is present; adapter unchanged (verified still a pure `title:activityName` passthrough). **`V6-F61`:** `normalizedGenericLabel`/`stripDuplicateLabelPrefix` moved out of `activity-import-adapter.js` into `src/operational-data.js` as the one shared implementation (dictionary extended with `payment status`/`booked by`/`ticket number`), applied at all three previously-untouched note-line sites — Hotel's `otherFields` mapping and both adapters' `importantNotes` mapping (`src/smart-import-adapter.js`, `src/activity-import-adapter.js`) — Activity's `otherFields` site repointed at the shared import, unchanged in behavior. **`V6-F65` extension:** `activityImportSchema` gained paired `activityNameCertainty`/`providerCertainty` enums (`exact`/`needs_review`/`unreadable`), giving the model a structural way to say "visible but illegible" (e.g. a small stylized logo) instead of guessing a plausible name; `activitySystemPrompt` gained a paragraph explaining the new fields; `activity-import-adapter.js` blanks the value and flags a `needsReviewFields`/`fieldConfidence` entry on `unreadable` (defense in depth — forces blank even if the model still returns a guess, proven by a fixture simulating exactly that noncompliance) and flags-but-keeps the value on `needs_review`. **`V6-F66` (new, High, registered and implemented in the same pass):** `mergeActivityTicketHolders`/`mergeActivityDetails` added to `src/activity-import-adapter.js`, mirroring Flight's already-solved `mergeFlightPassengers`/`mergeFlightDetails` (match by `ticketNumber` else normalized name, union both sides, existing non-blank field wins, blanks filled from the incoming side); `suggestionToItem` (`src/ingestion.js`) now branches on `type==='activity'` the same way it already does for `type==='flight'`, and the top-level `participants` array is unioned (deduped) for `activity` specifically, leaving every other type's existing either/or behavior untouched. Ten new named regression tests added across `tests/v6-55-correction.mjs` (4), `tests/v6-6-attraction-event-import.mjs` (12, spanning `F59`/`F61`/`F65`-ext/`F66`), `tests/v6-correction.mjs` (2, Hotel `F61`); several pre-existing tests updated where they encoded the now-superseded text-substring/single-adapter behavior as expected (`tests/v6-55-correction.mjs`'s own `V6-F55` plain-string test, `tests/v6-49-51-correction.mjs`'s `isDismissibleWarning` source-inspection pointer, `tests/v6-correction.mjs`'s `V6-F13` warning-shape assertions). **Quality gate:** every `node --check` syntax check and the complete automated test suite (all 18 files) pass. The production build step (`npm run build`) could not be independently re-verified in this session's sandboxed device VM — its `node_modules` was populated by a Windows-side `npm install`, which never fetches the Linux-native `@rollup/rollup-linux-x64-gnu` optional dependency, and this VM's network egress does not allow fetching it (403 from the npm registry proxy); confirmed by direct inspection that this is a pre-existing environment/platform mismatch unrelated to this change (the same failure mode, verified independent of any file touched here). Flagged honestly rather than claimed as passing. Register updated below: `V6-F55`'s open sub-issue, `V6-F59`, `V6-F61` and `V6-F65` move to **Implemented** (not `Fixed` — pending Eyal's QA retest, same discipline as every finding this session); `V6-F66` registered as **Implemented** directly (High severity, per `V64`'s own assessment). Committed to `version/V6`, not deployed — deployment is a separate, explicitly-authorized step. |
| V64 | 2026-09-03 | `0.6.6.1` QA retest — 7 findings confirmed Fixed, 3 reopened with new evidence, 1 new finding registered | Eyal's QA retest of `0.6.6.1` (build `0.6.6.1-31656f60`, commit `31656f6`). **Confirmed Fixed, verified** (real re-import/re-merge evidence, see each row): `V6-F56`, `V6-F57`/`V6-F58`, `V6-F60`, `V6-F62`, `V6-F63`, `V6-F64`. `V6-F55`'s core picker mechanism (the Product Owner's own requested fix direction) confirmed genuinely working across two independent real scenarios (`AE-003`/`AE-004` attach-merge, a Panvaree Resort re-merge) — not marked `Fixed` because a new open sub-issue surfaced under the same finding (see its row) that must be understood before the finding can close. **Reopened with new evidence, root-caused, not fixed** (awaiting Product Owner decision on a follow-up package): `V6-F59` (the "WaterLand" bug is gone, but Eyal wants the title to combine venue name **and** ticket type, e.g. "WaterLand - Day Entry," not either alone — the current prompt fix was written as a strict either/or preference, not a combination rule), `V6-F61` (the `AE-002` purchaser fix only patched `activity-import-adapter.js`'s `otherFields` path; the identical unfixed pattern exists in `smart-import-adapter.js` (Hotel) and in BOTH adapters' separate, entirely untouched `importantNotes` mapping — a real, single-field patch rather than the shared-mechanism fix requested), `V6-F65` (the known-brand-substitution fabrication is gone, but a fresh `AE-007` reimport produced a different specific organization name for `provider`, attributed by the model's own warning text to "identified from the logo" — Eyal confirmed against the real source that this text does not appear on it; root cause: `provider`/`activityName` are bare top-level strings in `activityImportSchema` with no paired certainty field and no `fields[]` vocabulary slot either, so the model has no structured way to express "I can see something here but can't confirm what it says" — it can only output a confident-looking string or leave it empty). **New finding registered**, `V6-F66` (High severity) — participant/ticket-holder data loss when attach-and-extract merges a second per-person Activity document onto an existing item, discovered via this retest's mandatory wild-card test (two real per-person match tickets). Full detail, root cause and evidence for all four investigated items in their own rows below; none implemented yet, per explicit instruction — Eyal decides on packaging a follow-up correction after reviewing. |
| V63 | 2026-09-03 | `0.6.6.1` implemented on `version/V6`, full quality gate passes | All eleven scoped findings (`V6-F55`\[merged with `V6-F54`\], `V6-F56`–`V6-F65`) implemented per `FAMILY_TRIPS_ALPHA_0_6_6_1_CORRECTION_SCOPE_V1.md`. Group A (Activity extraction/prompt): `V6-F56` — `dni` added as a real property to `ticketHolderSchema` (`netlify/functions/_shared/smart-import-schema.mjs`) and mapped through in `src/activity-import-adapter.js`; `V6-F59`/`V6-F63`/`V6-F64`/`V6-F65` — `activitySystemPrompt` gained explicit priority/combination rules for `activityName` (ticket-type label over bare venue name), `provider` (venue/organizer over ticketing platform, with the `AE-002` platform-is-provider case still honored), `location` (combine venue with a stated sub-venue), and a concrete negative-example paragraph against upgrading a generic source label to a specific real-world name. Group B (Activity rendering): `V6-F57`/`V6-F58` — new `activitySummaryBlock`/`activityTicketHoldersTable` in `src/v5-app.js` (mirroring `flightSummaryBlock`/`flightPassengersTable`), wired into both `suggestionView()` and `detailView()`; three missing assertions added to existing `tests/v6-6-attraction-event-import.mjs` cases (`AE-002`/`AE-004` `seatOrSection`, `AE-005` `amount`). `V6-F60` — `fmt()` gained a `precision` parameter; date-only values (`dateMeta.start/endPrecision==='date'`) no longer render the synthetic `12:00` default as if it were a real extracted time. `V6-F61` — `activity-import-adapter.js` gained `normalizedGenericLabel`/`stripDuplicateLabelPrefix`, closing the doubled/English "Purchaser" label. Group C: `V6-F62` — `detailView()`'s attach-document control now includes `activity` alongside `hotel`/`flight`. Standalone: `V6-F55` (merged `V6-F54`) — new `resolveMergeCandidate` (`src/ingestion.js`) and `mergeCandidatePanel` (`src/v5-app.js`) give an explicit keep/accept picker for a merge conflict, reusing the `directionPickerPanel` pattern, per the Product Owner's own requested fix direction; `isDismissibleWarning` generalized so any plain-string warning not currently backed by a live `needsReviewFields` label is dismissible by default, closing the "third category, never clearable" gap for `draft.warnings`/the place-not-validated notice across every Smart Import vertical without needing to wrap each generation site individually; `NEEDS_REVIEW_FIELD_MAP` gained a `location` entry and `smart-import-adapter.js`'s `FIELD_MAP` gained the address-related key mappings `place-validation.mjs` already recognized, closing a second, independent path by which a needs-review warning could survive editing every associated field. Full quality gate passes: all `node --check` syntax checks, the complete automated test suite (two new files — `tests/v6-55-correction.mjs` — plus updated assertions in `tests/v6-6-attraction-event-import.mjs`/`tests/v6-49-51-correction.mjs`/`tests/v6-54-correction.mjs` where the underlying mechanism's signature changed), and the production build (`npm run build`, identifier `0.6.6.1-e9f794ca`). Register updated below: all eleven findings move from `Assigned — target 0.6.6.1` to **Implemented**, each with a named regression test — none recorded `Fixed` yet, per the standard's responsibility split; that requires Eyal's own QA retest against a deployed `0.6.6.1` build. Committed to `version/V6`, not deployed — deployment is a separate, explicitly-authorized step. |
| V62 | 2026-09-03 | `0.6.6.1` correction package scoped — target-version assignment complete for the first `0.6.6` acceptance session | Product Owner confirmed target-version assignment, one finding at a time, for every open finding from the first `0.6.6` acceptance session. `V6-F52`/`V6-F53` retested directly by Eyal and confirmed **Fixed, verified** (sign-in/tab-title/About version labels now consistently read Alpha 0.6.6 with build hash; `FL-003`'s two internal extraction calls genuinely disagreed and the `directionAmbiguous` picker correctly appeared and was resolved). `V6-F54` **merged into `V6-F55`** rather than closed separately — Eyal's retest found no dismiss control on either the Panvaree Resort or Panan Krabi warnings and surfaced two new, worse symptoms folded into `V6-F55`'s entry below: a stale/misleading warning describing an already-correctly-resolved conflict (Panvaree, post-merge), and a warning that persists even after editing every field associated with it (Panan Krabi) — Product Owner elevated this combined finding to severity **High** as a cross-cutting warning-lifecycle defect (Hotel and other item types, not Activity-specific), with a proposed fix direction (an explicit conflict-resolution picker or trip-date-based proposal with confirmation, reusing the existing `directionAmbiguous` picker pattern rather than "silent-keep + indefinite warning") to guide, not dictate, implementation. Eleven finding IDs total (`V6-F55`\[merged\], `V6-F56`–`V6-F65`) moved from `TBD after acceptance completion` to **Assigned — target `0.6.6.1`**, grouped into three root-cause groups (Activity extraction/prompt defects; Activity detail-view rendering gaps; missing attach-capability) plus the standalone warning-lifecycle finding — see `FAMILY_TRIPS_ALPHA_0_6_6_1_CORRECTION_SCOPE_V1.md` for the full per-finding fix specification. `V6-F64`'s severity was elevated from an initial Medium proposal to **High** at Product Owner request — location accuracy is one of Decision 4's explicit no-omission blockers for the `activity` type, not merely required-when-present, so `AE-004`'s venue-dropping defect fails a hard project rule rather than a nicety; recorded here for an honest audit trail rather than silently overwriting the earlier severity. Naming decision: this correction is **`0.6.6.1`**, not `0.6.7` (`0.6.7` is reserved for the separate Insurance feature track) — patch-style naming, mirroring the `0.5.0`→`0.5.1` V5-lifecycle pattern rather than the sequential `0.6.x` pattern used so far; recorded as a new "For V6" entry in `VERSION_PACKAGE_AUTHORING_STANDARD_V30.md`. No implementation yet — scope-authoring and register-update only, per explicit instruction. |
| V61 | 2026-09-03 | Session checkpoint: `V6-F59`–`V6-F65` registered, first `0.6.6` acceptance session audited | Closed out all outstanding investigation prompts from the first `0.6.6` acceptance-testing pass (`docs/claude-code-prompts/0_6_6_1/`, 5 files) per a consolidated review-and-audit handoff. No fix implemented for anything — investigation and registration only, same discipline as every finding this session. Four investigations completed: (1) Follow-up 3 of the `V6-F56`/`V6-F57` thread — `AE-006` confirmed as a fifth instance of `V6-F57`'s display-gap pattern (folded into that finding's scope, not a new ID); plus two genuinely new, unrelated `AE-006` issues, both registered: `V6-F59` (title shows venue name "WaterLand" instead of frozen "כניסה לפארק יום") and `V6-F60` (the "12:00" shown for `AE-006`'s date-only value is a confirmed, code-proven UI default — `src/activity-import-adapter.js`'s `dateTime()` — not a model fabrication, but indistinguishable from a real time anywhere in the app, a real display gap in its own right). (2) `AE-002`'s "Purchaser: Purchaser: <name>" duplicate/English-only label investigated and registered as `V6-F61` — root cause traced to the same "no canonical field slot" gap class as `V6-F56`'s DNI finding. (3) `AE-003`/`AE-004` companion-pair issues investigated: Eyal's own hypothesis (no attach-existing-document control for `activity` items) confirmed directly from code and registered as `V6-F62`, established as the primary root cause superseding the initial duplicate-detection-regression framing (that logic is real, tested, and unrelated); the `provider`/`location` field defects registered separately as `V6-F63`/`V6-F64`, and shown to be the reason the existing dedup logic's exact-string match never had a chance to fire even via the global-Add path. (4) `AE-007`'s venue-name question investigated by direct inspection of the actual source image (`tests/fixtures/attraction_benchmark/AE-007_zoo_ticket_screenshot.png`, cropped/rotated/magnified via PowerShell `System.Drawing` for legibility) — both independently legible plain-text occurrences on the source (ticket body, PDF-viewer filename bar) consistently show only the generic "גן החיות" label, and code inspection of `place-validation.mjs` rules out Google Places lookup as a possible source of the more specific name in either field; registered as `V6-F65`, High severity, with an explicit, honest caveat that the source's one small stylized decorative logo graphic could not be read with full confidence at the image's native resolution despite repeated magnification attempts. Full audit of the entire first `0.6.6` acceptance session's findings (`V6-F49`–`V6-F65`) confirms the register is internally consistent — see the dedicated audit note below the QA acceptance findings table for the full report. |
| V60 | 2026-09-03 | `V6-F56`/`V6-F57` follow-up investigation; `V6-F58` registered (test coverage gap) | Investigated a second handoff prompt (`docs/claude-code-prompts/claude_code_prompt_v6f56_f57_followup.md`, same untracked-in-repo pattern as the first) asking two things, no fix authored for any finding. **Follow-up 1 (does `V6-F57` extend to `AE-002`/`AE-004`?):** confirmed plainly, not merely inferred — `activity-import-adapter.js:44-52`'s `ticketHolders` mapping is a single unconditional `.map()` over the array with no branching on case, ticket count, or single- vs multi-ticket source, and the confirmed-absent `v5-app.js` rendering path (`detailView()`/`proposalFields()`/`smartEvidencePanel()`) is likewise unconditional — so `V6-F57`'s scope is confirmed to cover `AE-001`+`AE-002`+`AE-004` uniformly, same root cause, no case-specific behavior found anywhere in either the capture or the display path. **Follow-up 2 (`AE-005`'s open items, checked via the existing `tests/v6-6-attraction-event-import.mjs` suite instead of live DB access):** ran the suite — currently **10/10 passing**. It does assert `AE-005`'s core precision test (`ticketNumbers` equals `2ZTN-G53R-GJ81P`, `assert.notEqual` against `confirmationNumber` — lines 185-186), so the order-number/ticket-number distinction is verified at the adapter level (not the live model — the suite's own header comment states it cannot exercise the live model, only the fixture-to-adapter mapping). It does **not** assert `AE-005`'s `amount` at all (fixture includes `f('amount','Price','120')` but no corresponding assertion, unlike `AE-004`/`AE-007` which do assert `amount`, and `AE-002`/`AE-003`/`AE-006` which assert it's correctly empty) — a real, confirmed test-coverage gap, registered as `V6-F58` below. It also does not assert `AE-002`'s or `AE-004`'s `ticketHolders[].seatOrSection` values, despite both fixtures including them and both cases' frozen ground truth explicitly naming seat/row/gate data — the same coverage gap, same finding. Separately, while tracing why `AE-005`'s `amount` is invisible on the saved item, found the root cause is broader than `AE-005`-specific: `detailView()` (`src/v5-app.js:150`) never renders `details.amount`/`details.currency` for any activity item at all (confirmed by reading its full render list — provider/confirmationNumber/location/startAt/endAt/notes/warnings/sources only) — the same "captured but never displayed" pattern `V6-F57` already named, just extended to cover `amount`/`currency` in addition to the ticket-holder fields; `V6-F57`'s entry updated accordingly rather than opening a fourth finding for the identical root cause. |
| V59 | 2026-09-02 | `V6-F56`/`V6-F57` registered — `AE-001` item 5 (per-passenger splitting) investigation | Investigated a task handed off via `docs/claude-code-prompts/claude_code_prompt_ae001_item5_investigation.md` (found directly in the repository working tree, untracked — no commit history, not authored by this Development Agent session): Eyal's `AE-001` retest (QA `0.6.6`, build `0.6.6-9b5b1ed0`) passed items 1–4/6/7 of the standard checklist but item 5 (per-passenger splitting: both DNI-holders, seats, gates) showed no structured per-passenger breakout in the item detail UI. Investigated by full code-path tracing of the Activity extraction schema, adapter and every rendering location (`netlify/functions/_shared/smart-import-schema.mjs`, `src/activity-import-adapter.js`, `src/v5-app.js`); **could not directly inspect the live saved Firestore item record** — this environment has no Firebase/Firestore access tool (no `firebase` CLI installed) and the Netlify Blobs audit store (`familytrips-smart-import-control`) only persists request metadata (`attemptId`, hashes, status, cost, latency — confirmed by reading `writeAudit()` directly), never the extracted draft itself — flagged explicitly rather than assumed. Found this is **not one uniform scenario but two, split by field, each registered separately**: `V6-F56` (Scenario B, real defect) — the `ticketHolderSchema` OpenAI structured-output schema has no field for a national ID/DNI at all and enforces `additionalProperties:false`, so a DNI value can never reach `details.ticketHolders` no matter how well the model reads the source, directly contradicting `AE-001`'s frozen ground truth (`ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` `V4`, line 100–101) which requires DNI per passenger; this is provable from the schema definition alone, independent of the live item's actual content. `V6-F57` (Scenario A, display-only, low severity) — `details.ticketHolders`/`ticketNumbers`/`ticketQuantity` (name, seat/gate/row/section, per-ticket number) are correctly captured into the suggestion by `smartImportActivityResultToSuggestion` when the schema allows it, but there is zero rendering code anywhere in `src/v5-app.js` for any of the three fields — confirmed via repository-wide search, zero matches — on both the suggestion review screen (`proposalFields()` only exposes a flat, comma-joined `participants` text input) and the saved item detail view; if the live item's data is actually present this is a pure UI gap, consistent with Eyal's own description ("only the source PDF is attached as a reference document"). Not fixed, investigation and registration only, per explicit instruction. Does not block continuing `0.6.6` Attraction/Event acceptance testing. |
| V58 | 2026-09-02 | `V6-F55` registered from `V6-F54`'s QA retest | Eyal's retest of `V6-F54` (build `0.6.6-9b5b1ed0`) surfaced a fourth finding: `V6-F55` — a third warning category (merge-conflict notices from `preserveTrustedFieldsOnMerge`, the place-not-validated notice, and model-authored `draft.warnings`) is a plain string that is neither `unresolved`-wrapped (`V6-F49`'s dismiss path) nor `needsReviewFields`-tracked (`V6-F54`'s edit-clear path), so it has never been clearable by any means, on a suggestion or an item, before or after either fix — confirmed by direct code inspection of `preserveTrustedFieldsOnMerge` and `smart-import-adapter.js`'s warning assembly. Investigated and root-caused only, not fixed, per explicit instruction. A second observation from the same retest (Panan Krabi, a warning that cleared only via a genuine value change, not by simply reopening the item) was assessed as very likely expected, by-design behavior — consistent with the tightened `V6-F49` prompt now producing a `needsReviewFields`-shaped result for this case, correctly explaining both the absence of a dismiss button and the edit-only clearing path — and is not registered as a finding, flagged instead as an assessment pending fuller confirmation. |
| V57 | 2026-09-02 | `V6-F54` implemented; two recovered ground-truth documents committed | `V6-F54` implemented on `version/V6`: a dismiss control added to `detailView()` for `isDismissibleWarning`-shaped entries (new `dismiss-item-warning` action, mirroring the pre-existing suggestion-only `dismiss-warning`); the item-edit save handler now computes a `changedFields` set and calls `reconcileStaleNeedsReview` before saving, the same way `updateSuggestion` already does for the pre-approval form. New test file `tests/v6-54-correction.mjs` (4 tests, including a direct functional proof that `reconcileStaleNeedsReview` works identically on a real item shape, not just a suggestion's `proposed` object); one pre-existing test corrected where it had encoded the old non-interactive rendering (`tests/v6-5-flight-import.mjs`'s `V6-F33` test). Full quality gate passes. Register updated: `V6-F54` now **Implemented**, not `Fixed`, same pattern as `V6-F52`/`V6-F53`. A related, non-defect enhancement idea (a one-click "accept new value" action for merge-conflict warnings) logged to the Beta 0.8 backlog alongside `V6-F14`/`V6-F16`, not registered as a finding. Also: `FLIGHT_SMART_IMPORT_DISCOVERY_V1.md` (`V2`) and `EXECUTABLE_TEST_SESSION_V169.md` committed verbatim to `docs/roadmap/` and `docs/versions/V6/` respectively, their real content recovered from the separate Project knowledge store per Eyal's confirmation — closing two of the three documentation gaps `V55` identified as never having been committed to this repository (`VERSION_PACKAGE_AUTHORING_STANDARD_V30.md` remains unrecovered; `SMART_IMPORT_HOTEL_BENCHMARK_CASE_H001_V3.md` is confirmed to not exist even in the knowledge store, and is accepted as a permanent gap since the existing `H-001` test fixtures already match `SMART_IMPORT_REDESIGN_DISCOVERY_V22.md`'s inline summary). No content fabricated; committed exactly as supplied. |
| V56 | 2026-09-02 | `V6-F54` registered — item warnings never clear | Third `0.6.6` QA acceptance finding, from Eyal saving the `H-001` (Panvaree Resort) Hotel item and separately attaching a companion document to it. Root-caused by direct code inspection (not inference): `detailView()` renders `item.warnings` as non-interactive text with no dismiss control, and the item-edit save handler never calls `reconcileStaleNeedsReview` or otherwise touches `item.warnings` — a repository-wide search confirms every `.warnings=` assignment in the codebase touches only `suggestion.warnings`, never an item's. Once a warning lands on a saved item, it is permanently stuck regardless of its origin (`needsReviewFields`, `unresolved`/dismissible, or the plain-string merge-conflict warnings from `preserveTrustedFieldsOnMerge`) and regardless of item type — this predates `V6-F49`/`V6-F50` and is not a regression they introduced; it explains both symptoms Eyal reported as one root cause. Two related points investigated and confirmed **not** defects: the missing website on Panvaree Resort (frozen `H-001` ground truth explicitly lists it as source-absent) and the merge-conflict "no one-click accept" behavior (the frozen preserve-trusted-value contract working as designed, `V6-F33`/`V6-F39`). Proposed fix (not implemented): a dismiss control on `detailView()` mirroring `suggestionView()`'s, plus extending the item-edit save handler to call `reconcileStaleNeedsReview` the same way the suggestion-edit handler already does — both reuse existing, already-tested mechanisms. Investigation and registration only, per explicit instruction; awaiting Product Owner decision alongside `V6-F52`/`V6-F53`. |
| V55 | 2026-09-02 | `V6-F52`/`V6-F53` implemented on `version/V6` | Both findings fixed and unit-tested: `V6-F52` — `appVersionLabel()` extracted from `v5-app.js` into a new shared module `src/app-version.js`, imported by both it and `src/main.js`'s sign-in screen, structurally preventing the two from diverging again; `V6-F53` — `runExtraction` (`netlify/functions/smart-import.mjs`) makes a second independent model call for any multi-segment flight source and cross-checks direction per segment (matched by flight number), routing a disagreement through the existing `directionAmbiguous`/`directionCandidates` picker rather than building new UI. New test file `tests/v6-52-53-correction.mjs` (6 tests); two pre-existing tests corrected where they had encoded the old hardcoded-version behavior as expected (`tests/v5-correction.mjs`, `tests/v6-5-flight-import.mjs`). Full quality gate passes. Neither finding is recorded `Fixed` — both await Eyal's QA retest, per the standard's responsibility split (same pattern as `V6-F49`–`V6-F51`). Not deployed to QA yet. Also: a broad documentation-sync investigation (requested after `V6-F52`/`V6-F53`'s registration surfaced three documents cited or expected to exist that could not be found — `VERSION_PACKAGE_AUTHORING_STANDARD_V30.md`, `FLIGHT_SMART_IMPORT_DISCOVERY_V1.md`, `EXECUTABLE_TEST_SESSION_V169.md`) found, via `git log --all --full-history`, that none of the three has ever been committed to this repository on any branch — `FLIGHT_SMART_IMPORT_DISCOVERY_V1.md` and `EXECUTABLE_TEST_SESSION_V169.md` have zero history anywhere; `VERSION_PACKAGE_AUTHORING_STANDARD` tops out at `V29` (this Development Agent's own sync commit). The `EXECUTABLE_TEST_SESSION_V169.md` citations in this very document were already present, unfulfilled, in `package_0_6_6`'s original handoff copy of this file (its `V52`), predating any `0.6.6` implementation work — confirming the gap originates upstream of the repository, in how a separate coordination/knowledge-store workspace hands off content to a Development Agent session, not in anything committed here. This document's own `V54` also diverged from a different `V54` reportedly read in that separate knowledge store (an "installer-reconciliation" entry) — full git history shows only one `V54` has ever existed in this repository (the `V6-F52`/`V6-F53` registration two revisions prior in this table), so this was not a revision collision inside the repo (that one `V54` is the row immediately below in this table); the other `V54` content, if real, was never committed here either. Full findings reported to the Product Owner; no content fabricated or assumed. |
| V54 | 2026-09-02 | `0.6.6` QA acceptance — two findings registered | Eyal's own `0.6.6` QA acceptance testing (build `0.6.6-3925bb20`) surfaced two real defects, investigated and registered per the standard's mandatory finding register: `V6-F52` (sign-in screen hardcodes a stale "Alpha 0.6.5" literal in `src/main.js`, disconnected from the build-injection mechanism every other version label uses — direct recurrence of the `V5-F05`/`V6-F31` defect class) and `V6-F53` (FL-003's flight direction/segment order differed between two immediate, unchanged re-imports, with the `directionAmbiguous`/`directionCandidates` picker built for exactly this case — `V6-F32`/`V6-F34`, commit `716f950` — never triggering; root-caused to the same live-model non-determinism that commit already documented and only partially mitigated, not a regression of the adapter/picker mechanism itself, which is unchanged and still passes every test). Two referenced documents (`FLIGHT_SMART_IMPORT_DISCOVERY_V1.md`, `EXECUTABLE_TEST_SESSION_V169.md`) could not be located anywhere in the repository or project directory tree despite being cited as authoritative records elsewhere in this document; flagged rather than assumed. Also note: `VERSION_PACKAGE_AUTHORING_STANDARD_V30.md` was referenced in the handoff for this work but does not exist anywhere either — the current standard remains `V29`; this revision continues to cite `V29`'s actual rules. Neither finding is fixed; both are investigation-and-registration only per explicit instruction, target `TBD after acceptance completion`. See "`0.6.6` QA acceptance findings" below. Separately, the QA-scoped `FAMILYTRIPS_ALLOWED_EMAILS` allowlist was investigated following an apparent contradiction between a stale, weeks-old memory of a denial and today's real evidence; a controlled empirical test (temporarily narrowing then restoring the QA allowlist, confirmed via real user attempts both times) proved the allowlist was correct all along — no finding registered, explicitly not a defect. |
| V53 | 2026-08-31 | `0.6.6` implementation complete on `version/V6`, awaiting Product Owner install/acceptance | Development Agent implemented the full combined `0.6.6` scope: (1) Attraction/Event Smart Import — `activityImportSchema`/`activitySystemPrompt` (`netlify/functions/_shared/smart-import-schema.mjs`), the `analyze_activity` operation and a public-URL acquisition path with a new distinct `robots_disallowed` safe-failure code (`netlify/functions/smart-import.mjs`), a generalized `validateActivityPlace` reusing Hotel's property-first place-validation gate (`netlify/functions/_shared/place-validation.mjs`), and a new `src/activity-import-adapter.js` mapping per-ticket-holder evidence into the existing `activity` item type — no data-model change, confirming the `V49` correction note. Implementation decisions made explicit and tested: `AE-003`/`AE-004` (Bloomfield companion pair) land as two separate items, surfaced to the Product Owner via the app's existing generic duplicate-detection dialog rather than silently merged or duplicated; `AE-006` (WaterLand, no per-person names) yields zero fabricated participants with all four physical tickets still individually represented via `details.ticketNumbers`/`details.ticketHolders`. (2) `V6-F49`–`V6-F51` — `NEEDS_REVIEW_FIELD_MAP` (`src/ingestion.js`) gained Hotel/Activity-shaped key entries it previously had none of at all, and `smart-import-adapter.js` now keys a needs-review entry by the resolved proposed-field name instead of the raw model key, so a manual field edit can actually match and clear it; an `unresolved`-sourced warning (no field key, can never auto-match) is now wrapped as a dismissible object with a manual dismiss control wired into the review screen; a date resolved only in `unresolved`/`importantNotes` free text now has a regex fallback that still reaches `startAt`/`endAt` (flagged `needs_review`); the tightened Hotel/Activity system prompts instruct the model to resolve a conflict into a field from the start rather than prose only; the Smart Import review screen now pre-fills a blank date with the Trip start date, mirroring `manualCreateDefaults`/`suggestionToItem`. All three findings **Fixed** below, each with a named regression test per the standard's V23 rule. Full quality gate (`npm run check`) passes: 8 pre-existing suites unchanged/still passing, 2 new suites added (`tests/v6-49-51-correction.mjs`, `tests/v6-6-attraction-event-import.mjs`), production build succeeds, build identifier `0.6.6-128eabae`. New executable acceptance session drafted: `EXECUTABLE_TEST_SESSION_V184.md`, covering all seven `AE-` cases (`AE-001` as the mandatory wild-card), the companion-pair and URL-scope steps, and `V6-F49`–`V6-F51` as first-class numbered tests — not yet run; installation and acceptance are the Product Owner's next step, per the standard's responsibility split. Production remains untouched and blocked until explicit Product Owner GO after this candidate's own acceptance completes. |
| V52 | 2026-08-31 | `0.6.6` implementation package authored on the combined scope | `VERSION_PACKAGE_AUTHORING_STANDARD_V28.md` was updated to `V29` to add the `V6-F49`–`V6-F51` scope (fix approach, source-file pointers, and mandatory named regression tests per finding) to the existing `0.6.6` candidate definition, alongside the already-frozen Attraction/Event feature scope. `package_0_6_6/PROMPT_FOR_CLAUDE_CODE.md` — the implementation handoff prompt for the Development Agent — was updated to instruct on the combined scope explicitly, rather than Attraction/Event alone, and the package zip was rebuilt from the updated docs. Updates "Next Actions" item 5 below to point at `VERSION_PACKAGE_AUTHORING_STANDARD_V29.md` (was stale at `V27`) and to describe the combined scope rather than Attraction/Event alone. No finding disposition, scope assignment, or historical fact changed — this revision only brings the process documents and the handoff package in line with the `V51` scope decision. |
| V51 | 2026-08-31 | `0.6.5` Production findings folded into `0.6.6` scope | Product Owner supplied a real post-launch engineering log covering `0.6.5` Production monitoring since the `V50` GO. Seven items total, individually registered: two already-fixed-and-live issues (`V6-F45` corrupted lockfile blocking the first deploy attempt — already recorded in `V47`, now given a finding ID for register completeness; `V6-F46` Product Owner locked out of Production Smart Import because `FAMILYTRIPS_ALLOWED_EMAILS` held only the QA test invite, not `eyalby@gmail.com` — new, fixed same-day by correcting the Functions-scoped env var and redeploying); one investigated-and-accepted non-bug (`V6-F47`, three consecutive Hotel-PDF Smart Import failures traced to genuine OpenAI 5xx responses via distinct request IDs, not a FamilyTrips defect — accepted, no fix, retry recommended); one monitored-not-reproduced issue (`V6-F48`, file picker reopened on the very first-ever upload, has not recurred, existing `V6-F05`/`V6-F11` regression coverage already matches the reviewed code path — left open as Monitoring, not assigned); and three real open defects with proposed root causes and fixes, all tracing to the same underlying gap — the model sometimes resolves a conflict into free-text `draft.unresolved` prose instead of a structured field, and the app only trusts structured fields: `V6-F49` (needs-review warnings sourced from `unresolved` text carry no field key and can never be cleared by editing, unlike `needsReviewFields`-sourced warnings), `V6-F50` (a date the model actually resolved correctly can end up only in `unresolved` prose and never reach `startAt`/`endAt` at all — a real silent-omission case, not merely a UX rough edge, given every other rule in this project's history exists to prevent exactly this), and `V6-F51` (the Smart Import review screen doesn't pre-fill blank dates with the Trip start date the way the manual-create form and `suggestionToItem` both already do — low severity, UX consistency only). **Product Owner decision:** `V6-F49`–`V6-F51` are assigned to `0.6.6`, bundled together with the already-frozen Attraction/Event Smart Import feature scope in one combined candidate, rather than as a separate focused correction package the way `0.6.1`–`0.6.4` were. This is a deliberate deviation from the one-slice-per-candidate pattern every prior `0.6.x` candidate followed; it is recorded here explicitly rather than silently reinterpreted. `V6-F45`–`V6-F48` require no `0.6.6` action; they are registered for finding-register completeness and audit trail only. |
| V50 | 2026-08-31 | `0.6.6` entry gate PASSED — implementation starting | `ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` reached `V4`/Frozen: Product Owner resolved all three open Decision 4 questions in chat (ticket/confirmation number is required-when-present, not a no-omission blocker; location must be accurate, property-first discipline frozen; ticket-platform/venue URLs in scope), the benchmark grew from zero to seven real, individually catalogued documents with frozen field-by-field expected values (`AE-001`–`AE-007` — FC Barcelona/Girona LaLiga ticket as mandatory wild-card, Bloomfield Science Museum companion-source pair, Gamzu orienteering order/ticket-number precision case, WaterLand 4-in-1 family ticket, a zoo-ticket screenshot closing the image-format gap), and a `V4` sub-decision on URL scope: ship URL acquisition, with ticket-resale/box-office platform URLs (Ticketmaster, vivenu/leaan.net, hapoelbc.com) expected to safe-fail — backed by real evidence, 4 independently tested ticket-platform URLs, 4/4 blocked by `robots.txt` — while official venue/museum/OTA URLs remain the real target of this sub-scope and are still untested, an explicitly accepted gap carried into implementation rather than assumed closed. Implementation package authoring for `0.6.6` begins now. |
| V49 | 2026-08-31 | `0.6.6` retargeted from Restaurant to Attraction/Event — prior "Activity blocked" claim was wrong | Product Owner spotted a live Trip Center category tile for `אטרקציה` (Attraction) in the running app, directly contradicting this document's own `V16`/`Blocked` claim that "Activities do not yet exist as a first-class Trip item type." Direct code inspection (`src/ingestion.js`) confirmed it: `ITEM_TYPES.activity` and `TYPE_FIELDS.activity` are full peers of every other type (including `restaurant`), used by the same generic dynamic-form system since `V4-F01` (Alpha 0.4.3) — there was never a data-model blocker. Separately, brainstorming surfaced that Restaurant's real-world sources are disproportionately phone/walk-in/informal-email rather than the PDF/image/URL documents Smart Import is built to read, while Attraction/Event sources are ticket-shaped (PDF, QR/barcode) — a closer fit to the pipeline already proven on Flight boarding passes (`FL-008`). Decision: `0.6.6` is retargeted to Attraction/Event Smart Import. `claude/RESTAURANT_SMART_IMPORT_DISCOVERY_V1.md` is shelved at `V2` (preserved, not deleted — a real candidate for a later slice). New draft: `claude/ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md`, mirroring the same discovery structure. Same gate as before: blocked on Product Owner review of three open questions and a real Attraction/Event benchmark set (including one wild-card), not yet supplied. Implementation has not started. |
| V48 | 2026-08-31 | Production import test confirmed; `0.6.6` discovery started | Product Owner confirmed both a Hotel import and a Flight import were tested separately against Production and both worked — the last open item from `0.6.5` is closed. Two repo loose ends from the Production push were also resolved and pushed to `version/V6`: the flight benchmark fixture rename (`e0be719`) and removal of a stray file accidentally committed in `d56482b` (`128eaba`). Started `0.6.6`: drafted `claude/RESTAURANT_SMART_IMPORT_DISCOVERY_V1.md` proposing the Restaurant-specific type contract (Decision 4/7 equivalents), mirroring the Hotel/Flight discovery pattern. That document is explicitly a draft, not frozen — it is blocked on two things only the Product Owner can supply: review/approval of three open product questions, and a real Restaurant benchmark document set (including one wild-card document) with frozen expected values. Implementation has not started and must not start before that document reaches Frozen status. |
| V47 | 2026-08-30 | Production live — post-deploy test blocked on sign-in | First Production build attempt (`94a32a2`) failed at `npm install`: `@firebase/installations@0.6.33` returned a 404 from the npm registry (a bad/stale lockfile pin, not the Node-version issue originally guessed — confirmed via the actual Netlify build log, which the Product Owner retrieved and pasted in rather than accepting the first guess). Netlify's atomic deploys meant nothing went live from the failed attempt — Production stayed safely on the old `0.3.0` build throughout. Claude Code fixed the lockfile (`@firebase/installations` fix + firebase `12.17.0`→`12.18.0`, verified via full test suite), pushed as commit `a915a44`, and the retry succeeded. **Production is now live**: deploy `6a9464b101a1690008db8823`, commit `a915a44`, state `ready`, build identifier `0.6.5-a915a440`. Claude Code then attempted the agreed live Hotel + Flight import test against Production (signed in as eyalby@gmail.com) but hit Google's real sign-in page with no existing session — it correctly declined to enter credentials itself and is waiting for the Product Owner to sign in in the shared browser pane before continuing. |
| V46 | 2026-08-30 | Production baseline correction — this is a first release, not a promotion | `origin/main` was found at `0.3.0-foundation`, 19 commits behind — not Alpha 0.5.6 as documented. This push is the first-ever Production release of the whole V4/V5/V6 line. Product Owner authorized proceeding. |
| V45 | 2026-08-27 | Production environment variables set | All 3 required variables confirmed set for the `production` context via CLI (dashboard UI bug worked around). |
| V44 | 2026-08-27 | Production promotion — OpenAI credential mystery resolved | Confirmed the OpenAI credential is provided automatically by Netlify's account-level AI Gateway, not a site variable. |
| V43 | 2026-08-27 | Production Smart Import secrets — launch decision made | Launch Flight Smart Import to Production now, Flight-only, rather than wait for `0.6.6`. |
| V42 | 2026-08-27 | **`0.6.5` GO** | All findings closed, all 8 Flight benchmark cases pass. |
| V41 | 2026-08-27 | `FL-007` fully passes — `V6-F35` and `V6-F44` Closed | All 8 `FL-001`–`FL-008` benchmark cases now pass. |
| V40 | 2026-08-27 | Seventh `0.6.5` fix pass reported | `V6-F35` (departure side) and `V6-F44` (baggage regression). Commit `94a32a2`. |
| V39 | 2026-08-27 | Retest of sixth fix pass — partial pass | Arrival side of `V6-F35` fixed; departure side reproduced; new regression `V6-F44`. |
| V38 | 2026-08-27 | Sixth `0.6.5` fix pass reported | `V6-F35` two root causes found and fixed. Commit `a95d0aa`. |
| V37 | 2026-08-27 | Two new Alpha 0.7 product decisions | Task creation from the global floating "+"; Today page Packing-list shortcut link. |
| V36 | 2026-08-27 | Retest of fifth fix pass | `V6-F35`'s general fix did not hold on the real `FL-007` source. |
| V35 | 2026-08-27 | Fifth `0.6.5` fix pass reported | Commit `1e42062`. |
| V34 | 2026-08-27 | Full `0.6.5` retest — 4 new findings, `V6-F35` reopened | `V6-F40`–`V6-F43`. |
| V33 | 2026-08-27 | Fourth `0.6.5` fix pass reported | `V6-F35`–`V6-F39` + QA quota increase. Commit `b500469`. |
| V32 | 2026-08-27 | Third `0.6.5` fix pass retested — 5 new findings | `V6-F35`–`V6-F39`. |
| V31 | 2026-08-27 | Third `0.6.5` fix pass reported | `V6-F34` + revised `V6-F32` strategy. Commit `716f950`. |
| V30 | 2026-08-27 | `V6-F32` reclassified | Non-determinism root-caused; `V6-F34` discovered. |
| V29 | 2026-08-26 | Today out-of-range fallback confirmed | — |
| V28 | 2026-08-26 | Retest of second fix pass — partial | — |
| V27 | 2026-08-26 | Second `0.6.5` fix pass reported | Commit `b10e48a`. |
| V26 | 2026-08-26 | `0.6.5` full retest — 2 new findings | `V6-F32`/`V6-F33`. |
| V25 | 2026-08-26 | Retest paused — QA quota; `V6-F31` | — |
| V24 | 2026-08-26 | `0.6.5` fix pass reported | `V6-F25`–`V6-F30`. |
| V23 | 2026-08-26 | Add-flow UX simplification | — |
| V22 | 2026-08-25/26 | `0.6.5` first live QA pass — 6 findings | `V6-F25`–`V6-F30`. |
| V21 | 2026-08-25 | `V6-F24` confirmed live in Production | Alpha 0.6.4 fully closed. |
| V20 | 2026-08-25 | `0.6.5` entry gate passed | — |
| V19 | 2026-08-25 | `V6-F21`–`V6-F23` promoted; `V6-F24` discovered; secrets decision | Commit `3c63e16`. |
| V18 | 2026-08-25 | `V6-F23` closed defensively | — |
| V17 | 2026-08-25 | `V6-F21`/`V6-F22` closed; `V6-F23` partial | — |
| V16 | 2026-08-25 | Restaurant approved as 0.6.6; Activity blocked | — |
| V15 | 2026-08-25 | Alpha 0.6.4 Production promotion; new findings | `V6-F21`–`V6-F23`. |
| V14 | 2026-08-24 | Alpha 0.6.3 finding assignment | 12 findings. |
| V13 | 2026-08-24 | Flight Smart Import approved as 0.6.5 | — |
| V12 | 2026-08-24 | Alpha 0.6.3 acceptance closure — NO-GO | — |
| V1–V11 | 2026-08-23/24 | Alpha 0.6.3 acceptance testing and earlier history | Preserved in `MVP_DELIVERY_HISTORY_V1.md`. |

*Full text of older change-history entries is preserved verbatim in this document's prior published versions and in `MVP_DELIVERY_HISTORY_V1.md`; this revision compresses older entries to keep the table readable.*

## IMPORTANT — Production baseline was misdocumented; corrected 2026-08-30

Every prior revision of this plan (and the repo's own QA-package README) stated or assumed Production was at Alpha 0.5.6. **This was wrong.** Direct inspection of `origin/main` in the real repository showed it at `0.3.0-foundation`, 19 commits behind. Practically: Production has been running a much older build than anyone realized throughout this entire `0.6.x` engagement — none of V4, V5, or V6 (including all of Alpha 0.6.4, previously recorded above as "live in Production") had actually reached `main`. **This means the "Alpha 0.6.4 fully closed and live in Production" status recorded in every prior revision of this document needs re-verification** — it's now unclear whether that referred to a different deploy target than `main`, or whether that record was itself inaccurate. Flagging this explicitly rather than silently correcting it, since it affects how much to trust other "confirmed live in Production" statements in this document's history. **Follow-up needed:** now that the current push is confirmed live, verify directly (not from documentation) exactly what Production is serving and reconcile this document's Alpha 0.6.4 status accordingly.

## Findings status — Alpha 0.6.4 (2026-08-25)

- **`V6-F21`**–**`V6-F24`** — All **Closed** in QA/testing terms. **Production-live status now uncertain — see baseline-correction note above.**

## Findings status — Alpha 0.6.5 (2026-08-27) — FINAL (QA)

- **`V6-F25`**–**`V6-F44`** — **All Closed.**
- All 8 `FL-001`–`FL-008` benchmark cases pass.
- Minor cosmetic note (non-blocking, Beta 0.8 candidate): Flight route-location label shows the full route rather than a single airport code; resolution itself is correct.
- Today out-of-range fallback (`FL-008`) — Confirmed working exactly as specified. `0.7` backlog item logged for a smarter version.

**`0.6.5` release gate: PASSED. GO in QA. Production: live, real-import verification CONFIRMED (2026-08-31) — Product Owner tested both Hotel and Flight imports against Production separately; both worked.** Full record: `EXECUTABLE_TEST_SESSION_V169.md`.

## Decision made — Production Smart Import secrets and credentials

**Decided 2026-08-27: launch Flight Smart Import to Production now, Flight-only.** As of `V46`, this is understood to mean launching the entire V4/V5/V6 line at once (see baseline-correction note above), not just Flight in isolation — the Product Owner has been informed of this and explicitly authorized proceeding anyway.

**Production environment-variable status (updated 2026-08-27):**

| Variable | Needed by Smart Import? | Production status |
|---|---|---|
| `FAMILYTRIPS_ALLOWED_EMAILS` | Yes — allow-list gate on every call | **Set** (via CLI, dashboard UI would not persist it) — currently `eyalby@gmail.com,paolakohan@gmail.com`. Who else should be invited to Production remains an open Product Owner decision, not yet made. |
| `FAMILYTRIPS_GOOGLE_PLACES_API_KEY` | Yes — Hotel location verification only (non-fatal if unset) | **Set** (via CLI) |
| `FAMILYTRIPS_SMART_IMPORT_DAILY_QUOTA` | Yes — per-user daily cap (defaults to 10 if unset) | **Set** (via CLI) |
| `FAMILYTRIPS_FIREBASE_PROJECT_ID` | Yes, but falls back to `VITE_FIREBASE_PROJECT_ID`, already correctly set in Production | No action needed |
| `FAMILYTRIPS_V13_GOOGLE_PLACES_API_KEY` | No — confirmed unused anywhere in the codebase (stale) | Ignore |
| `OPENAI_API_KEY` | Yes — every extraction call | Not a site variable — provided by Netlify's account-level AI Gateway, already active for this site across all contexts |

**Known Netlify dashboard bug encountered:** editing an existing variable's Production value through the web UI and clicking Save silently failed. Worked around via `netlify env:set VAR "value" --context production` directly in a local terminal.

**Deploy mechanism (confirmed 2026-08-30):** the `familytripz` Netlify site auto-builds Production from `main` only. Promotion = `git push origin <commit>:main`.

**First Production deploy attempt failed, second succeeded (2026-08-30):** `94a32a2` failed to build — `npm install` hit a 404 on `@firebase/installations@0.6.33` (bad lockfile pin; the Node-version hypothesis first offered was wrong and was ruled out by getting the actual build log rather than guessing). Netlify's atomic deploys meant Production stayed on the old build throughout — no user-facing impact. Fix pushed as commit `a915a44`; deploy `6a9464b101a1690008db8823` succeeded, state `ready`, **live build identifier `0.6.5-a915a440`** (differs from `0.6.5-94a32a28` only because of the extra fix commit — no functional change, only `package-lock.json`).

**Uncommitted working-tree changes (2026-08-30) — resolved 2026-08-31:** the two renamed test fixture images under `tests/fixtures/flight_benchmark/` were committed as-is (`e0be719`) and pushed to `version/V6`. A separate, previously-undiscovered issue was also found and fixed while investigating: a stray file accidentally committed back in `d56482b` ("Add Alpha 0.3 Firebase and Vite foundation") — leftover `less`-pager text from a botched shell command, tracked in the repo under a garbled filename — was removed (`128eaba`) and pushed. Working tree is clean on `version/V6`.

Before ramping real Production traffic: (1) ~~live Production Hotel + Flight import test~~ — **CONFIRMED 2026-08-31**: Product Owner tested both separately against Production, both worked; (2) check AI Gateway billing/usage in Netlify (Team → Usage/Billing) — account currently shows `ai_usage_limit_enforced: false` with no cap, which is a configuration state, not a cost guarantee — **still open**; (3) decide who beyond the Product Owner and the QA test account should be added to the Production `FAMILYTRIPS_ALLOWED_EMAILS` allowlist — **still open, Product Owner decision, deliberately deferred for now.**

## 1. Delivery Principles

### Alpha
Alpha versions complete the full MVP functionality.

- Functionality is the priority.
- Architecture must remain stable.
- UI improvements are included only when required for usability or testing.
- Every release must be installable, testable, and deployable.
- Every release includes QA, acceptance tests, regression tests, release notes, and rollback instructions.

### Beta
Beta improves the product experience after MVP functionality is complete.

- Visual design
- HMI and interaction design
- Navigation refinement
- Mobile usability
- Accessibility
- Consistency
- Performance
- Final user-facing wording

### Release Candidate
Release Candidate is limited to:

- Bug fixes
- Data protection
- Reliability
- Offline verification
- Final Thailand-trip acceptance testing
- No new features unless a critical blocker is discovered

## 2. MVP Scope for Thailand

The MVP must support the real Thailand trip from planning through active use.

- Trip creation and management
- Multiple independently-owned Trips running in parallel — the Product Owner's family Trip and any number of friends' own Trips, each fully isolated from the others unless explicitly shared by its Owner. This is non-negotiable MVP scope, not a post-MVP nicety: FamilyTrips is built to be shared with friends who create and manage their own Trip alongside the Product Owner's.
- Private Trips and explicit shared-Trip access — on any Trip, whether the Product Owner's family Trip or a friend's own independently-owned Trip
- Two-user, two-device collaboration on any shared Trip — the same Owner/Editor sharing and synchronization contract applies identically whether the Trip belongs to the Product Owner or to a friend; there is no reduced or "friend-tier" version of sharing or sync
- Near-real-time online synchronization and offline-change synchronization after reconnect
- One or more Destination Countries, destination/route summary, and Trip dates
- Trip lifecycle
- Smart document intake
- Manual item creation
- Dynamic forms by item type
- Item-owned documents plus a non-category Intake Inbox for unassigned sources; no standalone Documents category in Trip Center
- Direct camera/gallery/PDF/file attachment of documents and tickets to existing items, including items created from QR, without duplicate item creation
- Today
- Timeline
- Calendar and date selection
- Trip Center
- Games and travel activities
- Preparations and tasks
- Shopping places, scheduled shopping visits, and shopping lists
- Contextual website, booking, provider, document, and navigation actions attached to their owning items; no standalone Links category
- Item-owned locations and map/navigation actions; no standalone Locations category
- First-class Transport items for transfers, rail, ferry/boat, rental vehicles and other local transport
- Participants
- Participant-specific tickets and documents
- Packing lists
- Participant-specific packing
- Item-owned and Trip-level useful contacts; no standalone Contacts category in Trip Center
- Offline use
- Visible synchronization state and deterministic conflict handling
- Local persistence
- PWA installation
- Clear delete/reset behavior
- Interactive release testing

*Alpha 0.1 through Alpha 0.5.6 delivered this scope incrementally; see `MVP_DELIVERY_HISTORY_V1.md` for what shipped in each version. What follows is the current and future plan.*

# Alpha 0.6 — Smart Import Redesign

## Goal

Replace the failed browser-only heuristics for creating or enriching travel items from a URL, image/camera capture or PDF with one evidence-backed, source-preserving and review-first architecture. This release is dedicated to Smart Import and must not absorb the former broad MVP-completion backlog.

## Entry gate — Product Owner discovery session

**Entry gate: PASSED.** `SMART_IMPORT_REDESIGN_DISCOVERY_V22.md` freezes Decisions 1–7 for Hotel. `FLIGHT_SMART_IMPORT_DISCOVERY_V1.md` freezes the Flight-specific type contract and benchmark, inheriting the generic decisions unchanged.

## Alpha 0.6.4 — correction package: QA-confirmed; Production-live status uncertain (see `V46` baseline-correction note)

`0.6.4` corrected the twelve findings assigned in `V14`, plus seven findings discovered during its own QA/post-promotion retesting (`V6-F18`–`V6-F24`), all Closed against QA. Prior revisions of this document stated this was "live in Production" — that claim is now uncertain given the `V46` discovery that `main` was still at `0.3.0-foundation`. Needs direct re-verification now that the current push is live.

## Alpha 0.6.5 — Flight Smart Import: GO in QA; Production live and verified

Approved 2026-08-24. Entry gate passed 2026-08-25. Seven fix passes sent across the engagement, plus one Production-only lockfile fix. **All 8 `FL-001`–`FL-008` benchmark cases pass; all findings (`V6-F25`–`V6-F44`) closed; `0.6.5` release gate PASSED against QA.** Product Owner decided to launch to Production now — this turned out to be the first-ever Production release of the whole V4/V5/V6 line (see `V46`), explicitly authorized. **Production is live**, build `0.6.5-a915a440`, deploy `6a9464b101a1690008db8823`. **The live Hotel + Flight import test against Production is confirmed complete (2026-08-31)** — the Product Owner tested both separately and both worked. `0.6.5` is fully closed. Full status: `EXECUTABLE_TEST_SESSION_V169.md`.

## Approved scope addition — Attraction/Event Smart Import + 0.6.5 Production findings (0.6.6) — discovery FROZEN, entry gate PASSED, implementation package authoring underway

`0.6.6` was originally approved as Restaurant on 2026-08-25/27. Retargeted to Attraction/Event on 2026-08-31 (`V49`) — see correction note below. Entry gate passed 2026-08-31 (`V50`, `ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` `V4`). Sequenced after `0.6.5` (Flight), to launch independently to Production when ready. **As of `V51`, scope also includes three findings from `0.6.5` Production monitoring** (`V6-F49`–`V6-F51`) — Product Owner decision to bundle a correction scope into this feature candidate rather than spin up a separate `0.6.6`-correction/`0.6.7`-feature split. See the `V51` change-history row above for the full finding list and the four items (`V6-F45`–`V6-F48`) requiring no `0.6.6` action.

### Attraction/Event Smart Import feature scope

- **Correction — the prior "Activity is blocked" claim was wrong, not just stale.** This document previously stated (`V16`) that Activities do not exist as a first-class Trip item type and are therefore blocked. Product Owner spotted a live `אטרקציה` (Attraction) Trip Center category tile in the running app; direct code inspection confirmed `ITEM_TYPES.activity` / `TYPE_FIELDS.activity` in `src/ingestion.js` are full peers of every other type, including `restaurant` — same generic dynamic-form system since `V4-F01` (Alpha 0.4.3). There was never a data-model blocker; what was actually missing is exactly what was missing for Restaurant: the Smart Import extraction contract and benchmark.
- **Why Attraction/Event instead of Restaurant:** brainstorming surfaced that real-world Restaurant sources are disproportionately phone/walk-in/informal-email — a thin match for a pipeline built to read PDF/image/URL documents. Attraction/Event sources are ticket-shaped (PDF, QR/barcode), which is architecturally closer to the boarding-pass case Flight already proved out (`FL-008`). Restaurant is shelved, not abandoned — `claude/RESTAURANT_SMART_IMPORT_DISCOVERY_V1.md` is preserved at `V2` as a real candidate for a later slice.
- **Discovery FROZEN 2026-08-31 (`ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` `V4`):** `claude/ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` defines the frozen Attraction/Event type contract — inheriting Decisions 1/2/3/5 unchanged, with Decision 4 (no-omission fields), Decision 6 (benchmark) and Decision 7 (delivery slice) all frozen.
- **Decision 4, frozen:** event/attraction name, date, time and venue/location are no-omission blockers; location must be accurate (property-first, same discipline as Hotel). Ticket/confirmation number is explicitly **not** a no-omission blocker — required-when-present, same treatment as Restaurant would have had, not Flight's PNR-blocker treatment (Product Owner decision, reversing the `V1` proposal).
- **Decision 6, frozen:** seven real, individually catalogued benchmark documents, `AE-001`–`AE-007`, each with SHA-256 identity and frozen field-by-field expected values — `AE-001` (FC Barcelona vs Girona FC, LaLiga) is the mandatory wild-card; `AE-003`/`AE-004` (Bloomfield Science Museum) is a real companion/duplicate-source pair; `AE-005` (Gamzu orienteering) is a real order-number-vs-ticket-number precision case; `AE-006` (WaterLand) is a 4-tickets-in-one-PDF family case with no per-person names; `AE-007` (a zoo ticket screenshot) closes the image-format gap and carries an explicit lower-confidence note on its barcode digits. Both PDF and image source formats are represented.
- **Decision 7, frozen:** ticket PDFs and images from global Add or existing-item attach, mirroring Hotel/Flight. Public URL acquisition is **in scope**, with an explicit `V4` sub-decision on how to treat it: official venue/museum/OTA URLs are the real target and are expected to work; ticket-resale/box-office platform URLs (Ticketmaster, vivenu/leaan.net, hapoelbc.com) are expected to land on a safe-failure state, and that is documented as expected behavior — backed by real evidence gathered before implementation (4 independently tested ticket-platform URLs, 4/4 blocked by `robots.txt`, a new named failure reason alongside Hotel's `access_required`/login pattern). **One explicitly accepted, non-blocking gap:** zero official venue/museum/OTA URLs have been tested end-to-end yet — the implementation package carries this as a named verification step, not an assumed pass.
- **Entry gate: PASSED (2026-08-31).**

### `0.6.5` Production findings folded into `0.6.6` (added `V51`)

| Finding | Status | Disposition |
|---|---|---|
| `V6-F45` — corrupted lockfile blocked the first Production deploy attempt | Fixed, live | Already recorded in `V47`; registered here with a finding ID for register completeness only. No `0.6.6` action. |
| `V6-F46` — Product Owner locked out of Production Smart Import (`FAMILYTRIPS_ALLOWED_EMAILS` missing `eyalby@gmail.com`) | Fixed, live | Corrected same-day via the Functions-scoped env var and a redeploy. No `0.6.6` action. |
| `V6-F47` — Hotel PDF (Hyatt Regency Phuket Voucher) failed Smart Import 3 times in a row | Accepted, not a defect | Netlify function logs show three genuine OpenAI 5xx responses (distinct request IDs); a Flight import succeeded around the same time, confirming the AI Gateway path itself is healthy. No code change. Recommendation carried into support docs: retry; escalate to OpenAI with request IDs if the same document keeps failing while others succeed. |
| `V6-F48` — file picker reopened after selecting the very first upload on the account | Monitoring | One-time occurrence, has not recurred (including a second Flight upload). Reviewed code path already matches existing `V6-F05`/`V6-F11` regression coverage — no gap found. Left open as Monitoring, not assigned to any version, revisit only if it recurs. |
| `V6-F49` — needs-review warnings sourced from `draft.unresolved` free text never clear after a manual correction | **Fixed** (`0.6.6`, `version/V6`, not yet Production) | Root cause: `needsReviewFields`-sourced warnings were keyed by the raw model canonical key, which `NEEDS_REVIEW_FIELD_MAP` had no entries for at all, so nothing could ever match and clear them, regardless of source; `unresolved`-sourced warnings carry no field key by nature. Fixed: `NEEDS_REVIEW_FIELD_MAP` gained the missing Hotel/Activity-shaped entries and `smart-import-adapter.js`/`activity-import-adapter.js` now key by the resolved proposed-field name, so a structured needs-review warning genuinely clears on edit; an `unresolved`-sourced warning is wrapped `{message,dismissible:true}` with a manual dismiss control wired into the review screen (`v5-app.js`'s `isDismissibleWarning`/`dismiss-warning`); the tightened Hotel/Activity system prompts now instruct the model to resolve a conflict into a field from the start. Regression test: `tests/v6-49-51-correction.mjs` (both the structured-path clearing and the unresolved-path dismissibility). `src/ingestion.js:154-171`, `src/smart-import-adapter.js`, `src/v5-app.js`. |
| `V6-F50` — a date the model resolved correctly can silently never reach `startAt`/`endAt` | **Fixed** (`0.6.6`, `version/V6`, not yet Production) | Same root cause as `V6-F49`. Fixed: same prompt tightening as `V6-F49`, plus a regex fallback (`scanFreeTextForDate`, exported from `smart-import-adapter.js`, reused by `activity-import-adapter.js`) that recovers a labelled date from `unresolved`/`importantNotes` free text when the structured `fields` extraction found nothing, flagging the result `needs_review` rather than presenting it as confident. Regression test: `tests/v6-49-51-correction.mjs`, using the exact real Hyatt Regency free-text wording, plus a negative case proving the scan stays inert on unlabelled text. `src/smart-import-adapter.js`. |
| `V6-F51` — Smart Import review screen doesn't pre-fill blank dates with the Trip start date | **Fixed** (`0.6.6`, `version/V6`, not yet Production) | Low severity, UX consistency only. Fixed: new `suggestionReviewDefaults` (`src/ingestion.js`) mirrors `suggestionToItem`'s own trip-start fallback condition exactly (new-item suggestions only, respects schedule), applied in `suggestionView` before rendering `proposalFields`. Regression test: `tests/v6-49-51-correction.mjs`, covering range/single schedules, an already-dated suggestion (must not be overwritten), an update/attach-and-extract suggestion (must not get the new-item default), and no-trip-yet. `src/v5-app.js:145`, `src/ingestion.js:178-193`. |

Per `VERSION_PACKAGE_AUTHORING_STANDARD_V29.md`'s mandatory regression-test rule, `V6-F49`–`V6-F51` are recorded `Fixed` above because `tests/v6-49-51-correction.mjs` exists, is named for each finding ID, asserts the specific expected behavior recorded in this register (not merely that the code runs), and passes as part of the permanent `npm test` suite. "Fixed" here means fixed in the `0.6.6` candidate on `version/V6` — none of the three has reached Production, which remains on `0.6.5` until `0.6.6` completes its own acceptance and receives explicit Product Owner GO.

Implementation of the combined `0.6.6` scope (Attraction/Event feature + `V6-F49`–`V6-F51` corrections) is complete on `version/V6` and awaiting Product Owner install/acceptance testing — see the `V53` change-history row above.

### `0.6.6` QA acceptance findings (added `V54`)

Three findings discovered during Eyal's own `0.6.6` QA acceptance testing (build `0.6.6-3925bb20`), investigated and registered per the standard's mandatory finding register. All three were implemented on `version/V6`, each with a passing named regression test. **Update (`V62`):** Eyal has since retested all three — `V6-F52`/`V6-F53` are now **Fixed, verified**; `V6-F54` is **merged into `V6-F55`** (see below) rather than closed separately. See each row for full retest evidence.

`V6-F54`'s QA retest (build `0.6.6-9b5b1ed0`) surfaced a fourth finding, `V6-F55`, registered below — investigated and root-caused, not yet fixed. A second observation from the same retest (Panan Krabi) was investigated and assessed as very likely expected, by-design behavior, not a defect — see the note following the table; it is not registered as a finding.

Eyal's `AE-001` retest surfaced item 5 (per-passenger splitting) as not shown in the item detail UI. Investigated below and split into two findings, `V6-F56` and `V6-F57` — see the `V59` change-history row above for the full investigation narrative, including the explicit data-access limitation (no live Firestore/Firebase inspection tool available in this environment).

A follow-up investigation (`V60`) confirmed `V6-F57` extends uniformly to `AE-002`/`AE-004` (same unconditional code path, no case-specific behavior) and extends its scope to `details.amount`/`details.currency` (not shown in `detailView()` for any activity item, same "captured but never displayed" root cause) — `V6-F57`'s row below is updated to reflect this, not a new finding. The same follow-up also found a genuine test-coverage gap in the existing regression suite, registered as `V6-F58` below.

| Finding | Observed in | Test/evidence source | Observed vs. expected behavior | Severity | Assignment status |
|---|---|---|---|---|---|
| `V6-F56` — `ticketHolderSchema` has no field for a per-passenger national ID (DNI) and blocks one via `additionalProperties:false`, so DNI can never reach a saved item regardless of extraction quality | `0.6.6` QA, build `0.6.6-9b5b1ed0` | `AE-001` retest (FC Barcelona vs Girona FC ticket); direct code inspection of `netlify/functions/_shared/smart-import-schema.mjs` lines 87–90 (`ticketHolderSchema`) and 92–108 (`activityImportSchema`) | Observed: `AE-001`'s item detail shows no per-passenger DNI anywhere. Expected (frozen ground truth, `ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` `V4`, line 100–101): each of the two named passengers carries a DNI ("Eyal Ben Yitzchak Ben Yitzchak, DNI 40384410 …", "Amit Ben Yitzhak, DNI 40386943 …") as part of their evidence-linked per-person data. Root cause, confirmed by direct schema inspection (no live-item read needed for this half — the schema itself is the proof): `ticketHolderSchema` declares exactly `name`, `ticketNumber`, `seatOrSection`, `evidence`, `certainty` with `additionalProperties:false`; OpenAI's structured-output enforcement means any `dni`-shaped value the model tried to attach to a ticket holder is rejected/stripped, not merely dropped by app logic downstream. `activitySystemPrompt`'s `fields` key vocabulary (`location, meeting_point, duration, age_restrictions, included, not_included, amount, currency, confirmation_number, website, start_date, start_time`) also has no DNI/national-ID entry, so the model is never even instructed to look for one as a fallback generic field. This is a genuine data-capture gap in the schema/prompt design authored for `0.6.6`, not a rendering gap — DNI has no field to land in anywhere in the pipeline, so it cannot be "present but unshown." Distinct from `V6-F57` below, which concerns fields the schema does capture. | High — direct, provable failure against `AE-001`'s frozen ground truth, the mandatory wild-card case; requires a schema/prompt change (adding a `dni`/national-ID property to `ticketHolderSchema` and its vocabulary), not a UI fix | **Fixed, verified** (`V64`, target `0.6.6.1`) — Eyal's retest: a fresh delete-and-reimport of `AE-001` shows both ticket holders with correct DNI (`40384410`, `40386943`) |
| `V6-F57` — `details.ticketHolders`/`ticketNumbers`/`ticketQuantity`/`amount`/`currency` (per-passenger name, seat/section/gate/row, per-ticket number, and price) have zero rendering code anywhere in the app, on either the suggestion review screen or the saved item detail view — confirmed to apply uniformly to every Activity case (`AE-001`, `AE-002`, `AE-004`, `AE-005`, and now `AE-006`), not just `AE-001` | `0.6.6` QA, build `0.6.6-9b5b1ed0` | `AE-001` retest; direct code inspection of `src/activity-import-adapter.js` (capture) and repository-wide search of `src/v5-app.js` (rendering — zero matches for `ticketHolders`, `ticketNumbers`, or `ticketQuantity`; `detailView()`'s full render list, read directly, has no `amount`/`currency` either) | Observed: `AE-001`'s per-passenger seat/gate/row/ticket-code data (the `Acces`/`Gate`/`Boca`/`Row`/`Seat` vocabulary the discovery doc calls out as this case's defining wild-card trait) is not shown in the item detail view — only the source PDF is attached. `AE-005`'s follow-up investigation additionally found its ₪120 `amount` is likewise invisible on the saved item. Expected: this data, when the schema can capture it (unlike `V6-F56`'s DNI gap), should be visible. Root cause, confirmed by direct code inspection: `smartImportActivityResultToSuggestion` (`src/activity-import-adapter.js` lines 44–50, 115) correctly maps `draft.ticketHolders` (and `values.amount`/`values.currency`) into `proposed.details`, and the OpenAI schema slot exists for all of it — so this data is architecturally captured, not schema-blocked. But `proposalFields()` (`src/v5-app.js` line 217) only renders a single flat `participants` text input (names only) plus the fixed `TYPE_FIELDS.activity` list (which does not include `amount`/`currency` at all), `smartEvidencePanel()` (line 215) renders `details.smartImportFields`/`importantNotes`/place-validation only, and `detailView()` (line 150, read in full) renders only provider/confirmationNumber/location/startAt/endAt/notes/warnings/sources — none of it ever touches `ticketHolders`, `ticketNumbers`, `ticketQuantity`, `amount`, or `currency`. A follow-up investigation (`V60`) confirmed the capture-and-display code paths are unconditional — no branching by case or ticket count anywhere — so this applies identically to `AE-002` (Row 4/Seat 26/Gate B) and `AE-004` (3 assigned seats), not just `AE-001`; and it extends to `amount`/`currency` for every priced case (`AE-001`, `AE-004`, `AE-005`, `AE-007`), not only `AE-005`. A second follow-up (`V61`) confirmed `AE-006` (WaterLand, 4 tickets, no per-person names) as a fifth instance of the identical pattern — its `ticketNumbers` (`623000111`–`623000114`) are correctly captured (matching the passing `test_AE006_four_tickets_no_names_date_only` assertion that `ticketHolders.length===4`) but equally unrendered — this also confirms the `V53` implementation report's claim that `AE-006` "individually preserves all four physical tickets" is accurate at the adapter/capture level, just invisible in the UI like every other case, not a false claim. **Whether any specific item's actual saved data holds the full underlying values could still not be directly confirmed** — this environment has no Firebase/Firestore inspection tool, and the Netlify Blobs audit store does not retain the extracted draft (see `V59` row above); the existing regression suite (`tests/v6-6-attraction-event-import.mjs`) proves the adapter mapping is correct for the cases/fields it actually asserts (see `V6-F58`), but not for the live model's real output on any of Eyal's actual test documents. This determination rests on code-path tracing (capture path exists and is correctly wired for all these fields; display path does not exist anywhere for any of them) rather than a direct read of any live item, flagged explicitly per the investigation's own instruction not to assume. | Low if the underlying data is genuinely captured (a real, deferred-to-Beta-UI display gap, same class as other fields not yet in the current review/detail forms) — would need to be reassessed as Medium/data-loss if a later direct check of a saved item shows `ticketHolders`/`amount` themselves are empty or incomplete for reasons other than `V6-F56`'s DNI gap | **Fixed, verified** (`V64`, target `0.6.6.1`) — Eyal's retest across three fresh reimports: `AE-001` (seat/DNI/ticket-code table renders), `AE-005` (₪120 amount shows; the `ticketNumber` precision distinction — `2ZTN-G53R-GJ81P` vs. order number `2ZTN-G53R-GJ8` — is visible and correct), `AE-003` (three ticket holders correctly listed under Eyal Ben Yitzhak with distinct ticket numbers) |
| `V6-F58` — the existing "SHA-256-verified against the frozen benchmark" regression suite (`tests/v6-6-attraction-event-import.mjs`) does not actually assert several frozen-ground-truth fields it has fixture data for, so it would stay green even if the adapter mapping for those specific fields regressed | `0.6.6` QA acceptance, discovered while investigating `V6-F56`/`V6-F57`'s `AE-005` follow-up | Direct reading of `tests/v6-6-attraction-event-import.mjs`; suite executed directly (`node tests/v6-6-attraction-event-import.mjs`), currently **10/10 passing** | Observed: `test_AE005_order_vs_ticket_number_precision` (lines 166–188) builds its fixture with `f('amount','Price','120')` (matching `AE-005`'s frozen ₪120 ground truth) but never asserts `suggestion.proposed.details.amount` — unlike `test_AE004`/`test_AE007`, which do assert `amount`, and `test_AE002`/`test_AE003`/`test_AE006`, which assert it is correctly empty. `test_AE002_baseline_no_price_in_source` (lines 63–83) builds its fixture with `seatOrSection:'Row 4 · Seat 26 · Gate B'` but never asserts `suggestion.proposed.details.ticketHolders[0].seatOrSection`. `test_AE004_auditorium_companion_pair_becomes_two_related_items` (lines 139–157) builds its fixture with three distinct `seatOrSection` values (`Row 5 · Seat 11/10/9`) but never asserts any of them either. Expected: a suite whose own header comment (lines 9–17) claims to exercise "the adapter/mapping pipeline end-to-end against all seven real cases" per the frozen ground truth should assert every frozen field it has fixture data for, the same way `test_AE001` already does for `ticketHolders[0].seatOrSection`/`ticketNumbers`. Root cause: these three specific assertions were simply never written when each test was authored — not a mapping defect (the adapter code confirmed correct for these fields under `V6-F57`'s investigation) and not itself proof the live model's real extraction is correct either; it is a coverage gap in the test, independent of whether the underlying feature works. This does not by itself indicate `AE-002`/`AE-004`/`AE-005` are broken — `V6-F57` already establishes the capture-side mapping is uniform and correct for these fields — but it means the suite currently provides no regression protection for them and cannot be cited as verifying them. | Low-Medium — not a user-facing defect, but a real gap in the acceptance safety net for three frozen benchmark values (`AE-002`/`AE-004` seat data, `AE-005` amount); should be closed alongside any `V6-F57` fix so the corrected display code has actual regression coverage from day one | **Fixed, verified** (`V64`, target `0.6.6.1`, folded into `V6-F57`) — the three missing assertions were added directly to the named existing tests; `V6-F57`'s own retest evidence (`AE-005`'s amount/ticket-number precision now visibly correct, `AE-003`'s three distinct ticket numbers correctly listed) directly exercises the same fields these assertions now protect |
| `V6-F55` — a third category of warning (merge-conflict notices, place-not-validated notices, and every model-authored `draft.warnings` entry) has never been clearable, by any means, on a suggestion or an item, before or after `V6-F54` | `0.6.6` QA, build `0.6.6-9b5b1ed0` | Manual observation, `V6-F54` QA retest (`H-001`/Panvaree Resort, after attaching the companion document `Panvaree_Hotel_booking.png`) | Observed: after attaching a second document to the already-saved Panvaree Resort item, three new warnings appeared — two "ערך חדש התגלה עבור `startAt`/`endAt`: ... — הערך השמור נשמר; יש לבדוק ולעדכן ידנית במידת הצורך" (new value discovered, existing value kept, flagged for manual review) and one "המיקום נשמר כשם המלון בלבד; לא אומתו קואורדינטות" (location saved as hotel name only, coordinates unconfirmed). A manual edit to the item did not clear any of them — regardless of which field was actually edited, which matters here (see root cause). Expected: a warning describing a resolved-or-reviewed condition should be clearable, the same way `V6-F49`'s (`unresolved`-sourced, dismiss button) and `V6-F54`'s (`needsReviewFields`-sourced, edit-triggered) warnings now both are. Root cause, confirmed by direct code inspection: this project's warnings have always fallen into exactly two clearable shapes plus this third, uncovered one. (1) `unresolved`-sourced: wrapped `{message,dismissible:true}`, cleared only by the dismiss button (`V6-F49`). (2) `needsReviewFields`-sourced: plain string, but its originating entry is tracked in `details.needsReviewFields` and matched by field label, cleared only by `reconcileStaleNeedsReview` on a genuine field-value edit (pre-existing for suggestions; extended to items by `V6-F54`). (3) Everything else — `draft.warnings` (the model's own top-level array, spread in first in `smart-import-adapter.js`'s `proposed.warnings=[...(draft.warnings||[])...]`), the place-not-validated notice (same line, hardcoded string), and, confirmed as the actual source of this observation, `preserveTrustedFieldsOnMerge`'s merge-conflict notices (`src/smart-import-adapter.js`, `warnings.push(\`ערך חדש התגלה עבור ${key}...\`)`) — is a plain string that is neither object-wrapped nor registered in `needsReviewFields` at all, so it matches neither `isDismissibleWarning` nor any `reconcileStaleNeedsReview` label comparison, regardless of which field is edited or how many times. This category was never in scope for `V6-F49` or `V6-F54` — neither investigation identified it as a distinct shape, since the original Hyatt Regency case (`V6-F49`/`V6-F50`) happened to be entirely `unresolved`-sourced. It is not a regression of either fix and predates both: this gap has existed, unchanged, on the pre-approval suggestion review screen too (`updateSuggestion` calls the same `reconcileStaleNeedsReview`, with the same blind spot) since `preserveTrustedFieldsOnMerge` was first introduced (`V6-F15`). **`V6-F54` merged into this finding (`V62`):** Eyal's `V6-F54` retest found no dismiss control visible anywhere in the UI on either the Panvaree Resort or Panan Krabi item, plus two new, worse symptoms confirming this is the same root cause, not a separate one — (1) **Panvaree Resort:** after merging the companion PNG (wrong dates) and PDF (correct dates), the warning box still reads "new value discovered for `startAt`: 2027-01-17 — saved value kept, needs manual review," describing a conflict that has already resolved correctly (the correct date IS what's saved) — the warning is stale/misleading, not merely slow to clear; (2) **Panan Krabi:** Eyal actively edited both the check-in and check-out dates, which cleared most of the warning, but **one warning still remains** even after editing every field associated with it — worse than the previously-documented "edit clears it" fallback, since that fallback is now itself shown to be incomplete. **Product Owner's proposed fix direction (to guide, not dictate, implementation):** rather than silently keeping one value and showing a warning that may never clear, actively resolve the conflict with the user — either a picker asking which value is correct (reusing the existing `directionAmbiguous`/`directionCandidates` picker pattern rather than new UI), or a value proposed from the Trip's own date range requiring explicit user confirmation. Either approach replaces "silent-keep + indefinite warning" with a real decision point. **Implemented (`V63`):** `resolveMergeCandidate` (`src/ingestion.js`) and `mergeCandidatePanel` (`src/v5-app.js`) give an explicit keep/accept choice per conflicting field, reusing the `directionPickerPanel` radio pattern, wired into both `suggestionView()` and `detailView()`; `isDismissibleWarning` generalized so any plain-string warning not currently backed by a live `needsReviewFields` label is dismissible as a fallback, closing the third-category gap for `draft.warnings`/the place-not-validated notice without needing to wrap each generation site individually; `NEEDS_REVIEW_FIELD_MAP` gained a `location` entry and `smart-import-adapter.js`'s Hotel `FIELD_MAP` gained the address-related keys `place-validation.mjs` already recognized, closing a second, independent path (a location/address-shaped needs-review conflict with no target mapping) by which a warning could survive editing every associated field — a plausible, though not database-confirmed, explanation for the Panan Krabi "one warning survives full edit" symptom. | **High** (elevated from Medium at Product Owner request upon merging `V6-F54` in — a cross-cutting warning-lifecycle defect shared by Hotel and every other Smart Import vertical, not an Activity-specific or narrow cosmetic issue) | **Implemented** (`version/V6`, `V63`), awaiting Product Owner QA retest confirmation — not recorded `Fixed`; target `0.6.6.1`. Regression tests: `test_V6_F55_resolve_merge_candidate_keep_preserves_trusted_value`, `test_V6_F55_resolve_merge_candidate_accept_applies_new_value_and_reconciles_needs_review`, `test_V6_F55_resolve_merge_candidate_is_a_noop_for_an_unknown_key`, `test_V6_F55_resolve_merge_candidate_works_on_a_saved_item_shape`, `test_V6_F55_merge_candidate_panel_wired_into_both_screens`, `test_V6_F55_merge_conflict_warnings_excluded_from_generic_dismiss_list`, `test_V6_F55_plain_string_warning_dismissible_unless_backed_by_live_needs_review` (`tests/v6-55-correction.mjs`). **Core picker mechanism confirmed working (`V64`):** Eyal verified the explicit keep/accept decision point in two independent real scenarios — the `AE-003`/`AE-004` attach-merge (title/provider/location/date conflicts all presented as a choice, correctly saved once resolved) and a fresh Panvaree Resort re-merge (chose between differing PNG/PDF values). This is the Product Owner's own requested fix direction and it is genuinely working, generalized cleanly across Hotel and Activity — not marked `Fixed` because of the open sub-issue immediately below, discovered on the same Panvaree Resort re-merge, which must be understood before this finding can close. **Open sub-issue, investigated, not yet resolved either way:** after resolving the value-conflict picker, four other warnings remained even after saving and after directly editing the conflicting dates — a duplicate/unexplained second booking number, ambiguous check-in/check-out date labeling, RTL phone-formatting ambiguity, and a truncated address. Investigated by code-path classification (no live Firestore access, same limitation as every prior investigation this session): these four fall into two structurally distinct categories that require different answers, and without the exact stored warning text/`details.needsReviewFields` contents for this specific item, classifying each one with certainty isn't possible from here — (1) **RTL phone-formatting ambiguity and the truncated address** almost certainly originate from the model's own free-text `draft.warnings`/`importantNotes` commentary — there is no editable field concept in this app for "is this phone number's RTL rendering ambiguous" or "is this address truncated," so these were never resolvable by a field edit by design, only ever by the manual dismiss now available under `V6-F55`'s own fix (`isDismissibleWarning`'s new fallback). If Eyal saw no dismiss control on these two, that directly contradicts the fix and is a real bug; if a dismiss control was present but not used (only the dates were edited), these are working as intended and the open question is purely about UI clarity — should a permanent source-documentation note be styled differently from an actionable "needs review" warning, so it doesn't read as something an edit ought to clear. (2) **The duplicate booking number and the check-in/check-out date-labeling ambiguity** are plausibly `needsReviewFields`-shaped (Hotel's adapter can produce a `confirmationNumber`- or `startDate`/`endDate`-keyed entry for exactly this shape of conflict, and both keys are correctly mapped in `NEEDS_REVIEW_FIELD_MAP`) — if the booking-number one is confirmationNumber-keyed and Eyal only edited dates, its persistence is expected (unrelated field, correctly untouched), not a bug. The date-labeling one is the more concerning case: if it really is `startDate`/`endDate`-keyed and Eyal genuinely edited those fields, it should have cleared via `reconcileStaleNeedsReview` — persisting despite that is not explained by anything found in this investigation and needs either the live item's actual warning/needsReviewFields contents, or a real-device repro with the browser console open, to root-cause further. **Bottom line: cannot be fully resolved from code inspection alone this time** — reporting the classification hypothesis and exactly what evidence would settle it, rather than guessing a specific fix. **Root-caused and implemented (`0.6.6.2`, `V65`):** confirmed by code reading (not a live-repro against this specific item's exact stored `warnings`/`needsReviewFields` contents — that caveat still stands) that the actual mechanism was `isDismissibleWarning`'s label-TEXT-substring heuristic itself: it decided "resolvable by editing this field" by checking whether a warning's rendered text merely CONTAINED a currently-live field label's text, so a free-text warning that happened to mention a common label word (e.g. "טלפון") collided with an unrelated live field entry and was misclassified as field-backed though it never was — stuck both ways at once (not really field-backed, so editing did nothing; falsely appeared field-backed, so the generic dismiss refused it too). This plausibly explains all four warnings; the duplicate booking number and date-labeling ambiguity could equally be a genuine second `needsReviewFields` entry the edit didn't fully address — noted honestly rather than claimed with certainty either way. Fixed by replacing the substring heuristic with an explicit `needsReviewRef:{key,value}` a warning carries only when actually generated FROM a `needsReviewFields` entry (`smart-import-adapter.js`/`activity-import-adapter.js`); `isDismissibleWarning` (moved to `src/ingestion.js` for direct testability, imported into `v5-app.js`) now checks that reference by exact key+value match instead of text substring — anything without one (a plain string, or an object with neither marker) is unconditionally dismissible; `reconcileStaleNeedsReview` clears a `needsReviewRef`-tagged warning by exact key match on the matching field edit, keeping the old label-substring fallback only for a warning shape not yet migrated. Regression tests: `test_V6_F55_ext_needsReviewRef_warning_not_dismissible_while_live`, `test_V6_F55_ext_needsReviewRef_warning_dismissible_once_stale`, `test_V6_F55_ext_freetext_warning_dismissible_despite_label_word_collision`, `test_V6_F55_ext_reconcile_clears_needsReviewRef_warning_by_exact_key` (`tests/v6-55-correction.mjs`). **Fixed, verified** (`0.6.6.2`, `V66`). Eyal's QA retest: each of the four previously-permanently-stuck Panvaree Resort warnings (duplicate booking number, check-in/check-out date-labeling ambiguity, RTL phone-formatting note, truncated address) now shows a dismiss control, and dismissing it clears the warning. |

**Symptom B (Panan Krabi — a warning cleared only after a genuine value change, not by simply reopening/reconfirming the item) — assessed as very likely expected, by-design behavior, not registered as a finding:** Panan Krabi's date-label-conflict warning is the same wild-card source `V6-F13`'s history refers to; this project's own earlier investigation of it (this session, before `V6-F49` existed) found its warnings used the colon-prefixed "דורש בדיקה: " format specific to `unresolved`-sourced text at that time. Eyal's report that this retest's warning shows as structured `needs_review` fields is consistent with the tightened Hotel system prompt (`V6-F49`'s fix) working as intended this time — resolving the same kind of conflict into a proper field instead of free text. If so, this warning is `needsReviewFields`-shaped, not `unresolved`-shaped, which correctly predicts both halves of what Eyal saw: no dismiss button (dismiss only ever applied to the `unresolved` shape) and a requirement to make a genuine field-value change to clear it (`reconcileStaleNeedsReview`'s `changedFields` is a strict string-inequality comparison against whatever is currently stored at save time — opening the item and re-saving the same value produces an empty changed-field set and clears nothing; this is pre-existing, unchanged behavior already true for the suggestion review screen before `V6-F54`, not something `V6-F54` introduced). This reading could not be confirmed with full certainty — it would require either the exact warning text (the dash vs. colon prefix settles it definitively) or a direct read of the evidence panel's field-level certainty for this item, neither available from here. Flagged as an assessment, not a registered finding; happy to verify for certain later if wanted.

### First `0.6.6` acceptance session — remaining investigations closed out (added `V61`)

Four more investigation prompts from `docs/claude-code-prompts/0_6_6_1/` (Follow-up 3 of the `V6-F56`/`V6-F57` thread, plus three new AE-case reports) investigated and registered below, `V6-F59`–`V6-F65`. No fix implemented for any of them.

| Finding | Observed in | Test/evidence source | Observed vs. expected behavior | Severity | Assignment status |
|---|---|---|---|---|---|
| `V6-F59` — `AE-006`'s saved item title is "WaterLand" (the venue name) instead of the frozen "כניסה לפארק יום" (day park entry ticket-type label) | `0.6.6` QA, `AE-006` retest | Direct code inspection of `src/activity-import-adapter.js` line 101 (`title:activityName`) and the `activityName` paragraph of `activitySystemPrompt` (`netlify/functions/_shared/smart-import-schema.mjs`) | Observed: saved title is the venue's own name. Expected (frozen ground truth, discovery doc `V4`, which explicitly corrects an earlier draft that had mistakenly used a zoo-themed title): "כניסה לפארק יום". Root cause: `title:activityName` (line 101) passes the model's `draft.activityName` straight through with no fallback-to-provider logic anywhere in the adapter — ruled out as a code bug by direct inspection, there is no transformation in this codebase that could turn a correct ticket-type label into the venue name. The live model's own output must have been "WaterLand". The prompt's `activityName` paragraph explicitly permits "the venue/attraction **or** event name" as two alternative satisfying categories with no stated priority rule for a source (like `AE-006`) that shows both a prominent venue name and a smaller ticket-type label — unlike `location`'s explicit property-first discipline, there is no analogous priority rule for `activityName`. Notably the prompt's own worked example for the structurally identical `AE-003` shape ("כניסה למוזיאון", general museum entry) already demonstrates the desired pattern correctly, which argues this may be either an isolated live-extraction inconsistency on this one document, or a genuine ambiguity in the instruction that the example alone doesn't fully close — cannot be settled further without live model access. | Medium — fails one benchmark case's title check outright, but the adapter code itself is correct/verbatim and the prompt already has one correct worked example of this exact pattern; likely fixable by adding an explicit venue-vs-ticket-type-label priority rule to `activitySystemPrompt`, mirroring `location`'s property-first paragraph | **Reopened — root-caused, not fixed** (`V64`); target `0.6.6.1` correction resolved the original symptom but not the actual requirement, awaiting Product Owner decision on a follow-up package. Eyal's retest: the immediate bug (title = "WaterLand," the bare venue name) is gone, but the fix's replacement ("Entry Ticket," the bare ticket-type label alone) isn't what's wanted either — **new, more specific requirement from the Product Owner:** combine venue name and ticket type, e.g. "WaterLand - Day Entry" / "WaterLand - כניסה לפארק יום," not either alone. Root cause, confirmed by direct comparison against the shipped prompt text: the `V6-F59` fix added a strict *either/or* preference rule ("prefer the specific ticket-type/admission label as activityName, not the bare venue name... only fall back to the venue name itself when the source genuinely shows no more specific ticket-type/admission label at all") — this instruction was never written to produce a combined value, only to pick one of the two, so the model correctly followed it and picked the ticket-type label alone, exactly as instructed. Closing this requires rewriting the `activityName` paragraph's priority rule as a combination instruction (venue name + ticket type, in that order) rather than an either/or preference. **Implemented (`0.6.6.2`, `V65`):** `activitySystemPrompt`'s `activityName` paragraph rewritten from a strict either/or preference to a combination rule — when a source shows both a venue/brand name and a distinct ticket-type/admission label, report BOTH together as `activityName` ("<venue name> - <ticket type>", e.g. "WaterLand - Day Entry" / "WaterLand - כניסה לפארק יום"), falling back to whichever ONE the source actually shows only when the other is genuinely absent. Prompt-only change — the adapter's `title:activityName` passthrough is unchanged (re-verified by direct reading, not assumed). Regression test: `test_V6_F59_title_preserves_combined_venue_and_ticket_type` (`tests/v6-6-attraction-event-import.mjs`) — asserts the adapter preserves an already-combined `activityName` verbatim, guarding against a future adapter change that tries to split or cherry-pick one half. **Fixed, verified** (`0.6.6.2`, `V66`). Eyal's QA retest: a fresh `AE-006` reimport shows the title as a combination of venue name and ticket type ("WaterLand - Entry Ticket"), not either alone. |
| `V6-F60` — a date-only value (no time in the source) renders with a synthetic "12:00" that is visually indistinguishable anywhere in the app from a genuinely extracted time | `0.6.6` QA, `AE-006` retest ("24 באפר', 2024, 12:00" shown for a source with no time at all) | Direct code inspection: `src/activity-import-adapter.js` line 152 (`dateTime()`), `src/operational-data.js` line 158 (`parseTimeValue()`), `src/v5-app.js` line 37 (`fmt()`) and line 150 (`detailView()`'s `fmt(item.startAt)` call) | Observed: `AE-006`'s date/time display shows a specific 12:00 time. Expected per frozen ground truth: this source is explicitly a day-ticket with no time shown at all — no time should appear to read as extracted. **Confirmed by direct code trace, not model fabrication:** `dateTime(date,time)` (`activity-import-adapter.js:152`) computes `const clock=parseTimeValue(time)||'12:00'` — `parseTimeValue('')` (`operational-data.js:158-164`) provably returns `''` for empty input, so the `||'12:00'` fallback is a deliberate, code-level neutral default for a date-only value (exactly matching the existing regression test's own comment for this exact case: `tests/v6-6-attraction-event-import.mjs` line 223, `'T12:00 is the neutral compatibility time for a date-only value'`). The adapter does correctly track this distinction separately in `dateMeta.startPrecision:'date'` (line 126) — but `fmt()` (`v5-app.js:37`) and every call site of it, including `detailView()`'s `fmt(item.startAt)` (line 150, called with no second argument), never consult `dateMeta.startPrecision` at all — `fmt()` always renders the hour/minute portion unconditionally. This is a generic gap, not `activity`/`AE-006`-specific: every item type across the app that can produce a date-only `startAt` (Hotel, Flight, Activity) has this same synthetic-time-looks-real problem. | Low — not incorrect data, but a real, systemic display-trust gap: a user has no way to tell a real extracted time from a neutral placeholder anywhere in the app | **Fixed, verified** (`V64`, target `0.6.6.1`) — Eyal's retest: a fresh `AE-006` reimport shows the date with no time at all in the main detail view, no more misleading "12:00" |
| `V6-F61` — `AE-002`'s saved item shows "Purchaser: Purchaser: אייל בן יצחק" (the label doubled, and in English while every sibling label on the page is Hebrew) | `0.6.6` QA, `AE-002` retest | Repository-wide grep for the literal string "Purchaser" — **zero matches anywhere in application source**, ruling out a hardcoded template/default string; direct code inspection of `src/activity-import-adapter.js`'s `otherFields`/`noteLines` fallback (lines 55, 68, 87) | Observed: the label text appears twice before the name, in English. Expected: a single, Hebrew-localized label (or no such field at all if genuinely unmodeled). Root cause, the only mechanism in the codebase consistent with the exact observed string (confirmed nowhere else "Purchaser" is hardcoded): the "purchaser/orderer" concept has no canonical field key in `FIELD_TARGETS`/`activitySystemPrompt`'s field vocabulary at all — the same class of gap `V6-F56` found for DNI. An unmapped field the model itself labels and keys (e.g. `key:"purchaser"`, `label:"Purchaser"`) falls through to `else otherFields.push(field)` (line 68), and `otherFields.map(field=>`${field.label}: ${field.rawValue}`)` (line 87) then renders `${field.label}: ${field.rawValue}` into `item.notes` with no guard against `field.rawValue` itself already containing a copy of the printed label text (plausible if the model transcribed the ticket's own printed "Purchaser: <name>" string verbatim into `rawValue` while separately choosing `label:"Purchaser"` for the same field) — this exact mechanism reproduces the observed doubled string precisely. The English label is explained the same way: since "purchaser" has no canonical, pre-translated Hebrew UI label like every other officially-mapped field, whatever language the model freely chooses for its own ad hoc `label` leaks directly into the UI unfiltered. Could not be confirmed against the live model's actual raw output (no DB access), but no other code path in the repository could produce this exact string, and the "no canonical field" explanation is directly provable from the schema/vocabulary alone. | Low — cosmetic, notes-only, does not corrupt any structured field; fixable either by adding a canonical `purchaser`/orderer field with a proper Hebrew label, or by stripping a `field.label`-prefix from `field.rawValue` before concatenating in the generic fallback | **Reopened — root-caused, not fixed** (`V64`); target `0.6.6.1` correction was incomplete, awaiting Product Owner decision on a follow-up package. `AE-002`'s original purchaser instance is genuinely fixed — Eyal's retest confirms a single Hebrew-localized label there — but the identical symptom reappeared on a fresh `AE-005` reimport: "Payment status: Payment status: paid," "Booked by: Booked by: [name]," "Ticket number: Ticket number: 2ZTN-G53R-GJ81P," all doubled and in English. Root cause, confirmed by direct code inspection, broader than the original fix reached: (1) the fix (`normalizedGenericLabel`/`stripDuplicateLabelPrefix`) was applied only to `activity-import-adapter.js`'s `otherFields`→notes mapping — the byte-identical, entirely unfixed pattern (`otherFields.map(field=>\`${field.label}: ${field.rawValue}\`)`, no stripping, no translation) exists in `smart-import-adapter.js` (Hotel) too, confirmed by direct comparison — Hotel is equally vulnerable and was never touched; (2) both adapters' separate `importantNotes` mapping (`(draft.importantNotes||[]).map(note=>\`${note.title}: ${note.text}\`)`) has zero stripping/translation applied in either adapter — if the model reports an unmapped concept via `importantNotes` instead of `fields[]` (a real, plausible alternate path for the same "no canonical slot" root cause), it reproduces the identical doubled/English symptom completely unaffected by the existing fix; (3) the translation half of the fix relies on a small hardcoded synonym dictionary (`purchaser`/`buyer`/`orderer` only) that cannot scale to arbitrary field concepts a live model may invent ("Payment status," "Booked by" are not in it) — confirmed Flight is NOT affected by any of this, since `flight-import-adapter.js`'s `notes` field is always empty (`notes:''`), so this pattern never executes there at all. A genuinely centralized fix needs to apply generic label/value duplicate-stripping at every note-line construction site in both adapters (both the `otherFields` and `importantNotes` mappings), and likely stop relying on a hardcoded translation dictionary rather than trying to keep enumerating synonyms one at a time. **Implemented (`0.6.6.2`, `V65`):** `normalizedGenericLabel`/`stripDuplicateLabelPrefix` moved out of `activity-import-adapter.js` into `src/operational-data.js` as the ONE shared implementation (dictionary extended with `payment status`/`booked by`/`ticket number`, though full translation coverage was explicitly not the required fix — the dedup mechanism is), applied at all four note-line generation sites across both adapters: Hotel's `otherFields` mapping (previously unfixed, byte-identical to Activity's original bug), and both adapters' `importantNotes` mapping (previously untouched entirely — very likely the actual cause of the reopened `AE-005` evidence, which reads as `importantNotes` entries). Activity's `otherFields` site was already correct and is simply repointed at the shared import. Regression tests: `test_V6_F61_activity_important_notes_label_not_doubled` (`tests/v6-6-attraction-event-import.mjs`), `test_V6_F61_hotel_other_fields_label_not_doubled`, `test_V6_F61_hotel_important_notes_label_not_doubled` (`tests/v6-correction.mjs`). **Fixed, verified** (`0.6.6.2`, `V66`), for the scope this fix targeted. Eyal's QA retest: a fresh `AE-005` reimport shows no more literal doubled labels ("Payment status," "Booked by," "Ticket number" each appear once). A related but structurally distinct sub-case surfaced on the same retest (a translated canonical label followed by a differently-worded, same-meaning source-embedded prefix, which exact-string duplicate detection does not catch) — registered separately as `V6-F67` rather than reopening this finding; see that row. |
| `V6-F62` — `activity` item type has no attach-existing-document control at all, unlike Hotel/Flight | `0.6.6` QA, `AE-003`/`AE-004` retest | Direct code inspection: `src/v5-app.js` line 150, `detailView()`'s actions block | Observed (Eyal's own hypothesis, confirmed): `AE-003` and `AE-004` (the Bloomfield Science Museum companion/duplicate-source pair) landed as two fully independent items with no way to link them. Expected: per the frozen discovery doc, this pair "must land as two related items or one item with two attached sources." Root cause, confirmed directly from code: `detailView()`'s actions block reads `${['hotel','flight'].includes(item.type)?`<button ... data-action="attach-source" ...>הוספת מסמך</button>`:''}` — the attach-document button is hardcoded to exactly two item types. `activity` (and every other type — car, restaurant, insurance) has no attach control rendered at all, so there was never a way for Eyal to attach `AE-004` to the already-saved `AE-003` item even if he had wanted to, regardless of whatever duplicate-detection logic exists. This is a missing capability, not a duplicate-detection regression: `findPossibleDuplicates` (`src/ingestion.js` lines 88-111, wired into the live global-Add review flow via `src/v5-app.js`) is real, working code — proven by the passing `test_AE004_auditorium_companion_pair_becomes_two_related_items` regression test, which shows it correctly flags `AE-004` as a possible duplicate/companion of an already-approved `AE-003` item when given matching `provider`+date. Its exact-string matching rule (`sameType&&((sameProvider&&sameDate)||(sameTitle&&sameDate))`, `src/ingestion.js:109-110`, `normalized()` is only trim/lowercase/whitespace-collapse — no fuzzy matching) is precisely why it likely never fired on Eyal's *real* global-Add upload of both documents even independent of the missing attach control: see `V6-F63`, whose provider-extraction defect ("SmarTicket.co.il" vs. "SmarTicket" — not even identical to each other, let alone matching "Bloomfield Science Museum Jerusalem") would break the `sameProvider` precondition this dedup path depends on, while the titles are legitimately, correctly different by design (general entrance vs. specific show) so `sameTitle` was never going to catch it either. Both mechanisms (missing attach control, and the dedup path's dependency on a provider value `V6-F63` shows was wrong) plausibly contributed; the missing attach control is the more fundamental gap since Hotel's own precedent (Panvaree Resort, this same session) proves attach-after-the-fact is the intended, expected companion-source workflow. | Medium — blocks a core, frozen companion-source workflow for an entire item type, not an edge case (Bloomfield's general-entrance-plus-paid-show shape is a common real pattern for museums/venues) | **Fixed, verified** (`V64`, target `0.6.6.1`) — Eyal's retest: the attach control exists on activity items, and attaching `AE-004` to an existing `AE-003` item correctly became a second source on the same item, not a duplicate second item |
| `V6-F63` — `activity` extraction's `provider` field has no prompt guidance on venue-vs-ticketing-platform priority, producing inconsistent, platform-named results | `0.6.6` QA, `AE-003`/`AE-004` retest | Direct code inspection: `netlify/functions/_shared/smart-import-schema.mjs`, full text search of `activitySystemPrompt` for "provider" — zero paragraphs of guidance found, only the bare schema declaration (`provider:{type:'string'}`, line 96) | Observed: `AE-003`'s saved item shows `provider:"SmarTicket.co.il"`, `AE-004` shows `provider:"SmarTicket"` — two different strings for what should be the identical venue. Expected (frozen ground truth for both cases): `provider: Bloomfield Science Museum Jerusalem ... sold via SmarTicket.co.il` — ground truth treats the museum as `provider` and SmarTicket as a distinct "sold via" concept, not the same field (contrast `AE-002`, where ground truth explicitly makes the ticketing platform itself the `provider` — so this is a real, source-dependent distinction the prompt needs to make, not a fixed rule). Root cause: unlike `activityName` and `location`, both of which get dedicated explanatory paragraphs in `activitySystemPrompt` establishing exactly this kind of priority discipline, `provider` has zero prompt guidance at all beyond the one-line schema type declaration — the model has nothing telling it to prefer the venue over a ticketing platform when a source shows both, so it inconsistently picked the platform on both `AE-003` and `AE-004` (not even the same string between them, ruling out this being merely "the wrong but consistent choice"). This is also the direct explanation for why `V6-F62`'s dedup path never had a chance to catch the pair even via the global-Add route. | Medium — a required-when-present field per Decision 4, and the specific defect that breaks `V6-F62`'s dedup path; fixable by adding an explicit provider-priority paragraph to `activitySystemPrompt`, mirroring `location`'s | **Fixed, verified** (`V64`, target `0.6.6.1`) — Eyal's retest via the `AE-003`/`AE-004` attach-merge picker: provider shows "Bloomfield Science Museum Jerusalem" on both sides of the conflict, no "SmarTicket" anywhere |
| `V6-F64` — `AE-004`'s `location` shows only the sub-venue ("אודיטוריום") with the venue name dropped entirely | `0.6.6` QA, `AE-004` retest | Direct code inspection: the `location` paragraph of `activitySystemPrompt` (`netlify/functions/_shared/smart-import-schema.mjs`) | Observed: `AE-004`'s `מיקום:` field shows only "אודיטוריום" (Auditorium); `AE-003` (same venue, general entrance) correctly shows the full museum name for the same field. Expected (frozen ground truth): "Bloomfield Science Museum Jerusalem, Auditorium (אודיטוריום)" — both venue and sub-venue combined. Root cause: the `location` paragraph establishes the property-first discipline ("must identify the specific venue/attraction, never a general park, city, district or region as a stand-in for it") and separately instructs that a genuinely distinct meeting/transfer point belongs in its own `meeting_point` field — but gives no instruction at all for the case actually present here: a *sub-venue within* the main venue (an auditorium, a specific hall, a wing) that should be *combined with*, not substituted for, the venue name. This is a possible property-first location-accuracy issue per Decision 4 ("required when present, not [a no-omission] blocker" — but still graded ground truth), not merely cosmetic, since "Auditorium" alone doesn't identify which venue's auditorium — consistent with `AE-003`'s own correct result on the identical venue, confirming this is a source-dependent extraction gap for the sub-venue case specifically, not a systemic location failure. | **High** (elevated from an initial Medium proposal at Product Owner request, `V62`: location accuracy is one of Decision 4's explicit no-omission blockers for the `activity` item type, not a required-when-present field like `V6-F63`'s `provider` — this fails a hard project rule, not merely a nicety, since "Auditorium" alone doesn't identify which venue) — fixable by adding a sub-venue-combination instruction to `activitySystemPrompt`'s `location` paragraph | **Fixed, verified** (`V64`, target `0.6.6.1`) — Eyal's retest via the same picker: the location conflict correctly offered "Bloomfield Science Museum Jerusalem, Auditorium" as the new value |
| `V6-F65` — `AE-007`'s `provider`/`location` show a specific real-world venue name ("גן החיות התנ״כי ירושלים" / Jerusalem Biblical Zoo) that does not appear in any of the source's independently-verified legible plain text | `0.6.6` QA, `AE-007` retest | Direct visual inspection of the actual source file, `tests/fixtures/attraction_benchmark/AE-007_zoo_ticket_screenshot.png` (cropped, rotated and magnified via PowerShell `System.Drawing` for legibility, since the Read tool's default rendering was too small to fully verify); code inspection of `netlify/functions/_shared/place-validation.mjs` | Observed: both `provider` and `location` show the specific brand name. Frozen ground truth (discovery doc): the source only shows the generic label "גן החיות" (the zoo) — "venue's own precise name is not legible beyond this generic label in the source — frozen as a real location-precision limitation, not resolved by assumption." **Directly confirmed by reading the actual source image**: the ticket's main printed body text reads "כניסה לגן החיות 2020" (Entry to the Zoo 2020) — generic, no brand name; the PDF-viewer's own filename title bar, also visible in the screenshot, independently reads "...צתי גן חיות pdf(1)" — also generic, no brand name. Two independent, fully legible plain-text occurrences on the source, both consistent with the frozen ground truth's claim. The source also contains one small stylized decorative logo/wordmark graphic (bottom-right of the ticket, alongside an animal-silhouette pictogram) that could not be read with confidence despite repeated crop/rotate/magnify attempts at up to 10x — the image's native resolution (a screenshot of a screenshot, per the discovery doc's own description) does not support confident letter-by-letter reading of that stylized font, so genuine uncertainty remains about that one specific piece of the source, honestly flagged rather than guessed either way. **Ruled out as an alternative explanation, directly from code:** the place-validation mechanism (`validatePlace()`, `place-validation.mjs` line 24) sets `locationDisplayValue:name` — always echoing back the originally-extracted name, never the Google Places API's own canonical `candidate.displayName.text` — so a Places lookup could not have introduced a more specific name into `location`; and `provider` (`activity-import-adapter.js` line 102, `String(draft.provider||'').trim()`) is never touched by place validation at all. Both fields' specific values therefore trace directly back to the model's own raw extraction output, not to any code-level substitution. Combined, this is strong evidence — though not fully absolute, given the one unread logo — that this is a genuine violation of `activitySystemPrompt`'s own existing general instruction ("Do not fabricate, silently correct, infer from general knowledge... leave a string field empty... rather than guessing"), the exact failure mode this benchmark case was specifically designed to catch. This is a live-model compliance issue, not a missing code/schema guardrail (contrast `V6-F56`, where the guardrail itself doesn't exist) — the existing instruction is already correct and present; the model did not follow it on this real extraction. | High — per the investigation prompt's own instruction, given the balance of evidence; this is a direct, on-point failure of a benchmark case purpose-built to test against exactly this behavior, and a fabricated specific identity is materially worse than a merely-missing field. **Confirmed effectively-confirmed by Product Owner (`V62`):** Google Places lookup already ruled out as the source of the specific name; the one unresolved detail (the small stylized logo graphic that couldn't be read with confidence) is noted as a caveat but does not block treating this as confirmed. | **Reopened — root-caused, not fixed** (`V64`); target `0.6.6.1` correction reduced the surface symptom but did not fix the underlying behavior, awaiting Product Owner decision on a follow-up package. Eyal's retest: a fresh `AE-007` reimport correctly shows `location` as the generic "גן החיות" (that part of the fix worked), but `provider` now shows "החברה להגנת הטבע" (Society for the Protection of Nature) — a different specific organization, attributed by the item's own warning text to being "identified from the logo/text on the ticket." **Eyal confirmed directly against the real source that this text does not appear anywhere on the ticket.** So the known-brand-substitution failure mode the original fix targeted is gone, but a more subtle, arguably more dangerous version of the same underlying defect remains: when a source element (here, a small stylized logo) is visually present but not confidently legible, the model still outputs a specific plausible organization name rather than saying it can't be determined — and packages that guess with a citation ("identified from the logo") that makes it read as more credible, not less. Root cause, confirmed by direct schema inspection: `provider` and `activityName` are bare top-level strings in `activityImportSchema` (`type:'string'`, no paired certainty field), unlike every entry in `fields[]`, which carries its own `certainty:'exact'|'needs_review'|'unreadable'`. The `fields[]` key vocabulary also has no `provider`/organizer slot at all. So the model has no structured way anywhere in the schema to express "I can see something here but can't confirm what it says" for this specific field — it can only output a confident-looking string or leave it empty; there is no visible "unconfirmed" state available for it to choose, regardless of how the prompt is worded. The `V6-F65` anti-fabrication paragraph already shipped addresses "don't substitute a plausible KNOWN BRAND for a generic label" specifically, not this distinct "don't guess at all when a graphic can't be confirmed" case — closing this fully likely needs either a schema change (a certainty/hedge mechanism for `provider`/`activityName`) or an explicit instruction that an illegible graphic element must be treated as absent rather than a basis for a specific guess, not just a stronger negative example. **Implemented (`0.6.6.2`, `V65`):** `activityImportSchema` gained paired `activityNameCertainty`/`providerCertainty` enums (`exact`/`needs_review`/`unreadable`, both added to `required`), giving the model a structural way to say "visible but illegible" instead of guessing; `activitySystemPrompt` gained a paragraph explaining exactly when to use each value, restating the anti-fabrication rule concretely for this specific, previously-ungoverned case. `activity-import-adapter.js` adds defense in depth on top of the prompt fix: on `unreadable`, the value is force-blanked regardless of what the model actually returned (proven by a fixture that simulates the model still returning a guessed name despite declaring it unreadable) and a `needsReviewRef`-backed `needsReviewFields`/`fieldConfidence` entry is added; on `needs_review`, the value is kept but still flagged. Regression tests: `test_V6_F65_ext_unreadable_provider_certainty_blanks_value_and_flags_illegible`, `test_V6_F65_ext_exact_provider_certainty_unaffected`, `test_V6_F65_ext_unreadable_activityName_certainty_blanks_value_and_flags_illegible`, `test_V6_F65_ext_needs_review_certainty_keeps_value_and_flags` (`tests/v6-6-attraction-event-import.mjs`). **Fixed, verified** (`0.6.6.2`, `V66`), for the scope this fix targeted. Eyal's QA retest: a fresh `AE-007` reimport now returns a confident, specific provider name that Eyal confirmed against the actual source is genuinely correct — no longer an unverifiable/false "identified from the logo" claim. This exercised the normal `certainty:'exact'` path (the source turned out to be legible enough this time), not the new `unreadable` path — that mechanism's own behavior (blank + flag when genuinely illegible) is covered by the fixture-based regression tests, not by this specific live retest, since a live source that happens to be legible can't exercise it. An unrelated minor issue surfaced on the same item's ticket-holder name field — registered separately as `V6-F68`. |
| `V6-F66` — attaching a second per-person Activity ticket document to an existing item silently REPLACES the first document's ticket-holder/participant data instead of combining them, real data loss | `0.6.6.1` QA, mandatory wild-card retest (two real Hapoel Tel-Aviv vs. Beitar Jerusalem match tickets, one per person) | Manual observation: Eyal attached both documents to one `activity` item, expecting both people as ticket holders (the same shape as `AE-001`'s two-passenger case, just split across two source files); direct code inspection of `src/ingestion.js`'s `suggestionToItem()` and `src/smart-import-adapter.js`'s `preserveTrustedFieldsOnMerge()` | Observed: only the second-attached person (Paula Cohen) survived in the saved item; Eyal's own ticket (Gate 8, entrance 426, section 76B, row 35, seat 201) is completely absent, not merely unrendered — genuine data loss, not a `V6-F57`-style display gap. Expected: both per-person ticket-holder entries present, evidence-linked, the same "union, never silently replace" guarantee Flight's own multi-passenger merge already provides. Root cause, confirmed by direct code inspection: `suggestionToItem()` (`src/ingestion.js` line 131-133) merges `details` for every non-flight type with a plain object spread — `{...(existing.details||{}),...(p.details||{})}` — so when both the existing item's and the newly-merged suggestion's `details` contain a `ticketHolders` (and `ticketNumbers`) key, the second source's array wins outright; nothing unions the two arrays the way Flight's `mergeFlightDetails`/`mergeFlightPassengers` (matched by e-ticket number or normalized name) already does for exactly this class of problem on the Flight side. The same gap independently affects the top-level `participants` array too: `suggestionToItem`'s generic `value(key,fallback)` helper (line 116) picks the incoming suggestion's array outright whenever it is non-empty, never unioning it with the existing item's own non-empty array. `preserveTrustedFieldsOnMerge` (`src/smart-import-adapter.js`) offers no protection either — its `MERGE_FIELDS` list (`title/provider/confirmationNumber/location/website/phone/startAt/endAt`) covers only top-level scalars, never `participants` or any `details.*` array. This is not Activity-specific code with a bug in it — Activity has no merge-specific code for this at all; it inherits the same generic, array-unaware merge path every non-flight type has always had, which was harmless before `V6-F62` added the first realistic way to attach a second per-person Activity document to an existing item. | High — real, silent participant/ticket-holder data loss on a documented, no-omission-style requirement (mirrors `V6-F56`'s severity class: a benchmark-grade guarantee genuinely failing, not a display gap); discovered via the mandatory wild-card test specifically because it exercises a real multi-document merge the frozen `AE-00x` benchmark set never happened to test (every `AE-` case's ticket holders come from ONE source document, never split across two attach-and-extract merges) | **Implemented** (`version/V6`, `0.6.6.2`/`V65`): `mergeActivityTicketHolders`/`mergeActivityDetails` added to `src/activity-import-adapter.js`, mirroring Flight's already-solved `mergeFlightPassengers`/`mergeFlightDetails` — match by `ticketNumber` when present (most reliable, mirrors Flight's e-ticket-number priority) else normalized `name`, union all entries from both sides so no ticket holder from either side is ever dropped, existing non-blank field wins per matched entry with blanks filled from the incoming side; a small set of Activity detail scalars get the same "existing wins unless blank" rule, and `ticketNumbers` is deduped-unioned. `suggestionToItem` (`src/ingestion.js`) now branches on `type==='activity'` the same way it already does for `type==='flight'`, importing `mergeActivityDetails`; the top-level `participants` array is fixed the same way, but ONLY for `type==='activity'` — every other type's existing either/or `value('participants',...)` behavior is left untouched, to avoid any unintended behavior change for Hotel/Flight/manual items. Regression tests: `test_V6_F66_merge_activity_ticket_holders_preserves_both_people`, `test_V6_F66_merge_activity_participants_union_not_overwrite`, `test_V6_F66_merge_activity_ticket_holders_fills_blank_fields_from_either_side` (`tests/v6-6-attraction-event-import.mjs`) — the first two mirror Eyal's own wild-card repro (two real per-person tickets attached in two passes), the third proves a partial-overlap (same `ticketNumber`, complementary blank fields) merges rather than one side winning wholesale. **Fixed, verified** (`0.6.6.2`, `V66`). Eyal's QA retest: repeated the original wild-card two-document attach test (his own ticket, then Paula Cohen's, attached to the same item) — both people's ticket-holder entries are now present in the saved item, with both names in participants. |
| `V6-F67` — a translated canonical label followed by a differently-worded, same-meaning source-embedded prefix reads as a redundant compound line (e.g. "מזמין: הוזמן על ידי: אייל בן יצחק" — "Purchaser: Ordered by: Eyal Ben Yitzhak") | `0.6.6.2` QA retest, `V6-F61` verification pass | Manual observation, real item retest (Eyal) | Observed: the label "מזמין" (purchaser, via `normalizedGenericLabel`'s translation of the model's own canonical `label`, e.g. "Orderer") is immediately followed by the source's own embedded prefix "הוזמן על ידי:" ("ordered by:") inside the same value — a different phrasing of the same concept, not a literal repeat of "מזמין" itself. Expected: a single, non-redundant line. Root cause (not yet confirmed by code inspection — flagged for investigation): `stripDuplicateLabelPrefix` (`src/operational-data.js`) only strips a value's own embedded prefix when it is an exact, case-insensitive literal match against `field.label` — it has no concept of two different strings meaning the same thing, so a canonical label translated to Hebrew (`normalizedGenericLabel`) and a source-language prefix already embedded in `rawValue` (here, the source's own Hebrew phrasing) never match textually even though a human reads them as saying the same thing twice. | Low — cosmetic, notes-only, narrower than `V6-F61`'s original scope (that fix's own literal-duplicate detection is confirmed working; this is a distinct synonym/translation case it was never designed to catch) | **New — registered, not yet investigated further or assigned a target version.** Awaiting Product Owner decision on whether/when to package a fix. |
| `V6-F68` — a ticket holder's name renders with a one-character misread ("אילי" instead of "אייל"/Eyal) on a fresh `AE-007` reimport | `0.6.6.2` QA retest, `V6-F65` verification pass | Manual observation, real item retest (Eyal) | Observed: the saved item's ticket-holder table shows the name "בן יצחק אילי" where the correct name (matching every other benchmark case's use of the same real person) is "אייל בן יצחק" (Eyal Ben Yitzhak). Expected: the name transcribed correctly. Root cause: not yet investigated — this could be a live-model OCR/transcription misread of this specific source image (no code defect at all), or a genuine adapter/rendering issue; nothing in this finding has been code-inspected yet. | Low, pending investigation — a single-character name misread on one field, not a structural or data-loss issue like `V6-F56`/`V6-F66`, but a real accuracy defect on a personal-identifier field if it turns out to be systematic rather than a one-off live-model misread | **New — registered, not yet investigated. Awaiting investigation to determine whether this is a genuine code-fixable defect (e.g. a transcription-normalization gap) or an isolated live-model extraction inconsistency, then Product Owner decision on packaging.** |

| Finding | Observed in | Test/evidence source | Observed vs. expected behavior | Severity | Assignment status |
|---|---|---|---|---|---|
| `V6-F52` — sign-in screen shows a stale hardcoded version | `0.6.6` QA, build `0.6.6-3925bb20` | Manual observation, `0.6.6` QA acceptance session | Observed: the pre-authentication sign-in screen displays "Alpha 0.6.5" under the logo; the in-app About/landing banner correctly show `0.6.6-3925bb20 · Alpha 0.6.6` for the same build. Expected: every user-visible version label derives from the same build-injected source and can never drift independently — this is the exact class of defect `V6-F31` fixed for the landing banner, and (per Eyal) the same class as `V5-F05` (Alpha 0.5 era), which predates this project's mandatory-regression-test rule and almost certainly never received one. Root cause confirmed: `src/main.js`'s `renderLogin()` hardcoded `<p class="auth-kicker">Alpha 0.6.5</p>` as a plain literal — unlike `v5-app.js`'s `appVersionLabel()` (read `document.title`) and `buildVersionLabel()` (reads `<meta name="build-version">`), `main.js` never imported or called either helper; it was a fully disconnected hand-maintained string in a different file. **Implemented:** extracted `appVersionLabel()` into its own shared module, `src/app-version.js`, imported by both `src/main.js` and `src/v5-app.js` — the two screens now structurally cannot diverge again, since there is only one function that knows how to read the version. Regression test: `test_V6_F52_signin_version_matches_about` (`tests/v6-52-53-correction.mjs`), source-inspection style (asserts no hardcoded `Alpha 0\.\d` literal remains, and both files import the one shared source) — the same pattern this codebase already uses for DOM/Firebase-dependent UI code that can't run in the plain-Node test harness. Two pre-existing tests that had literally encoded the old hardcoded string as expected behavior were also corrected (`tests/v5-correction.mjs`, `tests/v6-5-flight-import.mjs`'s `V6-F31` test) — worth noting as its own small instance of the exact defect class this finding is about. **Eyal's retest (`V62`):** sign-in screen, browser tab title, and About panel all now consistently read "Alpha 0.6.6" (About panel additionally shows build hash `0.6.6-9b5b1ed0`). | Low (cosmetic, pre-authentication only) but a direct recurrence of a known defect class | **Fixed, verified** (`version/V6`) |
| `V6-F53` — FL-003 flight direction/segment order inconsistent across identical immediate re-imports, no disambiguation shown | `0.6.6` QA, build `0.6.6-3925bb20` | Manual observation during Test 13 (short regression) of `EXECUTABLE_TEST_SESSION_V184.md` | Observed: importing `FL-003_ELAL_itinerary_summary_both_legs.png` twice in immediate succession, file unchanged, produced the wrong segment direction/order on attempt 1 and the correct order on attempt 2; the `directionAmbiguous`/`directionCandidates` picker (built specifically for this class of case — `V6-F32`/`V6-F34`, commit `716f950`, 2026-08-27) was never shown on either attempt. Expected per the frozen `flightSystemPrompt`: either a stable correct read every time, or — when the source is genuinely undeterminable — `directionAmbiguous:true` with both candidates surfaced for explicit Product Owner selection; never a silent, possibly-wrong guess with no visible uncertainty. Root cause: confirmed via git history (commit `716f950`) that FL-003 was already identified at the time as exhibiting genuine run-to-run non-determinism in the raw model extraction (same unchanged source producing correct, confidently-wrong, and refused reads across separate attempts during the original investigation) — not a deterministic code defect, and explicitly not fixable by further prompt-tuning per that commit's own conclusion. The `directionAmbiguous` escape valve built in response is itself correct and unit-tested at the adapter level, but depends entirely on the model correctly self-diagnosing "this is genuinely ambiguous" on a given call — a decision with no determinism control at all (confirmed by direct code inspection: `netlify/functions/smart-import.mjs`'s `client.responses.create()` calls set only `reasoning:{effort:'low',context:'current_turn'}` — no `temperature`, no `seed`). Eyal's observation (two different confident guesses, escape valve never triggered) is consistent with this same previously-documented non-determinism resurfacing in the one place the fix could never make deterministic — not a regression in the picker/adapter mechanism itself, which is unchanged and still passes every existing test. Two referenced documents could not be located anywhere in the repository or the broader project directory tree and could not be used as evidence: `FLIGHT_SMART_IMPORT_DISCOVERY_V1.md` (no file by that name exists anywhere), and `EXECUTABLE_TEST_SESSION_V169.md` (referenced repeatedly in this very document as the "full record" of `0.6.5`'s QA/Production acceptance, but the file itself was apparently never created) — whether FL-003's direction was explicitly reconfirmed before the `0.6.5` GO could not be determined from any available record. **Implemented (Product Owner-approved approach — detect via repetition):** for a multi-segment flight source, `runExtraction` (`netlify/functions/smart-import.mjs`) now makes a second independent model call and cross-checks the two readings per segment (matched by flight number); a disagreement synthesizes `directionAmbiguous:true` with both readings as `directionCandidates`, routing through the existing picker mechanism unchanged — no new UI. Scoped to multi-segment sources only (`draft.segments.length>1`, detected from the first call's own result, since segment count isn't knowable beforehand) — Hotel/Activity extraction and single-segment Flight sources are unaffected, matching the accepted 2x cost/latency tradeoff for this shape only. Regression tests: `tests/v6-52-53-correction.mjs`, testing the exported, pure `reconcileFlightSegmentDirections` function directly with hand-constructed "first call"/"second call" segment pairs (disagreement → picker; agreement → unchanged; already-self-flagged → left alone; no matching flight number → fails open) plus a source-inspection check that the second call is genuinely gated to multi-segment sources — no live model call is mocked or invoked, matching this suite's existing pattern of testing deterministic logic directly rather than the live extraction itself. **Eyal's retest (`V62`):** re-imported `FL-003` — the two internal extraction calls genuinely disagreed on direction this time, the `directionAmbiguous`/`directionCandidates` picker correctly appeared, and Eyal resolved it to the correct direction — a real exercise of the disagreement-detection mechanism, not a lucky single-call pass. | Medium — data-accuracy risk (a wrong-but-confident direction could be approved unnoticed, no warning shown), self-correcting on retry in this observed instance | **Fixed, verified** (`version/V6`) |

`V6-F52` and `V6-F53` both now have passing named regression tests (`tests/v6-52-53-correction.mjs`) and are implemented on `version/V6` — see each entry above for detail. Neither is recorded `Fixed`; both await Eyal's QA retest per the standard's responsibility split.

A third finding, `V6-F54`, was registered from the same QA acceptance session (Eyal saved the `H-001` benchmark case, Panvaree Resort, and separately attached a companion document to it) — investigated and root-caused, not yet fixed:

| Finding | Observed in | Test/evidence source | Observed vs. expected behavior | Severity | Assignment status |
|---|---|---|---|---|---|
| `V6-F54` — a warning on a saved item can never be cleared, by any means, once the item exists | `0.6.6` QA, build `0.6.6-3925bb20` | Manual observation, `0.6.6` QA acceptance session (`H-001`/Panvaree Resort Hotel item) | Observed: the saved, approved Panvaree Resort item still showed two needs-review date warnings ("Arrival date: 2027-01-17", "Departure date: 2027-01-18", both correctly matching `H-001`'s frozen dates — the values themselves are right) after saving, not just during pre-approval review. Separately, attaching a companion document to the same saved item produced further warnings (phone/address/location review flags, plus three "ערך חדש התגלה עבור X" merge-conflict notices for phone/`startAt`/`endAt`) that were equally impossible to clear. Expected: a warning whose underlying issue has been reviewed, accepted, or resolved should be dismissible or should clear, the same way it already can on the pre-approval suggestion review screen (`V6-F49`'s dismiss control for `unresolved`-sourced warnings; `reconcileStaleNeedsReview`'s edit-triggered clearing for `needsReviewFields`-sourced ones). Root cause, confirmed by direct code inspection, not by inference: `detailView()` (`src/v5-app.js`) renders `item.warnings` as plain, non-interactive text — `${item.warnings?.length?...map(warning=>\`<div>⚠️ ${esc(warningText(warning))}</div>\`)...}` — no index tracking, no `isDismissibleWarning` check, no dismiss button at all. The item-edit save handler (`form.id==='edit-item-form'` in the same file) does `Object.assign(state.items.find(...), values)` and nothing else — it never calls `reconcileStaleNeedsReview` or touches `item.warnings` in any way. A repository-wide search for every `.warnings=` assignment confirms this precisely: every one of them touches only `suggestion.warnings`; none ever touches an item. Once `suggestionToItem()` copies `warnings` onto a newly created or merged item, that array is fixed for the remaining lifetime of the item — this predates `V6-F49`/`V6-F50` entirely (it is not a regression they introduced) and applies uniformly to every warning shape (`needsReviewFields`-sourced, `unresolved`-sourced/dismissible, and the plain-string merge-conflict warnings from `preserveTrustedFieldsOnMerge`) and every item type (Hotel, Flight, Activity) — this single gap fully explains both symptoms Eyal observed, which is why they are registered as one finding rather than split. Two related, separately-investigated points, both concluded **not** to be defects: (1) no website was saved for Panvaree Resort — confirmed correct against `SMART_IMPORT_REDESIGN_DISCOVERY_V22.md`'s frozen `H-001` ground truth, which explicitly lists "official website" among the source's explicitly-absent fields; (2) the merge-conflict behavior itself (existing value kept, new value flagged, no one-click "accept new value" control) is the frozen contract working as designed ("Duplicate detection must preserve non-empty trusted values... flagged for review", confirmed `V6-F33`/`V6-F39`) — a one-click accept action would be a legitimate future UX enhancement, not a contract violation, so it is not registered as its own finding here. One more referenced document could not be located anywhere in the repository or project tree, consistent with the pattern already reported for `V6-F53`: `SMART_IMPORT_HOTEL_BENCHMARK_CASE_H001_V3.md` (cited by `SMART_IMPORT_REDESIGN_DISCOVERY_V22.md` as `H-001`'s ground-truth document) — no standalone document by that name survives even in the separate Project knowledge store; only the inline `H-001` summary already embedded in `SMART_IMPORT_REDESIGN_DISCOVERY_V22.md` itself exists, and it is sufficient — the existing `H-001` test fixtures/regression tests already match it, confirmed independently during this investigation. Flagged and accepted as a permanent gap, not chased further. **Implemented:** a dismiss control added to `detailView()` for `isDismissibleWarning`-shaped entries, mirroring `suggestionView()`'s own (new `dismiss-item-warning` action, scoped to `state.items`); the item-edit save handler now computes a `changedFields` set the same way `updateSuggestion` already does and calls `reconcileStaleNeedsReview` on the item before saving. Regression tests: `tests/v6-54-correction.mjs` — `reconcileStaleNeedsReview` proven to work identically on a real saved-item shape (not just a suggestion's `proposed` object), plus source-inspection confirming both the edit-handler wiring and the dismiss-control rendering/action. Two pre-existing tests updated where they encoded the old rendering exactly (`tests/v6-5-flight-import.mjs`'s `V6-F33` test). **Eyal's retest (`V62`) found the fix incomplete on both cases** (no dismiss control visible anywhere in the UI on either item; Panvaree Resort's warning is now additionally stale/misleading, describing a conflict that already resolved correctly; Panan Krabi retains one warning even after editing every associated field) — this is the same underlying problem `V6-F55` already opened. **`V6-F54` is merged into `V6-F55` as of `V62` rather than closed separately; see `V6-F55`'s entry below for the combined finding, elevated severity, and Product Owner's proposed fix direction.** | High — a warning is meant to be a temporary review prompt, not a permanent, unremovable fixture; as implemented it degrades the item detail page's trustworthiness over time as attachments/edits accumulate warnings with no way to ever clear the noise, for every Smart Import vertical | **Merged into `V6-F55`** (`V62`) — not tracked separately |

Proposed fix (not implemented, per explicit instruction): two additions, mirroring what already exists for suggestions and only for suggestions. (1) A dismiss control on `detailView()` for `isDismissibleWarning`-shaped entries, the same interaction `suggestionView()` already offers, adapted to address an item (`state.items`) instead of a suggestion. (2) Extend the item-edit save handler to compute the changed-field set (the same way `updateSuggestion` already does) and call `reconcileStaleNeedsReview` against the item's own `details.needsReviewFields`/`warnings` before saving — currently only the pre-approval suggestion form does this; the item edit form never has. Both are additive, reusing existing, already-tested mechanisms rather than inventing new ones.

*The full frozen processing/privacy, universal source-landing, source-relative completeness/evidence and bounded external-enrichment contracts, the approved first vertical slice definition, the first-slice acceptance benchmark, explicit exclusions and the Alpha 0.6 release gate are unchanged from prior revisions and are preserved verbatim below.*

## Frozen processing and privacy contract

- `Attach only` is local and invokes no AI. `Analyze and propose` and `Attach and extract` are explicit per-attempt processing actions, preceded by a concise first-use disclosure for that account.
- Raw source bytes, transient OCR/layout artifacts and retrieved page content are deleted from the processing path immediately after completion/failure and no later than 24 hours after receipt. Eligible providers may not use FamilyTrips data for model training.
- The Alpha 0.6 original remains locally openable and linked to its Hotel until explicit confirmed deletion. Original-source cloud/cross-device synchronization is deferred to Alpha 0.7.
- Only the target Trip `Owner` may invoke paid AI processing in Alpha 0.6, with server-side invitation/ownership checks, attempt attribution and a per-user quota. `Editor` processing permission is deferred to Alpha 0.7.
- Passport, national-identity and visa documents are not processed by AI in Alpha 0.6. The Hotel extractor must not extract or retain a full payment-card number or security code.
- The 24-hour processing-copy rule is independent of revoked-account data: revocation still triggers immediate blocking and the separately approved 30-day quarantine/deletion-approval lifecycle.

## Frozen universal source-landing contract

- The same landing interaction applies to all current and future item types; support for that interaction does not imply that Alpha 0.6 can extract every type.
- Global Add presents **Analyze and propose item** as the default-selected path and **Save source only** as the no-AI path. Processing begins only after explicit confirmation and any required first-use disclosure.
- **Save source only** stores the unassigned original in the non-category Intake Inbox.
- Failed, unsupported or insufficient analysis creates no empty/generic item. The original remains in Intake Inbox with an explicit state and **Retry** / **Delete** actions. Retry now shows a loading indicator (`V6-F42` Closed).
- A successful analysis creates editable proposals only and requires approval before final creation.
- Every existing item exposes **Attach only** and **Attach and extract**. Until that item's extraction contract is supported, the second action reports a truthful unsupported state and keeps the source attached without changing structured data.
- Alpha 0.6 implements and benchmarks extraction for Hotel, with Flight (`0.6.5`) and Attraction/Event (`0.6.6`) approved as further vertical slices.
- Alpha 0.6 includes the minimum non-category Intake Inbox required by this contract.
- **No manual item-type picker is specified by this contract.** Removed in the `0.6.5` fix pass, confirmed working on retest.
- **Top-level Add options, revised:** `Manual` / `Document` / `Link` / `QR` / `JSON`. Confirmed working on retest. The per-item attach modal now matches — confirmed on retest (`V6-F38` Closed). **Note: `Manual` from the global floating "+" does not currently support creating a Task directly — new Alpha 0.7 item, see below.**
- **Today** now falls back to showing out-of-Trip-range items when it would otherwise be empty — confirmed working exactly as specified via `FL-008`. A smarter version is a `0.7` backlog item, not a `0.6.5` blocker.

## Frozen source-relative completeness and evidence contract

- Smart Import has no fixed absolute minimum field count. A sparse source may produce a sparse draft; a field absent from the source remains empty and is not fabricated. Confirmed again via `FL-007`: an invalid source time value is correctly left blank rather than guessed.
- Every readable travel-relevant value present in the source must appear in the review draft with evidence. For Hotel, dates, arrival/departure or check-in/check-out times/windows, location/address/map data, phone and booking/confirmation number are explicit no-omission blockers whenever present. For Flight, see `FLIGHT_SMART_IMPORT_DISCOVERY_V1.md`. For the Attraction/Event proposal under discovery, see `ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` (Restaurant's shelved equivalent: `RESTAURANT_SMART_IMPORT_DISCOVERY_V1.md`). `FL-007`'s baggage-allowance evidence and airline-reference ambiguity both confirmed correct on final retest (`V6-F44` Closed).
- A complete, unambiguous source address is the exact Hotel Location. A map link or coordinates are not required for source-extraction exactness. Flight route-type locations confirmed resolving correctly (`V6-F41` Closed); label wording is a minor Beta 0.8 candidate.
- If explicit property location is absent, the exact source Hotel name is the derived Location-field/search candidate.
- The named Hotel fields are not a ceiling.
- Every proposal requires a meaningful evidence-grounded title.
- Missing-in-source values do not block approval. Unreadable, ambiguous or conflicting present values stay visible as unresolved/needs-review. **Needs-review evidence must be correctly scoped to the actual field it concerns** — `V6-F43` confirmed fixed. **A field's needs-review warning must clear/re-evaluate whenever that field's value changes, by any means** — `V6-F35` confirmed fixed on both arrival and departure fields.
- Each proposed value carries direct field evidence and provenance.
- **An ambiguous or unresolved field must never block unrelated fields from populating.** `V6-F34` — confirmed fixed.
- **Duplicate detection must preserve non-empty trusted values, including a manually-resolved direction/location and any field populated as a result.** `V6-F33`/`V6-F39` — confirmed fixed on retest.

## Frozen bounded external-enrichment policy

- Source extraction is completed first. External enrichment never substitutes for source-relative completeness.
- Each processing attempt may retrieve at most three evidence-linked public pages and perform at most one structured place lookup.
- Every Hotel place lookup starts property-name-first; a generic park/dam/lake/island/city/region result is rejected or left `needs_review`.
- Every enriched field is labelled separately and retains field-level provenance.
- Login/session/CAPTCHA/credential-protected pages and broad crawling are excluded.
- The initial benchmark target is approximately USD 0.10 or less in variable third-party cost per import.

## Approved first vertical slice — Hotel

The first Alpha 0.6 technical spike and integrated implementation support text/scanned hotel PDFs, hotel images, public official hotel URLs, a new-Hotel proposal from global Add, and `Attach only`/`Attach and extract` from an existing Hotel. This is a complete architecture slice: immutable source identity, processing attempts, field evidence, proposal review, explicit approval, duplicate handling, multi-source relationships, attachment ownership, partial/failure states, authorization and audit/cost/latency measurement all apply.

## Required capabilities

### One source pipeline

- Camera capture, gallery image, PDF/file and URL all enter one source-ingestion pipeline with immutable original-source identity and traceable processing attempts.
- Manual item creation and validated `familytrips.import` / `FTI1` / `FTI1C` remain independent trusted paths.
- URL ingestion retrieves actual allowed page content through an approved server-side acquisition path.

### Reviewable structured result

- Every extracted value carries field-level evidence, source reference, extraction method and confidence.
- Values absent from the source or unsupported by direct evidence remain empty.
- Extraction creates a proposal only.
- Duplicate detection runs before approval and preserves non-empty trusted values. Confirmed on retest.
- An ambiguous field's unresolved status must never withhold unrelated fields. Confirmed fixed.

### Existing-item attachment

- `Attach only` is a reliable foundation.
- `Attach and extract` uses the same Smart Import pipeline and returns field-level proposals requiring approval.

### Safety and observability

- The user can cancel, retry, reject or keep the original source without accepting extracted data.
- Processing state distinguishes queued, processing, partial, needs review, failed, attached and rejected outcomes.
- Offline behavior is explicit.

## First-slice acceptance benchmark

The first-slice benchmark uses real, Product Owner-approved Hotel examples covering `H-001` through `H-005` plus the partial/missing-reference/companion/duplicate matrix rows — full detail preserved in `SMART_IMPORT_REDESIGN_DISCOVERY_V22.md`. The Flight benchmark (`FL-001`–`FL-008`) is preserved in `FLIGHT_SMART_IMPORT_DISCOVERY_V1.md`. **All 8 Flight cases pass in QA — GO. Production is live and the real-import test is confirmed.** The Attraction/Event benchmark is drafted but open in `ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md`, pending real Product Owner-supplied documents. The Restaurant benchmark drafted earlier is shelved in `RESTAURANT_SMART_IMPORT_DISCOVERY_V1.md` (`V2`).

## Explicit exclusions

- Multi-trip, sharing/synchronization, Shopping expansion, first-class Transport, task-model expansion, broad visual redesign and the remaining MVP-completion backlog stay in Alpha 0.7.
- Boarding-pass/Flight extraction is approved scope, sequenced to `0.6.5`. Attraction/Event ticket extraction is approved scope, sequenced to `0.6.6` (retargeted from Restaurant — `V49`; Activity was never actually blocked by the data model, see correction note above). Restaurant, Transport, Insurance and generic multi-type extraction remain excluded from `0.6.x` — Restaurant is shelved, not blocked, and is a real candidate for a later slice.
- The external-model `familytrips.import` and QR contract remains a supported fallback and test oracle.

## Alpha 0.6 release gate

Alpha 0.6 receives GO only after the approved end-to-end Hotel vertical slice passes its benchmark, all supported Hotel sources share the same proposal and ownership invariants, the bounded enrichment and measured accuracy/cost/latency gates pass, failure branches create no misleading data, and the exact tested build passes privacy, offline, schema, duplicate, persistence and regression gates. Flight (`0.6.5`) has its own equivalent gate, defined in `FLIGHT_SMART_IMPORT_DISCOVERY_V1.md` — **PASSED against QA. Production is live and the real-import verification is confirmed. `0.6.5` is fully closed.** Attraction/Event (`0.6.6`) will get its own equivalent gate once its type contract and benchmark reach Frozen status in `ATTRACTION_EVENT_SMART_IMPORT_DISCOVERY_V1.md` and its own independent Production promotion once implemented and accepted.

# Alpha 0.7 — MVP Completion and Hardening

*Full required-work, release-blocker and exit-criteria detail is unchanged from prior revisions — see `MVP_DELIVERY_HISTORY_V1.md` or the prior published version of this document. Two new product decisions added 2026-08-27, both confirmed by the Product Owner and assigned here:*

- **Task creation from the global floating "+".** Currently, creating a Task is only reachable from within the Preparations/Tasks section; the global Add ("+") flow does not offer Task as a creatable type. This is a genuine gap, not previously covered by any existing finding (`V5-F09` is a different, category-preset concern — it's about prefilling the current category's type when using Add *from within* that category, not about which types are available at all from the global entry point). Fix: Task must be a selectable type from the global floating "+", the same as any other manually-created item type.
- **Today page: Packing list shortcut, not individual items.** Open preparation Tasks already surface correctly in Today's pre-trip Quick Access (`V5-F13`, implemented Alpha 0.5.2 — up to 3 open tasks, overdue → today → nearest due → undated, with an "All tasks" overflow link). Packing/equipment list items currently do not appear in Today at all. Product Owner decision: do not add individual packing items into the same "top 3 urgent" Quick Access slots as Tasks — instead, add a shortcut link to the full Packing list (the same overflow-link pattern already used for "All tasks"), so packing stays visible and reachable pre-trip without crowding out the most urgent tasks.

*Whether to add cloud-synchronized Trip storage earlier than planned is worth revisiting during Alpha 0.7 planning. Backlog item carried from `V34`: a smarter Today-page data-quality alert design (for out-of-range/problematic items). New minor item from `V42`: Flight route-location label wording (full route vs. single airport code). (The open question about adding an explicit Activity item type is resolved as of `V49` — it already exists; see the Alpha 0.6 correction note above.)*

## 3. MVP Completeness Audit

*Unchanged from prior revisions.*

## 4. Features Beyond the Thailand MVP

*Unchanged from prior revisions — Expense tracking (deferred), product/platform expansion, intelligence expansion, social/content expansion, and the post-trip Magazine/PDF backlog entry.*

## 5. Beta Plan

# Beta 0.8 — Visual and Interaction System

Focus unchanged from prior revisions, including the carried-forward refinements: `V6-F14` (Hotel review notes field scoped down), `V6-F16` (unified "Add document" source-type selector), the Flight route-location label wording note from `V42`, and a new one from `0.6.6` QA acceptance (`V54`/`V56`): a one-click "accept new value" action for merge-conflict warnings (currently manual re-entry only, which is intentional/safe per the never-silently-overwrite contract, but could be faster for the common case where the new value is simply correct).

# Beta 0.9 — Usability and Real-World Refinement

Unchanged from prior revisions.

## 6. Release Candidate

# RC 0.10 — Thailand Release Candidate

Unchanged from prior revisions.

## 7. Feedback Classification Rule

Unchanged from prior revisions.

## 8. Working Responsibilities

Unchanged from prior revisions.

## 9. Next Actions

1. Alpha 0.6.4: QA-confirmed. **Production-live status uncertain — re-verify now that the current push is live (see baseline-correction note).**
2. **`0.6.5` Flight Smart Import: fully closed.** GO in QA, Production live — build `0.6.5-a915a440`, deploy `6a9464b101a1690008db8823`, state `ready` — and the live Hotel + Flight import test against Production is confirmed (2026-08-31).
3. ~~Product Owner signs in and drives the live import test~~ — **done.**
4. Remaining from the Production push: (a) re-verify Alpha 0.6.4's actual Production-live status directly — **still open**; (b) check Netlify AI Gateway billing/usage before real user traffic ramps up — **still open**; (c) decide who beyond the Product Owner and the QA account should be added to the Production allowlist — **still open, deliberately deferred.** The uncommitted test-fixture changes and the stray committed junk file are both resolved and pushed (`e0be719`, `128eaba`).
5. **`0.6.6` (Attraction/Event + three folded-in `0.6.5` Production findings): implementation complete as of `V53`, awaiting Product Owner install and acceptance.** All seven `AE-001`–`AE-007` benchmark cases and `V6-F49`–`V6-F51` have named, passing regression tests; the full quality gate passes; build identifier `0.6.6-128eabae`. **Next, and only the Product Owner can do this per the standard's responsibility split:** install the package, run `EXECUTABLE_TEST_SESSION_V184.md` one test at a time — including the mandatory `AE-001` wild-card test and, especially, the one still-open verification step neither the discovery document nor this implementation could close: a real official venue/museum/OTA URL test end-to-end (every URL tested so far was a ticket-resale platform, the side expected to safe-fail) — then assign any findings and decide GO/NO-GO. Production stays on `0.6.5` until that GO. Restaurant is shelved, not scheduled — `RESTAURANT_SMART_IMPORT_DISCOVERY_V1.md` (`V2`) stays available as a future candidate.
6. ~~Decide when to add an Activity item type to the roadmap~~ — **resolved as of `V49`: it already exists, no roadmap decision needed.**
7. Keep Alpha 0.6 limited to Hotel, Flight (`0.6.5`) and Attraction/Event (`0.6.6`) Smart Import and direct existing-item attachment; do not mix in Alpha 0.7 synchronization, Multi-trip, Shopping, Transport or task-expansion work.
8. After Alpha 0.6 fully closes (both `0.6.5` and `0.6.6` live and verified), proceed to the reassigned Alpha 0.7 scope — now also carrying: Task creation from the global "+", the Today packing-list shortcut, the smarter Today-alerting backlog item, and the Flight route-location label wording note — followed by Beta 0.8, Beta 0.9 and RC 0.10.

## 10. Process-rule pointers

Two process rules that were originally recorded directly in this delivery plan have a single canonical home now: `VERSION_PACKAGE_AUTHORING_STANDARD_V25.md`, under its "Responsibility split" and "Controlled-document revision rule" sections respectively. The original wording, preserved for the record, is in `MVP_DELIVERY_HISTORY_V1.md`.
