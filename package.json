{
  "name": "family-trips-alpha",
  "version": "0.3.0-foundation.1",
  "private": true,
  "description": "Family Trips Alpha 0.3 Firebase and Vite foundation",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node tests/smoke.mjs",
    "check": "node --check src/main.js && node --check src/firebase.js && node --check src/legacy-app.js && node --check public/service-worker.js && npm test && npm run build"
  },
  "dependencies": {
    "firebase": "^12.1.0"
  },
  "devDependencies": {
    "vite": "^7.0.6"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "allowScripts": {
    "@firebase/util": true,
    "esbuild": true,
    "protobufjs": true
  }
}
